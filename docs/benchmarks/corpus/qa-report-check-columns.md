# QA-REPORT — r10 复验（round-2）

> 冻结语料（2026-09-15 追加，只增不改）：缺陷表**新增的两列**（检测命令 / 通过判据）与**转义管道符**
> 的解析形状。来源形态：真实复验轮报告（P0–P2 行必须给「现在就能失败的命令 + 修好后的期望输出」，
> 命令里带 `|` 时按 HOST-ENFORCED 转义契约写 `\|`）。

## 1. 范围与环境

- 范围：语言层 + prompt 面 + 交付面。
- 环境：web profile，node 22，workspace-write。

## 2. round-1 缺陷复验对照

| 编号 | round-1 级 | 复验结论 | 独立证据 |
|---|---|---|---|
| R1-1 | P2 | **已关闭** | 工厂扫描 CJK 命中 0 |
| R1-3 | P3 | 维持现状 | 边界理由成立 |

## 3. 本轮缺陷

| 编号 | 严重级(P0/P1/P2/P3) | 功能模块 | 复现步骤 | 期望行为 | 实际行为 | 关联验收项 | 检测命令 | 通过判据 |
|---|---|---|---|---|---|---|---|---|
| R2-1 | P2 | prompt 语言面 | 复跑 17 个工厂 en 分支并扫描 | en 分支无中文字面量 | `prompts/index.ts` 4 处中文章节名 | AC-5 | `node logs/teamflow/tf-x/scripts/r3-scan.mjs` | CJK 命中 **0** |
| R2-2 | P1 | 缺陷解析面 | 跑 `parseDefects` 喂转义行 | 只出 1 条缺陷 | 多出 1 条（`retry \d+\|follow-up run` 转义行被切开） | AC-9 | `grep -nE "retry \\d+\\|follow-up run" src/` | exit 1 |
| R2-3 | P1 | 运行时 | 连续两次 resume | locale 快照稳定 | 第二次按界面语言重解析 | AC-2 | `node -e "console.log(1)"` | 输出 1 |
| OBS-1 | P3 | 观察项 | — | — | 全角标点残留 | — | - | - |

## 4. 结论

未达直接验收条件：2×P1 + 1×P2 待修复后复验。
