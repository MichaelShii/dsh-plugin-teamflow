# PRD · Markdown 表格对齐器（r1）

> 阶段：prd。本样本是**契约未兑现**的真实形状：目标宿主是别的框架，
> 但 PM 既没写「宿主契约调研」段，也没有任何"读过对方文档"的痕迹 —— 这正是硬门禁要拦下的 PRD。

## 1. 背景与目标

把选中的 Markdown 表格按列宽对齐，一键复制回剪贴板。

## 9. 假设与待澄清

- 假设：表格无外层竖线时也按列对齐。
