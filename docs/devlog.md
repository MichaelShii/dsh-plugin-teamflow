# TeamFlow 开发日志（devlog）— 迭代变更流水

> **本文件承载「历史变更流水」，只供人读/按需查阅——不注入 agent 会话**（AGENTS.md 只含当前状态；
> 历史由 git log 与本文档追溯，避免 token 常驻与决策污染，2026-09-03 起执行）。
> 维护规则：功能落地后，变更流水写本文件 + git commit message；AGENTS.md 仅在「当前状态」
> （§1-6：结构/约定/待办）变化时更新。

## 功能演进史（v0.3 → v0.1.0 发布前内部迭代；对外统一归 v0.1.0，见 CHANGELOG.md）



| 版本 | 核心变更 |
|---|---|
| v0.3~0.6 | journal 断点续跑（ADR-0001）/ AGENTS 最小侵入（ADR-0002）/ 防假交付(实质校验+熔断)/ 并发池 / QA 缺陷登记 / 完成汇报 |
| v0.8.0 | token 双口径 + 成本观测线 + lite 模式 + 部署契约（ADR-0003/0004） |
| v0.8.x | 领域化重构（11 文件）+ triage 5 档(model 驱动)+ lite/tech/patch 端到端 + 需求无效拦截（ADR-0004/0005 落地） |
| v0.10 | 多团队架构(teams.json)+UI"+团队"触发+workspace 级隔离(UUID)+单任务轮转+dev 子卡+官方口径 token 展示+会话暂停/resume+state.json 预编译索引+版本切片/一次成型纪律+子代理路由跟随主线程 |
| v0.10.1 | **验收结论解析修复**（误报实锤 run tf-msytlok5：验收 ✅ 通过，记忆回写段「SUMMARY.md 结构无需改动」命中旧正则「无需改动」→ 误判 reject 杀整条流水线）。`parseAcceptanceVerdict` 移入 util.ts：只认显式「验收结论/整体结论」行 + 专用「📝 需求不适用」全文命中，正文散文不再朴素子串匹配；verdict.test.js 回归覆盖 |
| v0.10.2 | **执行路径基准**（同一持久化需求 A/B）：流水线 38m/164 调用/11.6M billed vs 原生 DSH 27m/115 调用/19.1M billed。流水线省 ~39% token（阶段/子代理上下文隔离），原生快 ~29%（少门禁但单 agent 上下文膨胀）；质量等价。**拆分价值在「上下文隔离」而非并行次数**。详见 `docs/benchmarks/pipeline-vs-native.md` |
| v0.13+ | **执行路径基准（小需求样本，r14 Hold 暂存方块）**：流水线 47m/393 调用/17.48M billed vs 原生 28m/120 调用/9.69M billed——小需求上流水线反而多花 44% token（固定开销×子 agent 数占比高，规模拐点实证）；但流水线交付 470 行 Hold 专项测试 + AC 逐条验收，原生缺 hold 音效（AC-16/17）、测试零覆盖。**复核（二评）：多花的 44% 是质量预算非浪费——billed 口径 96% 为 cacheRead 但命中价≈input 1/10（真实差距 ~1.4M 等价）；「共享已读文件」判定为假优化（注入=前缀全价重放），真优化=devPrompt 注入按任务切片 + 测试任务合并**。详见 `docs/benchmarks/hold-pipeline-vs-native.md` |
| v0.11 | **「认知前置 + 架构落地」重构（破坏性，ADR-0006）**：① M0 状态核对（core/sanity.ts，start 跑 git 现状注入各阶段，治"认知过期/场外提交"）；② M1 架构阶段全模式启用（lite 也跑轻量架构蓝图，architectPrompt/techPrompt 产结构化 JSON 蓝图，state.__runCtx 统一注入）；③ M2 dev 继承蓝图 + 按蓝图自动拆任务（devTaskDefs 蓝图优先+文件冲突检测合并）；④ M3 质量门禁（QA/验收加架构核验，parseAcceptanceVerdict 识别架构打回）；⑤ triage 架构护栏（持久化/存储/独立模块等 → 强升 medium）。质量优先于 token：不砍「建全局认知」；原生工作流基线见 `docs/benchmarks/native-workflow.md` |
| v0.12 | **QA 打回修复闭环（ADR-0007）**：QA 发现 P0-P2 阻断缺陷 → 打回开发确认+修复（qaFixPrompt）→ 复验 QA → 干净才进产品验收；`QA_REWORK_LIMIT=2` 上限防无限循环、超限转 needs-human 人工介入；`parseDefects` 容忍 `**P1**` 加粗严重级（修对照实验 tf-mt317a5e 缺陷漏登记）；缺陷卡按 reqId+defectId 幂等登记、复验通过 `verifyReqBugs` 关单（P3 观察项保留）。实证：docs/benchmarks/pipeline-vs-native.md「复核」节 |
| v0.1.0（首次公开发布） | **文档层重构：活文档版本制 → 任务夹收口制（ADR-0008，破坏性）**：每需求一个自包含任务夹 `docs/teamflow/<yyyyMMdd>-r<N>[-<slug>]/`（PRD/TECHNICAL/QA-REPORT/ACCEPTANCE 收口其中），host 建夹命名+meta.json，`journal.runDocs` 固定需求级身份——重试/续跑复用同夹，双归档/版本虚增/memory 堆积三类 bug 结构性消失；SUMMARY.md 废除（扫描 meta 聚合）、memory.md 收窄为约定层；AC 局部编号+基线依赖/取代声明；代码头 VERSION 解耦为发布版本；triage 新增 slug 输出。存量项目零迁移 |

## 迭代变更流水（2026-08-19 起）
- 2026-09-23（第二十九批：**v0.2.0 定稿发布** —— 线上 0.1.9 在 dsh 0.1.7-alpha.1 下不可用，本批把开发线收口成可发的版本）：**范围（git 对比，非记忆）**：`git log v0.1.9..HEAD` = **69 提交**、`git diff --stat` = **90 文件 +5,785 −425**；`package.json` 版本号与 `engines.dsh: ">=0.1.7-alpha.1 <0.2.0"` 在开发线内已 bump，本批只做定稿动作。**发布动因（用户视角的「插件完全崩溃」）**：npm `latest` 仍是 0.1.9，而 0.1.7-alpha.1 的 v4 宿主在事件被 Session 采纳前校验 `source.kind` 并要求 producer-owned 形态（`plugin:dsh-plugin-teamflow`）——0.1.9 发的是 v3 wrapper `{kind:'plugin', plugin}`，**所有实时注入事件被当场拒收**（团队上下文 inject / 完成汇报 / 护栏提醒三处），用户侧表现就是插件整体不可用；该适配在开发线内已完成（第二十六批记录），故 **0.2.0 是唯一能跑在新宿主上的版本**（0.1.6-alpha.2 及更早也不可回退：双向互不兼容）。**本批定稿三件**：① `CHANGELOG.md` 段头 `未发布` → 发布日期 + 定位说明（详细论证归 `docs/releases/`）；② 新建 `docs/releases/v0.2.0.md`（**按 skill 铁律 1 由 `git log v0.1.9..HEAD` 生成**，不是凭会话记忆写功能清单——这正是 v0.1.2 发版事故的防法），中英双语 + 升级要点 + 已知限制；③ `docs/TODO.md` 补录本批实测出的 6 条未完成项（CHANGELOG 论证收口、熔断预算作用域、§5 余量 143 B、宿主沙箱 ACL 跟踪、事件 v4 缺直接断言、跨父会话跳转待实测）。**发布前验证（全部实测）**：`pnpm test` **21 套件全绿**（退出码 0）+ `pnpm run typecheck` 退出码 0 + `npm pack --dry-run` = **10 文件 / 305.9 kB**（`lib/*.mjs` ×4 + 2 README + CHANGELOG + package.json + cordis.patch.yml + LICENSE；**无** AGENTS.md、无内部文档、无 sourcemap）+ 三道文档门禁**反向自测 4/4 按预期变红**（AGENTS.md 写日期 / §5 超 6 KiB / CHANGELOG 塞历史性命中 / 客户端引 `sessions.openSubagent`），每次变形后原文与哈希均还原、工作树 `clean`。**发布后流程（本仓 anchor 已定，勿回退）**：`release-v0.2.0` → PR → **合入 `main` 并保留全部提交（`--merge`，不用 squash —— 维护者本批发版时拍板改的规则，理由：release 分支发布后删除，squash 会让整条开发过程在 main 上消失）**（main 唯一入口）→ `git tag -a v0.2.0` 打在 main 的发布提交上 → `gh release create --notes-file docs/releases/v0.2.0.md` → 另起下一条 release 分支（届时 bump 版本号 + AGENTS.md §1「当前状态」行）。**未收口（已记 TODO，非阻塞）**：CHANGELOG `[0.2.0]` 段仍留着 35 条取证型论证（`test/changelog.test.js` 存量基线 50 未下调）—— 属「债不是 bug」，发布后随时可做。**发布后同日三件**：① **上游上报已提交** —— `deepseek-ai/deepseek-harness` Discussions [#7538](https://github.com/deepseek-ai/deepseek-harness/discussions/7538)（该仓 public 但 **issues 关闭**，官方通道是 Discussions，见其 `CONTRIBUTING.md:11`；标题 `[BUG REPORT] Windows 沙箱无法在用户自建目录上 provision 工作区 ACE → 该目录下所有 shell 命令失败`，正文即 `docs/reports/2026-09-23-dsh-windows-sandbox-acl-provision-failure.md`）。② **按流程另起下一条开发线 `release-v0.2.1`**（提交 `4dd4ab1`，已推送并设上游）——同步三处版本锚点：`package.json` 0.2.0→0.2.1、`test/smoke.js:482` 版本硬断言（含「release-vX.Y.Z 开发线」标签，**不同步会直接让 smoke 变红**）、`AGENTS.md` §1「当前状态」行；`CHANGELOG` 暂不开 `[0.2.1]` 段（按 v0.2.0 先例，在首个实际改动落地时开）。③ **有意未改 `CHANGELOG.md:66`**（`[0.2.0]` 段仍写「squash 合入 main」）——它随 npm 0.2.0 已冻结，改它会让「main 发布面 == npm 包内文件」这条 SOP 不变量失效（本批已实测该不变量成立：`git diff 2a540f6 HEAD -- lib cordis.patch.yml README.md README.en.md CHANGELOG.md package.json` 无差异）；现行规则由两个**不进包**的文件承载，规则变更记进下个版本的 CHANGELOG 即可。④ **v0.2.0 对外通稿（两版）**：新建 `docs/announcement/show-your-plugin-v0.2.0.zh.md`（22,940 B / 226 行）与 `…en.md`（25,278 B / 224 行）——两份**完整独立**正文（不是 v0.1.8 那种「中文正文 + 英文摘要」），各配 6 张对应语言截图（zh `docs/screenshots/` / en `docs/screenshots/en/`，12 个链接逐一核对存在）；旧稿按文件头约定冻结、只加一行指向新稿。**三条对外口径（均已在文件头留痕）**：① **默认发英文稿**，中文稿作为正文首行的跳转目标；② 语言跳转分两套：**仓库内用相对路径**（与 README.md:5 / README.en.md:5 同款，仓库页永远有效）、**发出去的帖子里用帖子互链 URL**（相对路径贴进 Discussion / 站外即死链；帖子 URL 永久有效且不依赖分支 / commit / tag——曾试过「SHA 钉提交」，但它与「把本批合成一个提交」直接冲突，且解决不了自指；「打 tag」方案经维护者否决）；③ **不展开第三次对照测量（`assetd-clarity-vs-gate` 平局）与自评式措辞**（完整数据仍在 `docs/benchmarks/`，正文已给出目录链接，**不是删数据**）。标题落成主选（2026-09-23 依上一版标题血缘调整）：中文 `dsh-plugin-teamflow v0.2.0：把「一句话需求」跑成可验收交付`、英文 `dsh-plugin-teamflow v0.2.0 — from one sentence of requirements to an accepted delivery (multi-agent pipeline for DeepSeek Harness)`——两者均以**包名开头**，与 v0.1.8 原帖标题「dsh-plugin-teamflow：把「一句话需求」跑成一条带质量门禁的多 Agent 研发流水线」同族；原帖无版本号，承载不了本版最硬的信息「0.1.9 在新宿主上不可用、必须升 0.2.0」，故补 `v0.2.0`。备选（论坛带前缀风格）：`[插件] TeamFlow v0.2.0 —— dsh 多 Agent 研发流水线：一句话需求 → 可验收交付`。**投放记录（2026-09-23 补齐）**：上一版通稿发于 `deepseek-ai/deepseek-harness` Discussions **#6405**（2026-09-12 07:26 UTC），当时标题「dsh-plugin-teamflow：把「一句话需求」跑成一条带质量门禁的多 Agent 研发流水线」（`gh api graphql` 实测元数据，非记忆）；该帖三条评论（2026-09-14）中 @tongwoojun / @zweix123 的「意图澄清」反馈 → 第十二批需求澄清闸门（新稿 ②），@boshk0 的「Is there English translation?」→ **本版默认发英文稿的直接依据**；已回填进旧稿文件头、两版新稿跳读行/链接区与 devlog。**更正一条中途结论**：先前记「仓库里无该帖记录」不准确——编号早已在 `docs/devlog.md:44` 作为「社区讨论 …#6405」出现，只是没有 URL 形式（按 `discussions/\d+` 搜不到）。
- 2026-09-23（第二十八批：**客户端宿主 API 面迁移——跳会话入口换成 uiWorkspace.openSession + 契约面门禁**）：**症状**：工作台详情浮层「🎬 跳转子代理会话」长期灰着，全局面板产物跳转静默降级为「当前会话右栏内联打开」。**根因（宿主侧）**：dsh 0.1.7-alpha.1 的 2026-09-17 refactor 批次（`6830e1460d`/`b9b14dc05e`/`6d3b5d4526`）删除了 `sessions.openSubagent` 与 `sessions.open`，我们的守卫 `typeof sessions.openSubagent === 'function'` 恒为 false；第三处同源死代码是 `sessions.list.getSnapshot().current`（`SessionListState` 已无 `current`，只剩 ids/byId/phase/projectionsBySession）。**排除项（实测）**：run journal 里 18/18 阶段 `childId` 全部落盘 → 数据面完好，问题纯在客户端 API 面；**本次 0.1.7-alpha.1 兼容性核查当时只覆盖 session-format v4，漏了客户端服务 API 面**（该核查结论应视为不完整，已在本批补齐）。**修法**：`client/index.tsx`（inject 加 `uiWorkspace`、抽掉 `sessions` 死依赖、`hasChild/openChild` 改 `uiWorkspace.openSession({parentSessionId, childSessionId, mode:'one-shot'})`、props 与两处 slot inject 桥接同步）+ `client/panel.tsx`（`goOwnerSessionAndOpen` 改 `uiWorkspace.openSession(ownerSession)`，删 `current` 死判据、保留时间维度重试）。**收益**：新 API 是真跳转（替换 mainView 并让主区回会话），旧 `openSubagent` 只跳不切视图。**门禁**：新增 `test/client-host-api.test.js`（宿主成员冻结白名单 + 禁已移除成员 + inject/桥接一致性 + 无 sessions.* 引用时不得保留 sessions 死依赖），接入 test 与 prepublishOnly（21 套件）；AGENTS.md §2/§5「客户端面」同步更新。
- 2026-09-23（第二十七批：**文档分层治理——AGENTS.md 瘦身 + CHANGELOG 定位 + 指令预算门禁**）：**动因（实测）**：AGENTS.md 涨到 64,134 B，吃掉宿主 workspace 指令预算（64 KiB）的 98% → 宿主直接 `omitted ~/.dsh/AGENTS.md`，**用户自己的全局规则被整份丢弃**；其 §5「行为锚点」占全文 74%（47,456 B / 19 行，最长单行 7,386 B），且含 24 处「实锤」、36 个日期、20 个 runId —— 属**历史**，违反本文件 §6 自定的「历史不注入本文件」。**修法三件**：① 论证/实锤/门禁细节迁 `docs/anchors/`（18 份锚点详情 + 3 份段落详情：测试覆盖明细 / 发布与分支流程 / 宿主级插件两条硬约束 + README 索引，**逐字迁出、不新增主张**）；② §5 改 18 行一行式不变量 + 指针（47,456 → 5,741 B），全文 **64,134 → 19,766 B（−69%）**，日期/runId/「实锤」清零；③ 门禁 `test/instruction-budget.test.js`（全文 ≤22 KiB / §5 ≤6 KiB / 单行 ≤400 字符 / **禁写历史** / §5 指针双向存在 / anchors 无孤儿 / §2 索引的 test 文件存在）。**同批 CHANGELOG 定位**（不做大搬迁，留待发版）：未发布段加「⬆️ 升级要点（TL;DR）」6 条用户判据；`test/changelog.test.js` 两条门禁 —— 已发布段必须有 `docs/releases/<v>.md`、**历史性内容命中数只减不增**（存量基线 44 处，发 0.2.0 时随论证迁入 release note 应归零）。**定位差异记明**：AGENTS.md 是每轮注入的上下文（有功能损害，必须瘦）；CHANGELOG 是随包发行的产物（属债，发版前处理即可）—— 二者优先级不同，不可混为一谈。
- 2026-09-23（第二十六批：**环境不可用早停（env-unavailable）——把「工作区起不了命令」从「模型输出被截断」里分离出来**，probe-v4 实锤）：**触发**：非系统盘新建目录（随手 `mkdir`）里跑 csvstat CLI 需求，首个需要 shell 的阶段失败——旧行为把真因**误报成 `stopReason=max-tokens`**（11 次调用 / 52,588 输出 / 3 分 45 秒），真因是每次 `pwsh` 都返回 `Error: SetNamedSecurityInfoW failed (Win32 5): grantWrite(<workspace>)`（宿主 Windows 沙箱 ACL provision 失败；机制与取证见 `docs/reports/2026-09-23-dsh-windows-sandbox-acl-provision-failure.md`）。**护栏也太晚**：实测模型在第 **2** 次相同失败后就听劝停手、改走「文件工具绕道」，WARN=3 从未触发——它用 13 个文件 / 69,848 输出「把活干完」（seq=3 累计 83,496 输出，被人工中断），而那种「完成」无法验证。**修法（三件）**：① `constants.ts` 阈值 `GUARD_TOOL_FAIL_WARN 3→2` / `ABORT 5→3`（按实机校准：提醒必须早于模型放弃）；② `guard.ts` 新增信号 D **环境不可用**——判据只用**结构化** `message.isError`（`util.isToolErrorResult`，文本正则仅兜底）、按 `工具名::归一化错误` 指纹计数（`toolFailureSignature`）；WARN 档落 `stage.envUnavailable`（供 runner 判定）+ `guard.toolFail` 日志 + 注入 `[ENV GUARD · reminder]`，ABORT 档 `fire(reason, 'env-unavailable')`；③ `runner.ts` 让环境不可用**优先于「完成了」**（`if (stage.envUnavailable)` 提到成功分支之前）+ `withRetry` 对它不自动重试，直接 `humanIntervention`（outcome 名刻意不含 `context|limit|token|tool-error|400` 等 `isUnretryable` 词，避免被当成模型侧不可重试错误）。prompt 侧 `productCtx` 增 `[Env unavailable · policy]`：同一命令以同一错误连续失败即**停手上报**（附工具名 + 原始错误），禁止换命令绕道、禁止用推理代替执行。**实测（同一工作区、同一故障）**：52,588 输出 / 3 分 45 秒 → **1,510 输出 / 15 秒**，outcome 正确落 `env-unavailable` 并点名环境与原文错误，且不再自动重试。**门禁**：`smoke` 3q（源码断言 + 阈值 + **优先级顺序** + host/core 清单完整性）、`diagnostic`（`isToolErrorResult` / 指纹 / 真值表含生产阈值 2/3）、L1 `ENV-UNAVAILABLE-STOP`。**顺带纠正两处我自己的错判**：本批排查中我先后把"profile 副本陈旧/偶发失败"当结论——真因分别是**我脚本里文件名写错**（`test/smoke.test.js` ≠ `test/smoke.js`）与 **`deploy.mjs --sync | Select-Object -First 3` 提前终止了上游进程**（同步写一半被杀）。
- 2026-09-23：**dsh 0.1.7-alpha.1 适配——session-format v4 producer-owned source 校验（probe-v3 实锤整轮 run 失败，破坏性）**：升级到 dsh 0.1.7-alpha.1 后，宿主 `packages/session/session-format-v3-to-v4` 在事件被 Session 采纳前（`validation.ts:115` `assertReleasedV4Relationships`）校验每条事件的 `source.kind`，**拒绝 v3 退役的 `{kind:'plugin', plugin:'x'}` wrapper**（`message-sources.ts:9`：`format v4 message requires a producer-owned source kind`），要求带命名空间的 `kind:'plugin:<name>'`（`producer-owned`）。插件所有实时注入事件——`host/index.ts` 团队上下文 inject（两处）、`host/core/report.ts` 完成汇报（`deliverCompletion`）、`host/core/guard.ts` 护栏提醒——用的都是旧 wrapper；probe-v3 是新起 run，**事件写入即被拒、journal 没落盘**（目录里只有 `teams.json`）。修法：四处 `source` 全部改为 `{kind:'plugin:dsh-plugin-teamflow', ...}`（删 `plugin` 字段、保留 `form`/`summary`）；`test/smoke.js` 新增回归断言（`plugin:dsh-plugin-teamflow` 全池命中 + 无退役 wrapper 写法；并把原先检查旧 wrapper 的「notice 来源标记」断言同步更新为新写法）；`pnpm run bundle` 重建 `lib/` 后**重启 `dsh --profile web` 才生效**。注：`runner.ts:172` 的 `session.append('tool-workflow/agent-start', …)` 不带 message/source 槽位，不在 v4 source 校验的遍历范围内，无需改。**同日兼容性核查（无其余破坏性项）**：逐个核对插件依赖的宿主契约在 0.1.7-alpha.1 下均稳定——typert 严格描述符仍强制 `create()`（`descriptors.ts` 已满足）；`subagents.start(name, request)` + `SubagentRun.{id,localAgent,result,dispose()}` 签名未变；`tokenUsage` 投影四桶名（`uncachedInputTokens`/`outputTokens`/`cacheReadTokens`/`cacheWriteTokens`，`stateVersion:2`）与 `sessionStats.steps` 调用数来源与 `host/core/metering.ts` 读取完全一致；`agent.inject/followup/steer`、`ctx.get('settings').get('locale').preference` 只读端口、`ctx.remote.$mount({package, descriptors})`、`sessions.openSubagent`、`sidebarRight.openResource`、`ctx.locale.*` 全部兼容。结论：0.1.7-alpha.1 下**仅 v4 source 一项需适配**（已落地）。README「基于 0.1.6-alpha.2」应更新为「已适配 0.1.7-alpha.1（含 v4 producer-owned source）」。
- 2026-09-21（第二十五批：**安装入口探测的主源改成「宿主权威锚点」，不再依赖 `DSH_HOME`**——用户实锤追问：「DSH_HOME 这个变量是安装了 dsh 就自带的吗」→「用户安装了 dsh 之后，在安装了我们插件，DSH_HOME 也不会存在吧」——**用户是对的**）：上一批把「探测」当成了修好的事，但它的主源选错了，**默认安装的机器上会误判成"探测失败"**。**取证（四条）**：① `DSH_HOME` 是**可选覆盖变量**而非安装产物——`packages/util/home-paths/src/index.ts:18` 定义 `DSH_HOME_ENV='DSH_HOME'`、`:87-91` 优先级为「显式配置 > `$DSH_HOME` > `~/.dsh`」，`defaultDshHome()=join(homedir(),'.dsh')`；② 装 dsh **不会**写它——本机实测 `[Environment]::GetEnvironmentVariable('DSH_HOME','User')` 与 `'Machine'` **均为空**；③ `.env` **永远设不了**它——`packages/boot/app-boot/src/index.ts:117` `BOOTSTRAP_PREFIXES=['DSH_',…]` + `:167-180` 命中即抛错；④ 宿主进程里它通常 **undefined**。**我曾被自己的观测误导（撤回）**：本轮 `pwsh` 打出 `DSH_HOME=C:\Users\gyech\.dsh`、`cmd /c set DSH` 也有，我据此在 `util.ts` 写了「宿主注入的环境变量，实测存在」——**这句是错的**。真相：`packages/shell/shell-env/src/index.ts:99` 构造时 `resolveDshHome()`、`:150-152` `collect()` **每次 shell 调用现算一个受管 overlay** 交给 `tool-bash:340`/`tool-pwsh:361`，官方 README 明写「shell executor **discards inherited `DSH_*` values** … `process.env` is **never** modified」→ **shell 里的观测不能用来推断宿主进程环境**（值不是继承来的，是现算的）。**真正的缺陷**：`detectInstallEnv` **已经把答案算出来了却丢掉**——`profileFromModulePath()` 同时返回 `home`/`dir`，旧实现只取 `.profile`、改从 `opts.dshHome` 重拼 → `ok = !!home && !!profile && !!profileDir` → 默认安装下 `home=''` → **`ok=false` → PRD 被降级成「问用户 profile 在哪」**（源码运行 / 一键安装都可能中招）。**修法（用户拍板 A：主源换成宿主给的权威锚点）**：① **`ctx.baseUrl` 就是当前 profile 目录**——`apps/cli/src/profile-boot.ts:315` `rootConfig=join(profile.dir,'cordis.yml')` → `packages/boot/app-boot/src/index.ts:799` `ctx.baseUrl = pathToFileURL(dirname(absoluteConfigPath))`，且该赋值在 `:801`(Loader)/`:804`(挂载插件树)**之前**，Cordis `extend()` 原型继承（`cordis/lib/index.js:1703`）→ 插件构造时即读得到；**实测**（真 cordis）：`root.baseUrl='file:///C:/Users/u/.dsh/profiles/web/'` → `plugin ctx.baseUrl` 与嵌套 service 读到的**都是**该值。新增纯函数 `util.profileDirFromBaseUrl`（`file://` → `decodeURIComponent` + 去 Windows 前导斜杠；容忍 `cordis.yml` 文件名段；非 `profiles/<name>` 形态返回 null）；② 取值顺序改为 **`baseUrl`（主源）> `modulePath`（次源）> `dshHome()` hint（只补 home）**，`ok = !!profile && isAbsolutePath(profileDir)`（新增 `isAbsolutePath`，跨平台 POSIX/`C:\`/UNC）——**刻意不含 `DSH_HOME`**，且**要求绝对路径**（相对目录照做不了；实测旧版 `baseUrl='profiles/web'` 会错报 `ok=true`，已堵）；③ `ctx` → pipeline 经 **`context.installCtx` 单例 + `setInstallCtx(ctx)`** 搬运（与 `setRuntime` 同款，pipeline 无 ctx 且链宿主私有 peer 取不到，故不反向依赖），`index.ts` 构造时登记；④ pipeline 调用点改 `detectInstallEnv({ baseUrl: installCtx.baseUrl, modulePath: selfModulePath(), dshHome: dshHome(), hasCli: cliOnPath() })`，**删掉裸 `process.env.DSH_HOME`**。**为何 `baseUrl` 优于反推**：`link:`/junction 安装下 `import.meta.url` 会解析到**真实目标**——实测建 junction 后 import 得到 `file:///C:/…/junc-probe/real/lib/host.mjs`（probe-v2 的 `dsh-plugin-md-table-align` 正是 `<JUNCTION>` → 反推必落空），而 `baseUrl` 不碰模块路径。**store.ts 一行未动**：`dshHome()` 的 `process.env.DSH_HOME || join(homedir(),'.dsh')` **本来就是对的**（有兜底），22 个调用点（`teamflowRoot`/`stateFile`/`teams`…）自动继承——实测全仓 72 处 `DSH_HOME` 字样里**只有 2 处真读环境变量**（那处已修 + 存储那处本就带兜底），故改动面是 3 个文件约 20 行而非"很大"。**门禁**：`triage-gate[13]` 新增 20 条（baseUrl 三形态/跨平台/URL 解码/容忍文件段；**「不给 `dshHome` 也必须 ok=true」双向回归锁**（旧缺陷正是这条）；junction 场景 baseUrl 兜住；`baseUrl` 非 profile 形态（源码仓）→ `ok=false` 不误判；相对目录 → `ok=false`；POSIX 根拼接不产双斜杠；`isAbsolutePath` 正负例），并新增源码断言「pipeline 代码中不得读 `process.env.DSH_HOME`」（**剥注释后匹配**——本仓有在注释里点名反例的习惯，直接全文匹配会把解释性注释误判为违规，首跑即撞上）。**变异实测（6 条）**：`ok` 退回认相对目录 → 红；忽略 `baseUrl`（主源失效）→ 红；`profiles` 段不校验 → 红；URL 不解码/不去前导斜杠 → 红；pipeline 退回读 `process.env.DSH_HOME` → 红；恢复 → 全绿。**一条等价变异（诚实记录，非漏洞）**：给 `ok` 再加 `!!home` **杀不死**——`ok` 要求 `profileDir` 为绝对路径、而 `profileDir` 与 `home` 同源派生 ⟹ `home` 必非空；覆盖输入空间实测「与现值结论不同的用例数 = 0」。另**修正我自己写错的断言**：`file:///profiles/web/` 是**合法绝对路径**（原断言期望 `ok=false`，是断言错不是代码错），改为断言拼接不产 `//profiles/web` 双斜杠。全量 19 套件 + L1 + L2 23/23 + typecheck 全绿。
- 2026-09-21（第二十四批：**安装入口改为运行时探测 + 明确「安装归主 agent」** —— 用户三句话纠正了我两次）：**触发**：用户先说「**因为我机器上的 dsh 我都在这个目录下 E:\Code\OpenSource\deepseek-harness，然后 pnpm dsh，因为我走的是源码运行，不是那个一键安装**」，我据此提出"写人工步骤"；用户随即反问「**子代理做不了，不能让主代理去做吗？**」；我核实后给出机制，用户再补一句「**不只是兼容我的环境，因为我这是个 toc 插件，每个用户环境不一样，路径不要写死，要用灵活可靠的方法**」。**① 事实核查（三次，每次都推翻我上一次的推断）**：**(a)** 你的运行方式实测 = `scripts.dsh = node --import tsx/esm apps/cli/src/bin.ts`（源码直跑 tsx），且**当前 `dsh` 不在 PATH**（`Get-Command dsh` 无结果）——这正是 probe-v2 的 T5 "where dsh 无结果"的真因；**(b)** 我原先推断"主线程有特权所以能装"——**错了**：实测主线程沙箱**同样是 `workspace-write`**，而且它改 profile 时**真的被拒了**：`edit → .dsh\profiles\web\package.json` → `[sandbox: file access denied under workspace-write mode]`，**紧随其后宿主给出** `[sandbox: escalation available — retry this exact operation once with sandbox_permissions + justification]`，`approval/policy: ask` → **经用户批准才装上**；而子代理会话里明写 `your permission scope was fixed when you were started and cannot be widened` → **没有这条通道**。结论：**不是特权，是"人工授权闸门"**；无人应答时 fail closed。**(c)** 我随后又推断"那就只能写人工步骤"——**又被用户纠正**：主 agent 有权限通道且在会话里（用户能当场批准），所以**执行者应是主 agent**，不是"让人自己摸索"。**② 修法（三件）**：**(a) 探测替代写死**——新增 `util.detectInstallEnv`/`profileFromModulePath`/`installRecipe` 三个纯函数，事实全部运行时得到：`DSH_HOME`（宿主环境变量，实测存在）+ **插件自身路径反推 profile**（`$DSH_HOME/profiles/<name>/node_modules/<pkg>/lib/host.mjs`，跨平台、不依赖任何配置）+ `dsh` 是否在 PATH；pipeline 起跑探测并双注入（`__runCtx` 供 prompt、`journal.installEnv` 留痕+落盘）；**探测失败 → `ok=false` → PRD 必须改为"问用户"，绝不编路径**。**(b) 等价手动步骤不是我猜的**——读宿主 `apps/cli/src/plugin.ts` 的 `runPlugin`：语义 = 「`pnpm add <spec>`（cwd=profile 目录）→ `reconcilePlugins`：依赖里**声明了 `dsh.bundle.patch` 的包自动进 `dsh.profile.bundles`**」→ 源码运行下的手动路径与 CLI **等价且机械可推导**。**(c) 汇报带可执行指令**——完成汇报新增「🧩 安装待办（需主 agent 执行）」：本机 profile 目录 + 本机命令形态 + 回滚；探测失败改为"先问用户"。**③ 门禁**：`triage-gate` [13]（纯函数正负例 + 跨平台 + 失败降级 + 三态文案 + 接线）+ 契约层断言（**不得出现写死的 `--profile web`**、以探测为准、要问用户、安装归主 agent）+ L1 `PRD-INSTALL-ENV-PROBED`(zh/en) 与 `PRD-NO-INSTALL-ENV-WITHOUT-PROBE`。**④ 变异实测（7 处全红）**：契约写死命令 2 红 / 探测失败仍编路径 4 红 / 反推不校验 node_modules 2 红 / 失败文案不问用户 2 红 / pipeline 写死 profile 2 红 / prdPrompt 不注入环境段 L1 10 红 / installEnv 只写不落盘 2 红；恢复 → 19 套件全绿、L2 23/23、typecheck 无错。**⑤ 教训（本批第三次自我纠正）**：三次推断（"主线程有特权"→"只能写人工步骤"→"路径写死即可"）**全部被实测或用户否定**；凡涉及"谁能做什么/什么路径存在"，**必须读源码或查会话原文**，不能顺着话往下推。
- 2026-09-21（第二十三批：**契约样本来源改成「本机已装插件」** —— 用户一句反问戳穿了"指向我本地目录"的前提）：**触发**：讨论 probe-v3 用哪个目录测时，用户反问「**你这有问题啊，指向的是不是都是我本地的目录，那如果用户没有的话会导致什么结果**」。**① 先取证，两条实测**：`npm pack --dry-run` → 发布包**只有 10 个文件**（`lib/*.mjs` ×4 + `package.json` + `cordis.patch.yml` + 2 个 README + CHANGELOG），**源码 `client/index.tsx` / `host/index.ts` 根本不发布**；而旧 prompt 写的是「必须去读**本仓**已有的同类插件样本（`plugins/dsh-plugin-teamflow`）」—— **本仓相对路径**，只在工作区位于我们仓库 `plugins/` 下时成立。**② 后果定性（回答用户的"会导致什么结果"）**：npm 用户的孤立工作区里 ① 没有那个路径 → PM 读空或跳过 → 契约里「禁止凭记忆写字段名」变成**无法执行的空话**；② 就算改成指向我们插件自己的安装目录，那里也只有**编译产物**、没有源码样本 → 让 PM 读产物当样本比没有更糟。**③ 附带纠正我自己的一个错误推断**：我先前担心"子代理受文件沙箱限制读不了 `$DSH_HOME`"——解压 probe-v2 全部 12 个子代理会话核查：`FS_SANDBOX_DENIED` **命中 0 次**，`file access denied` 唯一命中处是**系统提示文本本身**（"a blocked file operation is reported as ..."），子代理**确实成功读了工作区外的文件** → 读操作不受限，方案可行（又一次印证：推断必须核）。**④ 修法（不写死任何绝对路径）**：`LOCAL_PLUGIN_SAMPLES_HINT = '$DSH_HOME/profiles/*/node_modules/<任一 dsh 插件>/'` 作**首选**样本来源——任何 dsh 插件开发者机器上必然存在（实测本机 profile 有 4 个：`dsh-plugin-teamflow` / `dsh-plugin-md-table-align` / `dsh-plugin-standup-reminder` / `dsh-workbuddy-connect`，**都带 `dsh` 声明块 + `cordis.patch.yml`**），其 `package.json` 的 `dsh` 块与 `cordis.patch.yml` 正是「声明到底怎么写」的权威现场；**本仓样本降为次选**（工作区恰在本仓时可就近读）；两者都无 → 读宿主文档或问用户。**诚实边界**：本机已装插件给的是**声明写法**，**不含源码写法** —— slot 那类源码级知识（`inject`/`register`）仍只能靠契约判据内嵌（上一批刚补），不能指望读样本得到。**⑤ 门禁**：`triage-gate` 增 3 条（本仓样本保留为次选 / hint 含 `$DSH_HOME`+`profiles`+`node_modules` / **不得写死绝对路径**）；L1 改 `PRD-ARTIFACT-CONTRACTS`（锚点换成"本机已安装的 dsh 插件"）并新增 `PRD-SAMPLE-SOURCE-LOCAL-FIRST`（含 `exclude`：旧文案不得复活），en 同步。**⑥ 变异实测**（用变量存原文还原，**不用 git checkout**——上一批刚踩过那个坑）：prompt 退回只指本仓 → L1 **4 红**；hint 写死绝对路径 → **3 红**；hint 去掉 `$DSH_HOME/profiles` → **2 红**；全部恢复 → 19 套件全绿、L2 23/23、typecheck 无错。
- 2026-09-21（第二十二批：契约补「UI 挂载点 `name` 必须是 slot 名」—— 从 probe-v2 残留 diff 挖出的真根因）：**触发**：用户问「probe-v2 已经完成了，我们还能继续使用这个吗，还是全部清理干净，从 0 跑看看效果」。回答前先核工作区状态，于是看到 `git status` 有 **3 个未提交文件**（`client/index.tsx` / `cordis.patch.yml` / `lib/client.js`）—— 那正是「走完流水线之后又沟通几轮才修复」的产物（用户提过，但会话原文不在 probe-v2 会话目录里，此前一直没拿到）。**① 读 diff 定性**：`cordis.patch.yml` 只是注释写得更准（顶层必须 YAML 数组 + 新增用 `- insert:` / `name` 用包根名）——**与我上一批 sharpen 的契约项完全重合**（说明契约补对了）；`client/index.tsx` 是**真知识**：`ctx.slots.register` 的 `name` 从 `'md-table-align.dock'`（插件自己的标识）改成 **`'conversation.input.dock'`（slot 名本身）**，另加 `id`/`order`。**② 先验证再写**（不拿未核实说法进契约）：查本仓 `client/index.tsx` —— **5 处 slot 注册无一例外 `name` == slot 名**，`cordis.patch.yml` 注释早已写明同类纪律 → **同仓即权威样本**，坑成立。**③ 为什么值钱**：这是**静默失败** —— 工具条永不出现，而功能单测 **31/31 全绿、AC 全过、验收 ✅**（与 dddd 同型：下游只验 AC 表，AC 里没有"UI 真的出现"）；我们契约表此前只说"client 半必须有 bundle 声明与 id/name"，**没说 `name` 该填什么**，等于把坑留给 dev 现踩。**④ 落地**：`ARTIFACT_CONTRACTS` 的 `plugin-client` 与 `plugin-full` 各加一条（判据给两段式写法 + 「`inject` 参数与 `register.name` 必须逐字相同」+ 端到端以「装进宿主后该 UI 真的出现」为准）；门禁 = `triage-gate` [11] 增 4 条 + L1 `PRD-SLOT-NAME-CONTRACT`。**⑤ 变异实测**：删掉 `plugin-full` 那条 → triage-gate **2 红**；恢复 → 19 套件全绿、L2 23/23。**⑥ 顺带回答用户的问题（结论）**：probe-v2 可继续用，但它已成"迭代需求"路径（`state.gitMode='repo'` + backlog req-1 已 accepted + 有 git 历史与任务夹），**测不出"从 0 建"**；要测新契约是否真少返工，应另开**全新空目录**（非 git → 弹存档问句；空仓 → needScaffold 路径），并把 probe-v2 原样留作"交付后仍需人工修"的历史样本。**⑦ 工具教训（记一笔）**：变异验证后用 `git checkout -- host/core/triage.ts` 恢复，把**尚未提交**的新契约一并回滚了 —— 变异本身有效（2 红），但恢复手段用错（该文件本批已有未提交改动时不得用 checkout 恢复）。已重新回填并复验全绿；后续同类操作一律先把原文存进变量、用 `[System.IO.File]::WriteAllText` 还原。
- 2026-09-18（第二十一批：**交付形态契约按宿主框架分键** —— 契约不是"开发插件的通用规则"，是 dsh 的私有规则）：**触发**：用户跑完 probe-v2、又问了几个开发 dsh 插件的坑之后，抛出一个更根本的问题 ——「那有个问题，我开发 openclaw 插件，或者 hermes 插件，或者其他 agent 插件，你如何识别不同的场景？这些在 dsh 的契约在其他的不一定有效吧」，随后补充「A，我只是想告诉你用户拿我们去干活，不只是干开发 dsh 插件，所以注入应该是更通用的，或者你明确识别到意图就是 dsh 插件开发，这块我们可以强化下这块的能力，因为我们插件本身就是寄生于 dsh 上面，至于其他的先不用管，但是也需要引导 Agent 先去了解再去开发，不然就是无尽的返工」。**① 先承认自己不知道**：我最初顺着话往下推设计，但被追问「你不知道这两个 agent?」时如实说明 —— 我不确定 openclaw / hermes 是什么；检索后确认 `NousResearch/hermes-agent` 是真实大型项目（Plugin Catalog、`ctx.llm` 见 PR #23194、工具覆盖 flag 见 PR #26759），`pi-agent` = `earendil-works/pi`（extension + hooks 体系），OpenClaw 有官方[构建插件文档](https://docs2.openclaw.ai/zh-CN/plugins/building-plugins)；**同时纠正自己的一处检索失误**（把用户给的 `NousResearch/hermes-agent` 与搜索里同名无关仓库并列）。**结论：这三个框架的插件机制与 dsh 毫无共同层** —— 一个是 `ctx.llm` + 工具注册、一个是 extension hooks、一个是 tools 注册，而 dsh 是 profile 层入口声明 + bundle patch + `dsh.client` 块 + `files` 白名单。**② 架构缺陷定性**：`ArtifactKind` 的 `plugin-host/plugin-client/plugin-full` 是 **dsh 私有词汇**，却被当成"开发插件"的通用形态 → 给别的宿主套 dsh 契约 = **反向返工**（改的是目标宿主根本不看的东西）。**③ 设计取舍（为什么不做多框架契约库）**：无界增长（每来一个框架加一套）+ 必然过期（对方 API 演进快）+ **我们对它们没有权威**（凭记忆写字段名正是 dddd 事故的成因，AGENTS.md 早已禁止硬编码宿主字段名）。故：**能通用的只有方法论（先去读目标宿主的文档、别套用别的框架），不能通用的是具体判据**。**④ 落地**：`ArtifactHost = 'dsh' | 'other' | 'unknown'` 作为与 `artifact` **正交**的新维度 —— `TriageVerdict.host` + `normalizeHost`/`ARTIFACT_HOSTS` + **确定性护栏 `forceHost`**（模型判 unknown 而需求含 `dsh`/`@deepseek-ai/dsh-*`/`deepseek-harness`/`cordis` → 抬为 dsh，防漏判丢契约；**不覆盖模型明确判的 other** ——「拿词表当身份」本项目已两次踩坑）+ **`contractsForDeliverable(host, kind, installable)` 作为唯一契约取用入口**（`plugin-*` × 非 dsh → 0 条本仓契约 + `hostResearch: true`）+ `TriageSettle` 增 `host`（第七档）与**宿主自洽门禁**（已判 host≠unknown 还问「装到哪个宿主」→ 丢弃，与 installable 那条同型） + `triageRecordOf` 搬 `host`（**第六次「白名单漏字段」**，门禁 [10] 的结构化断言已覆盖）+ `prdPrompt` 注入**「宿主契约调研」必填段**（要求写明：目标宿主 / 契约出处 / 实际要求 / 未核实项，**禁止凭记忆写字段名**）+ `util.extractHostResearchSection`（行式解析，与 `extractAssumptionsSection` 同源；**空标题判 null**）。**⑤ 硬门禁（用户定调「偏硬」）**：原话「不然你上下文都不知道你开发个啥出来都不知道」→ `notePrdAssumptions` 返回失败原因，pipeline 据此 `throw stageFailError('prd')`（走重试诊断）；只在 `journal.hostResearch === true` 时生效，故 dsh 场景零影响。**⑥ 顺手 sharpen dsh 契约内容**（把已踩过的坑写成具体判据，替代"参照同仓样本"四个字）：入口 `name` 必须包根名（子路径被静默跳过）/ client 声明块字段集合逐字段一致（少一个 loader 不扫）/ `files` 漏了就不加载 / `link:` 无 cwd 变量须给绝对路径 / **沙箱装不了真 profile 时必须写明人工手测步骤**（实锤：AC-7 被列为"人工补测"后无人真跑，用户装完才发现问题）。**⑦ 诚实边界**：注入能做的上限是"让 dev 一次照已知正确配方写"，不能让流水线替你**真装进 profile 跑 GUI**（沙箱无 `dsh` CLI + 改 profile 需授权）——这是环境限制，不是代码能补。**门禁有效性已实测**：契约分流 `if (false)` → triage-gate 6 红；`triageRecordOf` 漏搬 host → 3 红；护栏不生效 → 4 红；硬门禁接线断开（改回只 warn）→ 1 红；`prdPrompt` 不注入调研段 → L1 2 红；`extractHostResearchSection` 空标题判有效 → 1 红；全部恢复 → 19 套件 exit 0。L2 新增三份语料（`prd-host-research-openclaw.md` / `-missing.md` / `-empty.md`）。
- 2026-09-18（第二十批：引擎留痕 + 「序列化完整性」通用门禁 —— 门禁首跑又抓出四个只写不落盘的字段）：**触发**：用户说「加上吧」—— 指上一批回答里我自己留下的那句实话：「这次为了回答『是不是模型的锅』，我必须解压会话文件才拿到 `inception/mercury-2.5`，run 记录里查不到」。**① 引擎留痕（本次正题）**：`journal.engine = { provider, model }` 在 run 起跑由 `runner.resolveChildRoute(parent)` 解析后写盘（只在缺失时写，resume 保留首轮快照），并落一条 `log.engine`（`run.log` 第一屏就能看到）；**逐阶段**另记 `stage.provider/model` —— 因为子代理路由跟随主线程/团队配置、**可以被改道**（`runAgent` 里 `resolveChildRoute` 的显式路由优先于过期的 `parent.options`），只记 run 级会答错"哪个模型跑了这个失败的阶段"；完成汇报加一行「引擎（模型路由）：provider/model」，主线程不必再翻会话文件。落地位置：`store.ts`（两个接口字段 + 两处序列化）、`runner.runAgent`（阶段级）、`pipeline.executePipeline`（run 级 + 日志）、`report.ts`（汇报行）、`host/locales/pipeline.ts`（`log.engine`/`report.engine` zh+en）。**② 顺手立通用门禁（本次副产品，价值可能更大）**：同型「手工枚举清单漏一项」已五次（`journal.options` / `execOptions` / start 输出 schema / `loadState` / 本次 `triageRecordOf`），每次都是"补一处置一处"，**从没对持久化面本身立过门禁**。故在 `test/journal.test.js` [7] 新增：静态解析**持久化形态** `JournalRecord`（32 键）与 `JournalStage`（19 键）的接口字段，逐个断言 `serializeJournal` 真把它们写出来；豁免表只允许 `result`（进程内运行期字段）与阶段 `tokens`（历史遗留、仅读存量兼容），**豁免表本身也被断言**（防止以后有人把新字段悄悄塞进豁免表绕过门禁）。**③ 门禁第一次跑就抓出四个「只写不落盘」字段**：`branch`、`mergeStatus`、`guardReason`、`guardOutcome` —— 内存里写了、序列化丢了。**诚实定性（不夸大）**：这四者**当前没有可见故障** —— `branch`/`mergeStatus` 今天没有读者（`report.mergeHint` 与 `teamflow_merge` 都重新跑 `git rev-parse` 查分支），`guardReason/guardOutcome` 只在半程内被 runner 消费（据此定 `outcome`/`summary`），用户可见的原因已经写进 `stage.summary`；但"写进去就该读得出来"的意图被静默打破，而且**以后谁读它们谁踩坑**（`mergeStatus` 的语义明显是"记住合回决策"）→ 四者一并补进序列化（各一行，无副作用）。**④ 为什么这个门禁比再补一个字段值得**：它把"漏字段"从"靠人记得加断言"变成"新增接口字段默认就会红"——正是 `test/state.test.js`（`loadState`）与 `triage-gate` [10]（`triageRecordOf`）的同一手法，现在覆盖到了持久化层。**门禁有效性已验证**：不落盘 `engine`/`branch`/`guardReason` → `journal` 分别 **3 / 1 / 1 红**（含"漏了：X"的定位信息）；`pipeline` 不写 `journal.engine`、`runner` 不记 `stage.provider` → smoke 各 **1 红**；恢复 → 全绿。smoke 另加 4 条指针断言（阶段级/run 级/序列化/汇报键）。
- 2026-09-18（第十九批：**交付形态契约整条链从来没生效过** —— 第五次「白名单漏字段」，顺着上一批一个错误推断挖出来的）：**触发**：用户问「（上一批列的那两项）这两个是真实的问题还是什么」——逼着我去核，结果**一项是我错的、另一项顺藤摸出一个更严重的**。**① 撤回错误推断**：上一批我写「`ARTIFACT_REFERENCE_SAMPLES` 是空工作区里 14 次空转的大头」，那是**从 prompt 源码推出来的**；这次把 PRD 子代理会话全文解压（146701 字符）逐词核 —— `plugins/dsh-plugin-teamflow`、`sibling plugin samples`、`ARTIFACT_REFERENCE_SAMPLES`、`[交付形态契约`/`DELIVERABLE SHAPE` **各 0 次命中**，即样本路径**根本没进过 prompt**，那 14 次是空工作区里的环境定位（读不存在的 `AGENTS.md`→glob→列目录→重复读 `.gitignore`→找 `docs/teamflow/memory.md`）。已把 CHANGELOG/devlog 里那句话改成"经复核已否"，并留作反面样本：**推断不得写成结论**。**② 「命中 0 次」本身是线索**：为什么 `[交付形态契约` 一次都没进过 prompt？顺着注入链回溯 —— 注入源是 `journal.triage.artifact`/`.installable`（`pipeline.ts`：`const tj = journal.triage as { artifact?: string; installable?: boolean }`）→ `journal.triage` 的唯一写入点是 `triageRecordOf()` → **它只搬 9 个字段，漏了 `artifact` 与 `installable`** → `normalizeArtifact(undefined)` = `'other'` → `artifactContractsFor('other')` 恒为 `[]` → `items.length` 为 0 → **从未注入**。**③ 实测证据（不是推断）**：扫全部 run 的 journal —— **64/64 个 run 的 `triage` 都不带 `artifact` 字段**；`log.artifactContract`（locale 里那条"交付形态契约：判定形态「X」→ 已向 PRD 下发 N 条必填 AC"一直躺着）**一次都没落过**；`store.serializeJournal` 是整对象落盘（`triage: journal.triage || null`），所以**唯一丢失点就是这个函数**。**④ 后果定价**：2026-09-17 为 `dddd` 事故（插件"看着完整"却装不进 profile、功能 AC 全绿照样验收通过）建的三件套 —— `ARTIFACT_CONTRACTS` 数据表、`prdPrompt` 的必填 AC 注入、`qaPrompt` 的 0b 形态探针 —— **从建起来到现在一次都没跑过**，那道防线形同不存在。**为什么门禁没拦住**：`triage-gate` 的 13 条断言 + L1 的 4 条契约全在测**纯函数**（`artifactContractsFor`）与**手造 state 夹具**（`prdPrompt({__runCtx:{artifactContracts:[...]}})`），唯独没人测「verdict → journal → `__runCtx`」这条**接线** —— 同型问题第五次（前四次：`journal.options` / `execOptions` / start 输出 schema / `loadState`），前四次各补的都是"补这一步"，这次补的是"补这条链"。**⑤ 修法**：`triageRecordOf` 补两字段 + **把它从 pipeline.ts 挪进 `triage.ts`**（纯函数、门禁够得着：pipeline 链宿主私有 peer `@deepseek-ai/dsh-llm`，仓库内无 node_modules，测试取不到）+ **结构化接线门禁**（照 `test/state.test.js` 的做法：静态解析 `TriageVerdict` 接口顶层键（实测 14 个），逐个断言 `triageRecordOf` 真的搬运；只三项豁免且**豁免表本身也被断言**——`needDesign` 落 `journal.options`、`slug` 落 `journal.runDocs`、`rationale` 只回给主线程）+ 三条"注入源与可见判据"断言（注入源必须 `journal.triage` 而非透传的 `options.__triage`——后者不落盘，resume 就没了；有契约才注入；必须落 `log.artifactContract`）。**⑥ 门禁在我自己的代码上先抓到一次**：接口声明的字段是 `upgradedFrom`，但三处写入点（护栏强升）写的是**内部标记 `__upgradedFrom`**，我搬运时写成读 `.upgradedFrom`（**没人写它**）→ 等于读错一个下划线就让 `log.modeUpgraded` 静默消失（同型）；更值一提的是**门禁第一版没抓住**——SPECIAL 判据 `/__upgradedFrom/` 被同一行的**类型注解** `__upgradedFrom?:` 蒙过去，改成 `\.__upgradedFrom`（只看属性读取）+ 判据前**去注释**（否则注释里提到字段名就假绿）之后才真正生效。**门禁有效性已验证**：去掉两字段搬运 → `triage-gate` **6 红**（含"漏了：artifact, installable"）+ smoke 1 红；`.__upgradedFrom` 改 `.upgradedFrom` → **2 红**；恢复 → 全绿。**⑦ 副产品**：`journal` 里查不到 provider/model（这次为回答"是不是模型的锅"必须解压会话文件才拿到 `inception/mercury-2.5`），列为待办。
- 2026-09-18（第十八批：doc 阶段判据认文件 + 熔断预算随缓存能力自适应 —— 回答「真正失败的原因是啥，是不是和那个模型的问题」）：**用户提问**：probe-v2 那条 run 为什么失败、是不是模型的锅。**先分层取证，再回答**（不猜）：① journal + `run.log`：失败在 PRD，两行相邻日志 —— `warn 产出过短（284 字符），未通过实质校验` → `error 累计新增 token 259k … 超出预算 200k，熔断，需人工介入`；② PRD 子代理会话 `3b95189f` 逐条 usage：17 次调用、input 259k、cacheRead 29.7k（命中率 **10.4%**）、cacheWrite=0、output 仅 2k；逐次 input 稳定在 15.4–17.6k（**说明每轮都在重付固定开销，且没有缓存**）；③ 调用序列：14 次探索（`read AGENTS.md`（不存在）→ `glob AGENTS.md` → `glob **/*.md` → `Get-ChildItem -Force` → 读 `.gitignore` 后又 `Get-Content .gitignore` → `Test-Path`/`Get-ChildItem` 任务夹 → `glob docs/teamflow/memory.md`…）→ 1 次 `write PRD.md`（2154+ 字符）→ 1 次 `present` 声明交付物 → 最后**只回一个 state 块**；④ **两次 probe 对照**（本批的关键证据）：probe-cache 的 PRD 阶段（`workbuddy/hy3` 混 `deepseek-v4.1-flash`，run 整体命中 72.9%）**只用了 6 次调用**、回复 1566 字符、通过；probe-v2 的 PRD（`inception/mercury-2.5`，命中 10.4%）17 次调用、回复 284 字符、失败。**结论（三段，不是一句"模型的锅"）**：**触发**是模型行为（空转 14 次 + 极简回复）；**判定**只看回复不看文件（`PRD.md` 4894 字节已落盘、`present` 也调了）→ 把"摘要写少了"这种形式违规升级成"没交付"；**终结**是熔断预算门（259k > 200k 把本该发生的自动重试拦掉）。而熔断之所以触发，是因为 `FRESH_TOKEN_BUDGET = 200k` 的立论「cacheRead 廉价、新增只是零头」**对不缓存的 provider 不成立**：有缓存时实测 **1.2–2.4k fresh/次**（`tf-mu2ioilr` dev 115 次 280k；`tf-mu5wcm2j` qa 153 次 177k），而不缓存时是 **~15.5k/次**（system prompt + 工具定义，该会话 tools 段 38130 字符）→ 200k 实际等于「一个阶段最多约 13 次调用」。**历史时间线佐证**：09/15–09/17 的 run 缓存命中普遍 88–99%（57/65 个 run ≥50%），09/18 18:25 那次 72.9%、22:25 这次 10.4%。**修法一（判据认文件）**：`util.DOC_STAGE_FILES`（prd→PRD.md/TECH-CHANGE.md、design→DESIGN.md、tech→TECHNICAL.md/TECH-CHANGE.md、qa→QA-REPORT.md、acceptance→ACCEPTANCE.md）+ `stageDocText`（取最长候选）+ `runner` 在**回复不合格且 stop=completed 且回复非空**时回读文件，达该阶段长度下限即判交付并记 warn `diag.docDelivered`；**不豁免空回复**（pipeline 要用回复合并 state 块）；dev/qaFix 与 scaffold/architecture 不入表；patch 档 PRD 刻意不产文件 → 自然不生效。**修法二（预算自适应）**：`metering.cacheHitRatioOf` + `effectiveFreshBudget(base)`：命中率 <50% 且调用数 ≥5 → ×`UNCACHED_BUDGET_FACTOR`(3)（保持原设计「≈两轮尝试」的相对余量），熔断走独立措辞 `diag.breakerUncached`（带命中率/调用数/放宽后预算）；有缓存 provider 分毫不动；汇报口径仍是 `totalTokensOf`（两套口径不合并）。**顺带**：`artifactText` 从 pipeline 挪到 `util.ts` 共用（pipeline 原断言「pipeline：任务夹产物读取助手 artifactText」同步改为 util/pipeline 双指针）。**门禁**：`metering` +15（含「1 次调用不下结论」「恰 50% 不算无缓存」「空 usage 不抛」「不除零」）+ `verdict` +12（**真实形状复刻**：4894 字节 PRD.md + 284 字符回复 → 单看回复仍判未交付、文件兜底成立；缺失/过短/非 doc 阶段/空 journal 边界）+ smoke 6 条（含**反向断言**「兜底不得豁免非空回复」）。**门禁有效性已验证**：关掉 doc 兜底 → smoke 1 红；关掉自适应预算 → smoke 1 红；关掉无缓存判定 → metering 2 红；恢复全绿。**明确未做**（用户只选了这两项）：journal 记 provider/model（要回答"是不是模型的锅"竟需解压会话文件——这是诊断面缺口）、契约 prompt 补「样本不在本工作区别空转」——**此项已被下一批复核推翻**：解压 PRD 子代理会话全文后，`plugins/dsh-plugin-teamflow`/`sibling plugin samples`/`ARTIFACT_REFERENCE_SAMPLES` **各 0 次命中**，样本路径**根本没进过 prompt**；原判断是从 prompt 源码推出的推断、且是错的（见第十九批 ①）。
- 2026-09-18（第十七批：分诊不问「需求里已答过的问题」+ 不吞显式 `needDesign` —— probe-v2 两张分诊卡实锤）：**触发**：用户在新开的验证空间 probe-v2 发完需求，回来一句「还是两次需求分诊，一次是 lite，一次是 medium」。**先证伪自己的第一反应**（"缓存又没生效？"）——三向取证后结论相反：① 主人会话 `session-8455886b` 里 `teamflow_start` 被调 **3 次**（#1 无 supplement → needs-clarification；#2 带 supplement → needs-decision(git)；#3 同 requirement + 同 supplement + `branchPolicy=new/preAction=init` → 建 run）；② 该工作区只有 **2 个**分诊子代理会话（`090cc58a` → `lite`、`87bc9db7` → `medium`），**#3 没有第三个**；③ #3 的 tool/call 时间戳 `…530192` 与 `journal.startedAt` `…530205` 相差 **13 毫秒**——而模型分诊实测 **1.5–1.9 秒**（两个子代理的会话时长），且 `journal.triage.source="model"` 排除"退化成正则兜底"。→ **`pendingDecision` 缓存按设计命中**（上一批的修复在生产路径上生效）。多出来的那一次是**澄清后的新输入按设计重判**（缓存键 = 需求 + `requirementSupplement`，答复进来键就变）。**真根因在第一次分诊**：需求原文结尾写着「装进我的 dsh web profile 里真实可用」，而**同一次裁决**已经输出 `installable: true` + `artifact: plugin-client`，却**同时**抛出 blocker「Must this plugin be installable/publishable as a real DSH profile plugin…」——`TRIAGE_PROMPT` 里那句 `**Do not** ask it when the requirement already states the form` 就在同一份 prompt 里（把两个分诊子代理会话的 prompt 全文解压出来逐字核对过），照样被违反。这条不该问的 blocker 凭空造出一轮澄清（主线程拿它生成 3 个问题问用户）→ 输入变了 → 缓存必然不命中 → **同一条需求被分诊两次**（15.8k + 15.6k tok，用户看到两张「需求分诊」卡）。**修法（不解析问句文本）**：blocker 增机器可读字段 **`settles`**（`installable|artifact|scope|ui|data|other`，= 这条问出来是为了定哪个裁决字段），宿主 `qualifyBlockers(raw, ctx)` 增**自洽门禁**：`ctx.installable === true` 时丢弃 `settles='installable'` 的条目并计入 `blockersDropped`（沿用既有 warn 留痕，不静默）。**只按 `true` 判**：`false` 是"模型没给该字段"的默认值，既可能"不需要装"也可能"还不知道"，后者正是 must-ask 缺口的合法形态（丢了会重演 dddd「插件装不进 profile」）。**同批修第二个症状**（用户看到的"一次 lite 一次 medium"）：调用方显式传了 `needDesign=true`、分诊却判 `lite`——`lite` 的档位定义就是「no UI design」（`MODE_REGISTRY.lite.needDesignDefault=false`），两者语义冲突，而旧 `guardrailUpgrade` 只比较「调用方显式 `mode`」→ 调用方只给 `needDesign` 时它不生效；prompt 里那句 `needDesign=true → 强升 medium` 只是 regex **预筛提示**（模型实测无视）。现规则：**未给档位 + 显式 `needDesign=true` + 分诊判轻档位 → 宿主抬到 `medium`** 并沿用 `log.modeUpgraded` 留痕（`__upgradedFrom` 记分诊原档位；用 `MODE_RANK` 比较，避免刷出「medium → medium」噪音）；调用方显式给了 `mode`/`lite` 时仍以调用方为准（`lite` + `needDesign` 由 `resolveStages` 按 flag 追加设计阶段，不吞显式请求）。**门禁**：`triage-gate` **+18**（自洽门禁真值表 9 条：`true` 丢弃 / `false` 保留 / 不给 ctx 保留 / 非形态类不受影响 / `settles` 归一六档；needDesign 下限真值表 9 条含「显式 lite 不动档」「已是 medium 不放大」「full 不降档」）+ L1 `TRIAGE-INTENT-BLOCKERS` 更新为**五字段**（`settles` 进契约，改 prompt 必跑）+ smoke **5 条**（两条解析路径 `parseVerdictText`/`normalizeTriagePassthrough` 都必须把 `installable` 传给合格线、两条 start 路径都带 needDesign、`settles` 同时出现在 prompt 与类型里、**反向断言宿主不解析问句文本**）。**门禁有效性已验证**：把自洽门禁行与 needDesign 下限行各改成 `false &&` → **5 条红**；恢复 → 全绿。**顺带修掉一处 smoke 自伤**：`triageSrc` 原在文件后段块作用域里二次声明，新增断言提前引用会 TDZ 崩；改为文件头统一声明一次。**本轮验证到的（probe-v2）**：存档问句按预期弹出（N=0）、`state.json` 里 **`gitMode: "repo"` 存住了**（上一批修复生效）、基线提交按索引层排除、`feat/markdown-table-aligner` 分支建成、git 决策回程吃缓存（13ms）。**未做（用户选定只做这两件，留下批）**：doc 阶段判据改为"任务夹文件存在即算交付"（本轮 PRD 写了 4894 字节 `PRD.md` 但回复只有 284 字符 state 块 → `too-short`(<400) → `insubstantial`；历史 61 个成功 PRD 回复 min 569/p50 2159，且该阶段 17 次调用 fresh 259k > 200k 熔断，缓存命中率仅 10% vs 历史 84.5%）、分诊输出语言跟随 run（zh 会话下 `TRIAGE_PROMPT` 正文无输出语言指令 → blocker 问句与 rationale 全英文，本次实测发生）。
- 2026-09-18（第十六批：`state.json` 的 `gitMode` 从来存不住 —— 白名单漏字段第四次，立结构化门禁）：**触发**：收尾核对 probe-cache 时顺手读 `state.json`，发现**没有 `gitMode`**——但那条 run 的日志明写「「改动存档」已开启（git init 完成；现有内容已记录为初始状态（基线提交））」。**根因**：`loadState` 是**逐字段白名单重建**（`const base = emptyState()` 之后逐个赋值，**不是**整体读取），而它**漏了 `gitMode`**。链路：pipeline 写 `st.gitMode='repo'; saveState(...)` → 文件里确实写进去了 → 之后**任一阶段产出 state 块**触发 `mergeStateBlock`（`loadState` → 改 → `saveState` 往返）→ `loadState` 重建时把 `gitMode` 丢掉 → 落盘回来就没了。**后果**：`gitMode` 永远存不住 → **每次 run 又从头问一遍「要不要开启改动存档」**，而"答案记住、后续 run 不再问"正是 2026-09-17 方案 A 的核心承诺 → 整条失效。**同型第四次**（B1：`journal.options` 丢 `requirementSupplement`/`__triage`；`execOptions` 丢分支策略四字段；`host/index.ts` 输出 schema 漏声明）——每次都是"手工枚举的清单漏了一项"，每次补完就完事，于是有下一次。**修法**：`loadState` 显式搬运 `gitMode`，且**只接受 `'repo'`/`'none'`**（脏值过滤，不把垃圾固化）。**门禁（本批重点，不再只补一个字段）**：新增 **`test/state.test.js`**，四层——① **静态解析 `TeamflowState` 接口源码的顶层键**，逐个断言 `loadState` 函数体里出现 `raw.<key>`（豁免运行时字段 `__runCtx` 与常量 `version`）→ **将来新增持久化字段忘了搬运立刻红**，不依赖"有人记得加断言"（这才是防同型第五次的关键）；② 行为级 save→load 往返，每个字段逐一比对仍在；③ `gitMode` 三态语义（`repo`/`none`/缺省=未知不编造）+ 非法值被过滤；④ **复刻实锤丢失路径**：写入 `gitMode` 后跑一次 `mergeStateBlock`，断言 `gitMode` 仍在**且合并本身照常生效**（不是靠"跳过合并"来保住）。**门禁有效性已实测**：临时移除修复行 → **7 条红**（含"漏了：gitMode"那条结构化断言）→ 恢复 → 全绿。+ smoke 2 条指针断言（loadState 显式搬运 + state.test.js 存在）。同批确认 probe-cache 工作区已干净（主 agent 交付已提交、`git status` 0 改动），下一轮验证改在**新空间**做（避免新 run 的收口提交把主 agent 的改动一起卷进去）。
- 2026-09-18（第十五批：dev 任务身份稳定化 —— 合并执行后 resume 不再重复补跑）：**触发**：用户观察 probe-cache 那条 run「**开发 T0 会被触发两次**，第一次明面成功了，而且第二次还消耗了 2m token」，且刚经历限流后 resume。**取证（三向印证，不猜）**：① journal `tf-mu6tb281-4n43oc` 里 stage[3] `taskKey="T0 工程约束/分支/.gitignore/关闭提交 + T6 契约收口+构建+test:gui + T7 装入 profile 验证 AC-14"`（**拼接串**）且 `status=done`/`outcome=completed`/`verifyEvidence` 齐全，stage[9] 却是单独的 `T0 工程约束/分支/.gitignore/关闭提交`；② `journal.blueprint.tasks` 是 **8 个分开的任务**（T0…T7）；③ backlog 里 `dev-1` 与 `dev-7` **同为 T0**、`dev-8` 同为 T6、`dev-9` 是 T7 补跑失败——肉眼可见的重复卡。**根因（host 自己的判定 bug，不是模型发挥）**：冲突检测把 `files` 有交集的 blueprint 任务**合并**成一个子代理执行（本意正确：并发不写同一文件），合并时把 `title` **拼接**（`pipeline.ts` 里 host 自己拼的），但当时 `taskKey` **只存 title**、`devTaskStatuses` 按 **title 全文精确匹配**归并；resume 时 `buildDevTaskDefs` 重新从蓝图取回**未合并**的 title → 三个都查不到 → 判「未完成」→ **重复执行已成功的工作**（日志「复用 5 个已完成任务，补跑 4 个失败任务」= T5 + T0 + T6 + T7，多补了 3 个）。子卡匹配键同样是 title → 重复卡。**修法（用户拍板：身份必须结构化，不用产物存在性、更不用字符串规则）**：`buildDevTaskDefs` 按定义顺序生成稳定 **`dt-N`**（`util.devTaskIdAt`）作任务身份，**合并时 `ids` 数组累加**（`['dt-1','dt-7','dt-8']`）→ `stage.taskIds` 落盘 → `devTaskStatuses` **逐 id 归并**、resume 判定 `taskStatuses.get(d.id)`；子卡匹配键改 **`dtId`**。**明确否掉的方案**：②「看产物文件是否存在」——用户指出不合理（判定应看记录的 taskId 状态）；「按分隔符切分 title」——拿文本长相当身份，与 per-plugin 正则、固定 .gitignore 词表**同型**，已犯两次。`devTaskStatuses` 移入 `util.ts`（纯函数；pipeline 链宿主私有 peer `@deepseek-ai/dsh-llm`，行为测试取不到——同 `cancelRun` 住 context 的处置）。**一处此前的误判已撤回**：曾推断「失败阶段 token 未记账」是 bug——实测那两次 T0 补跑会话 usage 全为 0（`429 soft_rate` 秒退、未产生 token，`assistant/attempt` 六次全是 0 usage），stage 显示 0 是对的；真正的大额消耗在 **T5 首次开发**（5122k/67 calls，触发熔断）——**不在无证据时改代码**。
  **⚠️ 同批修正首版引入的回归（用户实测「刚刚去点了一下 resume，0,1,2 开始重试了」）**：首版方案是"存量 stage 无 `taskIds` → **在判定函数里**回退 `taskKey`/`label`"。**这是给脏数据打补丁，而且回退是 per-stage 的**——历史 stage 回退还用 title、新任务定义却用 id，**id/title 两套命名空间永远对不上**。实测同一份 journal：**补跑 8 个**（T0/T1/T2 明明 done 却全重跑），比修之前的 title 判定（2 个）**差 4 倍**。用户一句「**你这是为了兼容脏数据？**」问到点上——我随后提的"双键匹配 + 按 ` + ` 切分 title"两条**都违背自己刚写下的禁令**（拿文本长相当身份），当场否掉。**正确做法：给存量数据补 id，判定只认 id**。新增 **`util.backfillDevTaskIds(stages, defs)`**：用**蓝图自己的 title** 去匹配 stage 的 `taskKey`（方向**结构化 → 文本**，不是反解字符串），命中的即该 stage 承载的任务；合并 title 会补出**多个** id（与新逻辑「合并时数组累加」同构）；结果**写回 journal**（幂等：已有 `taskIds` 的 skip），日志 `log.devIdsBackfilled`。`devTaskStatuses` 收敛为**只认 id**（函数体内不出现 `taskKey`），判定因此**始终只有一个键空间**。调用点两处（resume 开发分支、`interruptedPhaseOf`）在判定前补算。**实测同一份真实 journal（21 个 dev stage）**：补算前 8 个待补跑 → **补算后仅 1 个（`dt-6`）**，比 title 判定的 2 个**更准**——T7 确实被那个 done 的合并 stage 承载过（`seq=4` 的 `taskKey` 含 T7 title 且 `status=done`），旧 title 精确匹配看不到拼接串里的它，会把它再跑一遍。门禁升级为 **25 条**（新增：补算前 8 个全待补跑=复现灾难、合并 title 补出多 id、幂等、空 stage/空 defs/无匹配不臆造；`devTaskStatuses` 的两个反向断言）+ smoke 11 条（新增反向断言：**判定函数体内不得出现 `taskKey`**、**不得按分隔符切分 title**）。**教训**：向后兼容不是"让新逻辑迁就旧数据"，而是"把旧数据升级到新模型"；per-item 回退必然产生两套键空间。
- 2026-09-18（第十四批：基线排除不再污染用户 `.gitignore` —— 方案 B 根治）：**触发**：用户在 IDE 里打开 probe-clock 的 `.gitignore` 截图发问「pnpm-store 怎么是 teamflow 产生得了」。第 7–9 行确实是 `# TeamFlow 运行日志（插件自有产物，非交付物；host 提交时另有 pathspec 强制排除）` + `.pnpm-store/`。**表象 vs 真问题**：表象是注释张冠李戴——`mergeGitignore(before, entries, locale)` 的注释参数是**整批一条**，而 `ensureCommonNoiseIgnores()` 传 `'zh'`（落到 `log.gitignore` 那条 TeamFlow 文案），于是 `.pnpm-store/`+`node_modules/` 与 `logs/teamflow/` 共用同一句说明，还让同一句注释在文件里出现两次。**真问题是越界**：`.gitignore` 是**用户的项目资产**，"该忽略什么"归 L2（PRD 阶段 PM 按技术栈规划，`prdPrompt` 必查项）/L3（QA 收口探针）/用户本人；host 只该在**自己那一次 `git add`** 上收敛范围。**上一批我的"设计修正"前提就错了**——我以为"把 L1 精简到 `.pnpm-store/`+`node_modules/`"等于"不越界"，但越界与否取决于**写到哪去**，不取决于写多少；那条 smoke 断言（`smoke.js:609`，注释还写着"设计修正，勿回退"）实质是给 bug 发了合法化文书。**修法（方案 B，用户拍板）**：**删除 `ensureCommonNoiseIgnores()`**；新增 `sanity.BASELINE_NOISE_EXCLUDES = ['.pnpm-store','node_modules']` + `tfAddArgs(excludes?, cwd?)`，基线提交改走 **magic pathspec 索引层排除**（`git add -A -- . ':(exclude).pnpm-store' …`）——只影响 host 自己那次 add，**用户 `.gitignore` 逐字节不动**（真 git 集成测试逐字节断言）；另加 `log.baselineExcludes` info（用户能看到"排除了什么、且没动你的 .gitignore"）。**安全性立论（temp 仓库实测，方案成立的全部依据）**：`:(exclude)` 报错退出 1 的**唯一前提**是该路径**已被 `.gitignore` 忽略**（「显式点名 + 被忽略」——实测 `add -A -- . ':(exclude).pnpm-store'` 在无 .gitignore 时 `exit 0 / A real.txt / ?? .pnpm-store/`，有 `.gitignore: .pnpm-store/` 时 `exit 1`）；本清单项**从不写进 `.gitignore`**，故恒定 exit 0——这也正是它与 2026-09-11→09-15「4 天没有任何 run 提交过」那个坑的分界。**B 的前提我一开始也判断错了**：曾认为"`.pnpm-store/` 挪给 L2"可行，但 L2 在 **PRD 阶段**才产出，而基线提交发生在 **backlog 建立后、PRD 之前**（`pipeline.ts:437-449`）→ L2 救不了这次超时（probe-clock 实测 549 文件/51 MB 被 8s 默认超时杀掉）。方案 B 的真正形态不是"挪给 L2"，而是"**不在文件层留痕，改在索引层收敛**"。**自检必须针对目标仓库（初版写错，测试当场抓出）**：初版自检用 `existsSync('.gitignore')` 相对路径 → 读的是**宿主进程 cwd**，而排除判定应针对 `gitRun` 的 `cwd`（目标仓库）→ **两个方向同时错**：① 插件仓 `.gitignore` 有 `node_modules/` → 误拦 → 目标仓库没忽略它 → **噪音照样进索引**（`commit-path`「node_modules 未进索引」红）；② 插件仓没有 `.pnpm-store/` → 误放 → 目标仓库忽略了它 → **点名 + 被忽略 → exit 1**（`dup` 用例红）——自检形同虚设，且第二个方向恰好把 4 天那个坑原样搬回来。改用 **`git check-ignore -q`**（以目标仓库为根，git 自己的规则引擎：通配/取反/嵌套 `.gitignore`/全局 excludesfile 全覆盖）；**无法判定时一律不下发**（宁可少排、add 慢一点，也绝不让 add exit 1——代价不对称）。**门禁**：`commit-path.test.js` +11（真 git：未忽略路径 + `:(exclude)` → exit 0 且两项噪音不进索引、真交付仍在面内、**用户 `.gitignore` 逐字节未变**、基线提交跑通、基线树无噪音目录、**已忽略项被自检拦下且仍 exit 0**）；`gitignore.test.js` +13（清单形状 2 项且不含 `.idea`／空清单退回 4 参数／省略参数向后兼容／真仓内逐项独立判定）；smoke 8 条**反向断言**（pipeline 不得再出现写用户 `.gitignore` 的噪音排除、`ensureCommonNoiseIgnores` 不得回到代码、忽略判定必须走 `check-ignore`、**不得用相对路径读 `.gitignore`**、基线清单不得含自有日志、写文件点收敛）——两条旧断言随实现更新（`tfAddArgs` 前缀不变；「零回退——不再用负 pathspec」精确化为**只禁点名自有日志**，因为 `:(exclude)` 语法本身被正当用于基线排除）。**顺带**：清掉 probe-clock 的 profile 安装残留（`dsh plugin remove` 后 pnpm 留着 junction，手动删；源目录未受影响）——那是"场外改动"，留着会让下次 QA 的安装探针看到"已装"。
- 2026-09-16（第十二批：需求澄清闸门 Phase 1 —— 探索态不建 run + 假设可见化）：来自社区讨论 deepseek-ai/deepseek-harness#6405 的两条实测反馈（tongwoojun：「我想开发一个 dsh 插件」→ 直接跑完，用户「都不知道自己想要啥」；zweix123：「意图澄清要与人对话，子代理形态怎么做？」）。**先定价再动手**：只读回溯近 12 天 23 个 run（journal + 任务夹产物 + 会话记录纠错话术，已过滤 `system-reminder`/汇报注入/checkpoint 文本）——PRD 可查 12 份 **0 份**记录假设（另一次 39 份 PRD 全量扫描同为 0）；`humanIntervention` 8/23；归为「需求理解/范围偏差」的明确案例 2–3 条（`tf-mtmv0x8e-n8tqwr` 排行榜位置 + model 关不掉 + 部署/安全追问；`tf-mtvpho6h-zfu75e` 冒烟产物/项目选择）；社区实证 1 例属**全身浪费**。**位置论证（关键）**：按阶段聚合实测调用占比——`tf-mu2ioilr`（full 718 calls）prd 12 / tech 29 / dev 458 / qa 219；`tf-mu2ye559`（lite 168 calls）prd 10 / tech 6 / dev 141 / qa 11 → **PRD 后 2–6% vs dev+QA 89–95%**，故确认单必须放 PRD 之后（放启动前问不出重点、放 tech 之后是事后追认）。**本次实现（Phase 1，不动启动后的流水线语义）**：① `triage.ts` 增 `intent` + `blockers[]`，host 侧 `qualifyBlockers` 合格线（三项证据 + 上限 3 条 + 丢弃计数）；② `teamflow_start` / Remote `start` 增 `clarificationPreflight`（复用同一次分诊，不额外调用）→ `needs-clarification` 不建 run，答复走新参数 `requirementSupplement`（`[CLARIFIED]` 进 PRD）；③ 显式 mode/lite 不走闸门、分诊失效放行（`fallbackVerdict` intent=requirement）→ **零回归**；④ `prdPrompt` 必填「假设与待澄清」段 + state 块 `openQuestions`（tech/patch 各一句）；⑤ PRD 收口 `notePrdAssumptions` → `journal.assumptions`（缺段 warn）→ 汇报 `report.assumptions`；⑥ `journal.triage` / `assumptions` / `requirementSupplement` 落盘（shadow 埋点）。门禁：`test/triage-gate.test.js` 31 断言 + smoke 16 条 + L1 四条契约；16 套件全绿。**Phase 2 待定**（PRD 确认单：`resume(redoFrom='prd')` + 审批面 user-questions seam 可行性 + 按 shadow 数据定闸门强度）见 docs/TODO.md。**同日实测补充（注入闭环）**：把闸门落地当天的真实会话拉出来核对——`session-518e9188`（工作区 `E:\Code\OpenSource\DSH-Plugin\plugins\test`，产品线 `ws-test-a5c6a95f`）里团队注入**已下发**，用户说「我想开发一个 dsh 插件」，但 **`teamflow_start` 调用 0 次、该产品线 runs=0**：第一道防线（模型自觉：Glob 探空目录 → 读插件文档 → 调 brainstorming 给 4 个方向候选）生效，**第二道闸门根本没执行**。结论：两层互补（社区实证第一道失效 → 闸门兜住），但旧注入只写「不明确就别调用」、**没写「澄清完必须回来开工」** → 澄清链没有闭环保证。故 `tool.ctx.team`（zh/en 同形）补三段：澄清前置（2–4 方向候选 + 只问 must-know）/ 澄清后务必调 `teamflow_start`（原话进 `requirement`、结论进 `requirementSupplement`）/ `needs-clarification` 处理指引（按 blockers 问 → 带 supplement 重调，禁止替用户假设）；smoke 三条守门。
**同日实测又抓到两处自伤并修掉（首条真实 run 的价值）**：`tf-mu34afd2-wcjaw1`（用户在 `plugins/test` 说「我想开发一个 dsh 插件」，模型两轮 `ask_user_question` 后带 **1144 字符 `requirementSupplement`** 启动 lite 流水线）暴露：① `startPipeline` 传给 `executePipeline` 的是**白名单化的 `journal.options`**（审计面），`requirementSupplement`/`__triage` 被静默丢弃 → 澄清结论进不了 PRD、`journal.triage` 永远为空（shadow 埋点空转）→ 修 `execOptions` 显式带上两个内部字段；② PRD 里确有 `## 9. 假设与待澄清`（A1–A3 默认值 + 若改变的后果，写得很好），但提取正则匹配不到**编号标题**、且 `\s*$` 分支会摘出空串 → `journal.assumptions` 为 null、汇报不高亮 → 新增 `util.extractAssumptionsSection` 行式解析。门禁：`triage-gate` +10（编号/附录/en/空正文/文末/空输入）、smoke +4。**同日第三轮（档位自选治理 A+B）**：维护者追问「为啥每次都被主线程显式 `lite:true`」——取证发现**根因是我们自己的参数描述**：`lite` 说明写着 "(recommended) for small changes"，模型推理里逐字引用（`testb`/`test` 两处原文可查）。统计 22 会话 33 次 `teamflow_start`：`lite` 6 次、`mode` 8 次（**0 次**先预览 `teamflow_triage`）→ **42% 启动跳过 triage**，闸门与 ADR-0006 架构护栏同时失效（上一轮"没有真实损失"的判断被这条数据推翻）。修：**A** 删 "(recommended)"，三条描述统一「默认不要自选档位，交给自动分诊」，仅用户明确要求轻量时才传 `lite`；**B** 预检只豁免 `patch`，其余一律跑分诊 → 闸门判定 + `guardrailUpgrade` 强升（轻档位 + 分诊 ≥medium → 升档并落日志；已选 medium/full 保持）。门禁：`triage-gate` +12 护栏真值表、smoke +6（含「描述里不得再有 (recommended)」）。**顺带验证**：testb 那条 run（04:39:54 起，宿主 04:36:36 重启）实证了 B1/B2 两处修复生效——`requirementSupplement` 652 字符落进 journal、PRD 假设段 1285 字符被记录并打日志「已记录（12 行）」。**同日第十一轮（改动存档两态，方案 A）**：维护者连问两个对的问题——「为啥入口可以忽略而出口必须要 git？」（答：出口其实不强制——`log.commitFail` 是"尝试+失败+忽略"；真相是**入口静默跳过、出口当失败记**，两边都在猜同一件从没被问过的事）与「非程序员能知道这个东西不？」（答：三处可见性全缺，而版本控制对非程序员恰是**唯一的安全网**）。落地（用户拍板：选项两个、答案记住、基线提交要、**危险路径必须挡**——"用户直接在 C 盘打开文件夹咋办"）：`gitMode: "repo"|"none"` 记工作区 state → 入口人话问句（init/keep，危险路径只给 keep+建议换目录）→ 出口按态分支（none 不尝试提交、汇报带 📦 已存档/未存档行）→ `isDangerousVcsRoot`（盘根/家目录祖先/系统目录/深度≤1）+ `dirTooLargeForBaseline`（有界采样 >1000 文件/>100MB）两层防线 + `preAction="init"|"keep-nogit"` + 执行期二次校验（命中 L1 → 降级不初始化并继续，run 不打断）。门禁：verdict +12、smoke +8（提交点计数改 >=2：init 基线成为第三个提交点）。
**同日第十轮（② 形态澄清项 + 📝 行首锚定）**：① 落 ② —— `TRIAGE_PROMPT` 把「新交付物但需求没写安装/交付形态」定为必问 must-know（形态不同 → 契约集/AC 不同；同时明确仓库惯例已能确定时不问），L1 `TRIAGE-INSTALL-BLOCKER` 守门。② 修一个**同类假判**：r2 修复 run（`tf-mu4i779p-kze5kl`）验收报告 L163 `验收结论：⚠️ 有条件通过`、L120 标题「为什么不判「📝 需求不适用」」、L122「dev 结果并非「无需改动」」——报告在**论证该词不成立**，而解析器对 `📝 需求不适用` 走全文匹配 → 判 `reject` → run failed + 需人工（`humanIntervention` 还漏置）。修：📝 改**行首锚定**（剥 markdown/表格前缀与结论标签后须以 📝 开头；句中命中落 needs-human）+ reject 分支补 `journal.humanIntervention = true`；真实报告复验：`reject` → `accepted`。门禁 verdict +5 / smoke +3。
**同日第九轮（交付形态契约）**：维护者反馈 dddd 的插件「缺胳膊少腿、不满足 dsh 插件条件」——查证：那条 run（`tf-mu4bve7t-duux2k`）**验收 ✅ 通过**但产物**装不进 web profile**：缺 `cordis.patch.yml`（profile 层入口）、缺 `package.json` 的 `dsh.bundle.patch`、`files` 白名单漏该文件、依赖用 `workspace:` 协议。根因是**AC 里从没有"宿主可加载/可安装"**（PRD 把「做一个 dsh 插件」展开成功能 AC；QA/验收只验 AC 表）→ 形态隐含契约无人负责。修法**刻意不走"每类插件加一条正则"**（无界增长 + 词表交叉）：改为**一个形态维度**（`triage.artifact` + `installable`）+ **host 契约数据表**（`ARTIFACT_CONTRACTS`，新增形态=加一行）+ PRD 强制落成可测 AC（字段名要求读同仓样本，禁止硬编码）+ `other` 不注入。门禁：`triage-gate` +13、L1 +4。（修复该插件的 run 已在跑：`tf-mu4i779p-kze5kl`，tech 档，任务夹 `20260917-r2-profile-install-manifest`，技术方案逐条对标 4 个缺口且有 V1–V7 判据。）
**同日第八轮（外部供应商故障：不再当交付缺陷 + 长退避）**：维护者追问「429 或没额度这类，之前花 20 分钟又得重跑一次完整的？」——查证：dev 已是**任务级增量补跑**（dddd 实测只补 E/I/J 三个任务，7 个已完成任务复用），所以"重跑 20 分钟"不成立；真正的浪费在**非 dev 阶段**（整段重跑）与**旧处置把外部故障当内容失败**（快速重试两次 → failed+human → 看起来像交付问题；而实测同请求 16 分钟后即成功）。落地三件套：分类纯函数（中英 + HTTP 码 + 超时/连接重置；content 类只有上下文超限/护栏/产出过短；未命中按内容类，宁严勿松）→ 外部类走 30s/60s/120s/240s 退避（不计熔断、不受 RETRY_LIMIT 约束、可被取消打断）→ 用尽落 `interrupted/external` + `journal.externalFailure` + 汇报明写"非交付缺陷、resume 只补这一段"。**不做阶段内续写**（动产物"写一次不可变"契约与全部完成判定，收益低）。门禁：verdict +21、smoke +7。
**同日第十三轮（probe-clock 实测三连修）**：主载荷测试第一次跑通大半（AUQ 澄清 ✓ → 用户选"开启存档" ✓ → run 启动 ✓ → PRD/scaffold/tech/dev/QA/两轮打回复验闭环 ✓，mode=full），但抓到三个问题：① **start 输出 schema 漏声明**——git-init 决策返回的 `kind` 未声明 → 宿主 `additionalProperties:false` 拒收，两次 start 失败、零 run；静态抽取顺带发现 paused/no-team 的 `message` 同样漏（这两个分支此前从未通过校验）。修：schema 补 kind/message + render 兜底（message 直出）+ git-init 决策专用渲染键。② **AUQ 化**：五处闸门/决策注入统一"建议用 ask_user_question 呈现（点选项 + 其他兜底）"，澄清闸门明确映射（题干=question、选项=readings）。③ **execOptions 白名单第三次同型丢失**：用户选了"开启存档"（preAction='init' 正确回传、run 启动），但 branchPolicy/branchName/preAction/commitMessage 四字段不在 startPipeline 的显式携带清单 → executePipeline 收到 undefined → **git init 静默没执行**（probe-clock 目录无 .git，journal 只有 sanity 的 warn）。修：execOptions 补齐四字段；**两条新门禁**：smoke「输出 schema 严格性」（抽取 execute 各 return 顶层键 ⊆ schema 声明）+「execOptions 逐字段携带」。合并教训一句话：**工具输出形状与 options 传递面都是白名单制，加字段必须两处同步，且都要有形状级门禁**。
**同日第七轮（回归修复：续跑不重跑分诊）**：`dddd` 续跑日志多出 `自动分诊 … source=fallback` —— 上一批「除 `patch` 外一律跑分诊」的条件没排除 `resume`，而档位早在首次启动就定稿在 `journal.options.mode`。修：新增 `} else if (resume) {` 分支，不跑分诊、只补 `source: 'resume'` 的 shadow 记录（仅当 `journal.triage` 缺失时，首轮真实裁决保留）+ `log.triageResumed` 日志；理由：续跑重跑分诊既白花一次模型调用、又可能让档位在续跑路径上漂移（护栏强升不得二次触发）。门禁 smoke +2。
**同日第六轮（dddd 续跑完成 → 验收结论行被章节标题蒙住）**：续跑（01:53 起）补跑 E/I/J 三个任务成功，dev 10/10、QA 查出 3 个阻断缺陷并打回修复（第 1 轮复验通过）、验收报告结论为 `验收结论：✅ 通过`——**质量链路全绿**；但 host 判 `ACCEPTANCE.md 缺少验收结论行` → run 落 `failed` + human。根因：`parseAcceptanceVerdict` 用 `.find()` 取**第一个**含「验收结论」的行，而第 11 行是章节标题 `## 1. 验收结论摘要` → 取到标题 → 四档词落空 → needs-human（**通过却判人工**）。修：只认字面量模板行（允许 `## `/列表符前缀），多命中取最后一个；不认加粗破坏冒号连写的写法。回归：`verdict.test.js` 增 8 条（真实 98 行结构复刻：摘要章在前→accepted、同结构 ❌→rework、只有标题→needs-human、en 同构）+ smoke 4 条；**用真实产物复验：同一份 ACCEPTANCE.md 判定 needs-human → accepted**。
**同日第五轮（dddd 实测 → 闸门必须收敛）**：新工作空间 `dddd` 里模型连投 6 次 `teamflow_start` 全被 `needs-clarification` 退回、**零 run**；它自己复盘：「第 5、6 轮已明确回答『真实功能插件』，又被问『带 UI 功能插件 vs 脚手架』」，blocker 3→2→2 绕同一个问题。根因：① 分诊输入**只有原话**，`requirementSupplement`（1012 字符的澄清答复）**没进分诊** → 答复再多也听不见；② 缺收敛规则。修：① 分诊输入 = 原话 + `[CLARIFIED] supplement`（写明 do NOT re-ask），tool 与 pipeline 同口径（与 PRD 阶段的 `[CLARIFIED]` 同源）；② **收敛规则**：没给过 supplement 才拦，给过就不再拦，残余 blocker 作假设开工并记 warn（假设进 PRD「假设与待澄清」段 + 汇报高亮）。**语义定了**：闸门只负责"唤起第一轮对齐"，不负责"追问到满意"。门禁 smoke +3。（注：该会话的工具结果被标为 Skip history 未落盘，证据取自主线程的复盘原文。）
**同日第四轮（testc 实测 → 权威判定下移）**：新工作空间 `testc` 里模型**不再自选档位**（A 生效 ✓），闸门也真的执行了，但 `journal.triage.source=fallback` 且 `teamflow_start` **0.4s 返回** → 判定为「工具内分诊当场抛错被吞」：对照实验（同在工具上下文调 `teamflow_triage` 得 `source=model`）指向唯一差异 = 预检**漏传 `exec.signal`**。同时抓到日志 bug：调用方没选档位却被记成「自选档位「full」→ 已升档」。修：① 预检传 signal + 失败写 `__triageError`（不静默）；② 升档日志仅在调用方真选过档位时记；③ **权威判定移到 pipeline**——除 `patch` 外一律跑分诊（含显式 `lite`/`mode`），在那里做护栏强升 + 闸门兜底 `abortForClarification`（`status=interrupted`、不建阶段、汇报 `run.needsClarification`）；工具侧预检降级为快路径。门禁：smoke +5（含「预检必须传 signal」回归锁，防这次这个坑）。
- 2026-09-16（第十一批：流水线图 dev 卡片标题丢失 —— 0.1.9 起的投影回归）：维护者发来前后两张流水线截图问「现在开发不会像之前显示里面的 title 了是吗，只显示开发两个字」。**先定位再归因**：① 用截图里的卡片时长（2m18s/3m3s/2m48s）在 106 份 journal 里唯一定位到 `tf-mtr9mi37-m9zx1u`（三个 dev 阶段 139/184/168s）；② 读该 journal：`taskKey`/`label` **标题完好**（「game.js ghostY rot 归一加固（单点防御）」等）→ 数据没丢，问题在展示；③ 读渲染链：客户端 `stageLabelOf(s)` = `s.taskKey ? label : phaseNameOf(s.phase)`，而 **host snapshot 投影 `stages: j.stages.map(...)` 从不带 `taskKey`** → 该分支永不成立 → 所有 dev 卡片显示阶段名「开发」；④ 查历史：P1 客户端 i18n 改造（0.1.9）之前节点标题**直接渲染 `s.label`**，改造后改走 `stageLabelOf` 而投影未同步 → **0.1.9 起的回归**（0.1.8 用户仍能看到标题）。**副作用面**：同一根因还吃掉 QA 缺陷修复轮的「（第 N 轮）」（那类阶段 `taskKey=null`）。**修法**：投影补 `taskKey: s.taskKey || null` + `stageLabelOf` 增 dev 兜底（`phaseKeyOf(s.phase)==='dev' && raw → raw`）；门禁 smoke 两条。**历史 run 零迁移**：标题一直在 journal 里，部署+重启后老 run 也恢复显示。
- 2026-09-16（第十批：README 截图分语 + 全局面板小节 + en 两处滞后补齐）：维护者指出「README 有几个优化点：之前没有全局看板和英文翻译，英文应该换成英文的图，全局看板的 UI 是不是可以补上去」。核查结论（grep + 文件清单）：① `README.md` 与 `README.en.md` 的 5 张图**引用完全相同的 `docs/screenshots/*.png`** → 英文 README 展示中文界面；② **全局面板（0.1.8 起存在）零截图、零小节**（`## 界面预览` 只有 5 项会话内工作台内容）；③ 现有 5 张拍于 0.1.8 之前 → 缺「⏹ 中断」按钮、阶段状态还是旧词「已关闭」；④ en README 两处**滞后于 zh**（安装清单缺「左侧边栏图标（全局面板）」、快速上手缺「跨会话全局视角」）；⑤ 两语 README 的开发命令写 `npm test`/`npm run …`，而仓库是 **pnpm**（`pnpm-lock.yaml` + CI + CONTRIBUTING 全用 pnpm）→ 混用会诱导贡献者生成 `package-lock.json`。**落地（维护者选定「先改结构、图后补」）**：en 6 张改指 `docs/screenshots/en/…`、两语各加第 6 项「全局面板」、补齐 en 两处滞后内容、`npm→pnpm`；`CONTRIBUTING.md` 新增截图约定（**禁止 zh/en 共用同一张图**、英文界面重拍、单张 ≤400 KB、两语同视口）。**12 张图（zh 6 + en 6）已由维护者重拍并替换**（3200×1662 PNG，235–413 KB；`en/*` 为英文界面实拍）——图片列表顺序按维护者要求改为「全局面板 → 流水线视图 → 阶段详情 → Backlog 看板 → 看板任务详情 → 团队选择」；本批与图一同推送。

- 2026-09-16（第九批：界面可中断在跑的流水线 —— 三处入口 + host 取消门禁 + 行为级测试）：**维护者问「流水线触发之后界面上没办法直接中断，这个功能是不是可以加个，另外 dsh 支持不，我们应该实现了接口只是没在界面上弄操作按钮吧」**——三问都先查证再动手。
  **① 核查结论（用户猜对了）**：`descriptors.ts:85-92` 有 `teamflow/cancel`（wire 参数恰为 `[runId]`）、`host/index.ts:793-797` 有服务方法 `cancel(runId) → cancelRun`、`host/index.ts:434-440` 有模型工具 `teamflow_cancel`、`pipeline.ts` 的取消语义（置 `cancelled` + `dispose()` 在飞子代理 + 持久化）与 9 处 `journal.cancelled` 检查点全在；而 `client/` 全目录 grep `cancel` 的 10 处命中**全是状态文案**（`status.cancelled`/`runStatus.cancelled`/终端态数组），**0 个调用点** → 确实只差 UI。
  **② DSH 能力核查**：宿主原生 `SubagentRun.dispose()`（`packages/subagent/subagent/src/types.ts:329-333`，官方契约原话「Cancel remaining work, reach child quiescence, and release resources. Idempotent.」）；宿主另有 `subagents.interruptByParent(childSessionId, parentSessionId, mode:'continuable')`，但它**只服务 continuable 常驻子代理**，流水线用的是 `subagents.start` 的一次性 run（`runner.ts:167-173`）→ 继续用 `dispose()` 是正确路径，不换。
  **③ 顺带发现并修掉一个既有缺陷**：旧 `cancelRun` 只要 runId 在内存 `runs` 里就 `j.cancelled = true` 并返回 `true`——对已 `completed` 的 run 是**污染 journal**（`cancelled:true` 而状态仍 `completed`），对重启后由 `loadJournals()` 回填的 `interrupted` run 是「提示取消成功、状态永远不变」（`host/index.ts:539-548` 把 running/pending 残留统一标 interrupted）。现加门禁 `if (!j || j.status !== 'running') return false`——`running` 是唯一真实活动态（`executePipeline` 首句同步写入、早于任何 await，故无窗口），且**只置位不改状态机**（终态仍由收尾落定：`cancelled` + 不提交 + 保留 resume 入口）。**阶段间隙（`inFlight` 为空）必须仍可取消**：`inFlight` 每阶段覆盖/阶段末删除（`runner.ts:175/251`），若拿它当门禁，host 侧 IO 期间按钮就失效了。
  **④ 落地（三处入口 + 一份实现）**：`client/shared.tsx` 新增 `CancelButton`——**两段式内联确认**（首击变「确认中断?」、3 秒无操作复原、`stopPropagation`），理由是全目录 grep `confirm|dialog` **0 命中**（客户端没有宿主对话框依赖，不引新依赖也不改宿主能力面）。入口：会话内工作台顶栏（`index.tsx`，带 `cancelSentFor`：发出请求后先隐藏按钮，等 2s 轮询把状态刷过来，避免重复点出「未生效」）、全局面板 run 行（`panel.tsx` RunList，仅 `status==='running'` 的行）、run 详情头（RunDetailPane，右栏 tab 与面板浮层共用）；数据面统一走 `productApi.cancel`（`unwrap(await remote.cancel(runId))`，`ok!==true` 抛 `cancel.failed`）。文案 7 个 key × zh/en（`cancel.btn/btnWithId/arm/busy/sent/tip/failed`），工作台报错可见、面板给 `cancel.sent` hint；刷新沿用既有轮询，**不新增事件通道**。
  **⑤ 可测试性重构（顺带）**：`cancelRun` 从 `pipeline.ts` 移到 `core/context.ts`——它只操作该模块声明的 `runs`/`inFlight`，而 `pipeline.ts` 链到 `report→@deepseek-ai/dsh-llm`（宿主私有 peer，仓库内**没有** `node_modules`），行为级测试根本 import 不进来。移动后新增 **`test/cancel.test.js`（30 断言）**：门禁真值表（completed/failed/interrupted/pending/undefined/未知 id/null 全 false 且不置位）、running 置位 + `dispose()` 恰好一次 + 落盘 + 状态机不被抢、阶段间隙可取消、`dispose` 抛错不吞掉取消、以及 **descriptor↔服务↔三处 UI 入口同源**（含「两段式确认只有一份实现」）。`package.json` test/prepublishOnly 挂上（15 个测试文件），smoke 的 h() 渲染闸加入 `CancelButton`。
  **⑥ 已知边界（如实写进 `AGENTS.md` §5）**：`inFlight` 每个 journal 只记**最后启动**的那个子代理 → 取消会立即中止当前阶段子代理，但**并发 dev 子任务的兄弟不被 dispose**（跑到自然结束，下一个 `journal.cancelled` 检查点不再启动新阶段）。要立即中止全部兄弟需改 runner 的 inFlight 结构（存集合 / per-run AbortController），本批**刻意不动核心执行路径**。
  **⑦ 验证**：`pnpm run typecheck` exit 0、`pnpm run bundle` exit 0（`lib/host.mjs` 已含 `j.status !== "running"` 门禁、`lib/client.js` 已含新按钮与词典）、`pnpm test` 15 个文件全绿；待维护者 `node deploy.mjs` + 重启 `dsh --profile web` 后点按实测。
  **⑧ 实地验证（维护者点界面按钮）与一处被截图暴露的文案修正**：维护者在 slugkit-en 跑英文需求并**点按钮**中断（`tf-mu2ye559-obtauw`，lite / 29.8s，中断落在 PRD 阶段）——journal `status=cancelled`、`cancelled=true`、`humanIntervention=false`、`error=运行已取消`；阶段 `#1 prd` = `cancelled/cancelled`（在飞子代理被 dispose）、`agentsStarted=1`、被取消的阶段仍记 token（10.4k 未命中 / 47.6k 命中 / 526 输出 / 4 calls）；**未提交**（slugkit-en `git log` 仍停在上轮 `f783bff`，工作区只剩未跟踪任务夹）；项目内无 `logs/`，归档 `$DSH_HOME/teamflow/ws-slugkit-en-7fbf1b55/logs/tf-mu2ye559-obtauw/run.log`；backlog `req-4` 保持 `in-progress` 并记「流水线已取消（可 teamflow_resume 续跑…）」、`task-4` = `pending`（resume 入口保留）。**客户端按钮路径就此验证**（此前唯一未实测环）；截图另外证实取消后「↻ 从断点重跑」出现在同一位置，且 journal 无 resume 痕迹（`stages=1`、mtime 未再写、`interruptedAt=null`）——排除了「截图是 resume 之后」的误读。
  **文案缺陷（截图暴露，当场修）**：`client/locales.ts` 的 `status.*` 是 **backlog 卡片词表**（`closed`/`cancelled`/`verified` **都**译「已关闭」/`Closed`），而阶段渲染此前直接复用 `stText` → 被中断的阶段显示「已关闭」，与同一屏 run 行的「已取消」自相矛盾（en 更明显：`Closed`）。修法：新增阶段专用词条 `stageStatus.cancelled` =「已中止 / Stopped」（刻意避开 `runStatus.interrupted`「已中断」与 `runStatus.cancelled`「已取消」两个已占用的词）＋ `shared.stageStatusText()`（只有 `cancelled` 分道，其余阶段状态仍共用同一张表，不进两套词表），5 处阶段渲染改走它（流水线节点 / 阶段抽屉 / 阶段行 / 选中阶段 / 尝试历史）；顺带修两处同义重复与误标——阶段行里多余的原文 `cancelled` chip、以及「尝试历史」把被中止的尝试显示成「进行中」（`a.status` 为 `cancelled` 原本落进 else 分支）。门禁：`test/smoke.js` 新增 3 条（阶段词表分离 + 5 处调用点不得回退 `stText` + 词条 zh/en 同形取值正确）。
  **⑨ 同一病根的第二处（取色）：被中断的相位组头是红的**。文案修完后维护者再发一图问「这是符合预期的不」——文案对了（阶段卡「已中止」、run 行「已取消」、token 10.4k/47.6k/526·82% 与 journal 逐位吻合、resume 入口在），但**节点上方「#1 PRD 产品需求」组头是错误色（红）**：`layoutFlow` 的 `anyFail` 把 `cancelled` 与 `failed`/`needs-human` 并列（`client/index.tsx:54`）→ **用户自己点的中断被渲染成失败**，而同一节点里阶段卡竖条与「已中止」chip 是灰的（`STATUS_COLOR.cancelled = T.text2`）= 红头灰身；组头的红还顺带把该区域渐变底与连线一起染红。**同为既有写法被冷路径走热才显形**。修法 1 行：从 `anyFail` 删掉 `|| s.status === 'cancelled'` → 组头与连线落到兜底灰（与 chip 同色），真失败仍为红；**不会误变绿**（`allDone` 要求每个阶段都 `done`）。门禁：`test/smoke.js` 新增 1 条（正断言 anyFail 只认 failed/needs-human + 负断言其后 120 字符内不得再出现带引号的 `cancelled`）。
  **⑩ 并发中断的两个真缺陷（维护者第二次实测：dev 组里按了中断，却又冒出一个"开发中"）**。现场：resume 后的 run（`#9-obtauw`）跑到 dev 阶段（`×5` 并行），按中断后一个节点变「已中止」、其余 4 个仍「开发中」，并且**又自我启动了一路**。`git log`/journal 层面取消没错，问题全在 runner 的并发池：① **队列补位**——`runPool` 的 worker 循环只写 `while (cursor < items.length)`，被 dispose 的任务返回 null（`withRetry` 因 `journal.cancelled` 正确提前返回）后，worker **立刻 `cursor++` 取下一个任务并启动新子代理** → 用户看到"中断了又自动启动一个"。修：`runPool(items, max, fn, shouldStop?)` 取任务前判定，dev 与 resume 补跑两处传 `() => journal.cancelled`；未启动条目 `undefined`，调用方一律 `r &&` 过滤（resume 补跑循环补 `if (!t) continue`）。② **只 dispose 最后一路**——`inFlight` 旧形状「每 run 一个 `{run, stage}`」被每个阶段 `set` 覆盖，`cancelRun` 只 dispose 最后启动的那个 handle，**并发 dev 的兄弟照跑**（这正是我 ⑧ 里写进 AGENTS 的"已知边界"，维护者当场踩到并判定为缺陷）。修：`inFlight` 改 `runId → Map<stage, run>` + `trackInFlight`/`untrackInFlight`（阶段末只注销自己那一路、空桶自动清），`cancelRun` 遍历 dispose 全部，并对 `dispose()` 的 Promise 挂 `.catch`（它返回 Promise，拒绝不得变成未处理拒绝）。**顺带**：`runPool` 从 `runner.ts` 搬到 `host/util.ts`（runner 链 guard→`@deepseek-ai/dsh-llm`，仓库内 import 不进来，行为级测试取不到并发池）；`test/cancel.test.js` 新增 4 组断言（5 路并发全 dispose 且各一次 / 取消后不再取新任务·未启动项为 undefined / `shouldStop` 缺省行为不变 / 两处调用点同源），全套测试 + typecheck + bundle 通过。
  **⑪ 第三处：取消后径直进 QA（resume 路径漏了收口）**。维护者重启后复验：「现在能一次性终止了，但是直接进入了 QA」——截图里 dev 组 ×3 三路全「已中止」，紧接着 QA 组起了一个「开发中」的节点。根因在 `pipeline.ts` 的 dev 收口：**取消检查（`if (journal.cancelled) return`）与提测门禁（failedCount → needs-human + throw，实锤 r26）原先只写在「新开发」分支里，`if (resume)` 补跑分支一个都没有** → 从断点重跑时中断，两个分支汇合后直接 `persistJournal` → 进 QA → 起 QA 子代理。更早的同类隐患：resume 补跑**失败**也会带着已知缺口直接进 QA（QA 检查轮必然重复报告同一缺项，正是 r26 白烧 450k 的形态）。修法：把两者移到 `if (resume) / else` 的**汇合点**，且顺序为**先取消检查、后提测门禁**——取消时 dev 任务的 `failed` 只是「没跑完」（`text=null`），不该被记成提测失败转人工；顺带 `devResults || []` 兜底（取消时未启动条目是 undefined）。副作用（对齐契约而非新增行为）：**resume 补跑失败现在也会停线不进 QA**（与 §5「任务 failed 不进 QA」一致，人工处理后 resume 再补）。门禁：smoke 两条源码断言（「取消检查 → 提测门禁」在汇合点且顺序正确 + 门禁全仓只有一处）。
  **⑫ 第四处：取消把 run 卡在「运行中」（终态归一），并解释「主 agent 为什么去找原因」**。维护者发来主线程自述的排查截图并问「手动中断会导致主 agent 这是去找原因了？」——先把会话记录挖开（`$DSH_HOME/sessions/.../session.v3.jsonl.zstd` 是 **98 帧 zstd 多帧**，逐帧解压得 207 条）：① **中断确实会唤醒主 agent，且这是设计**——run 终态我们主动投递（`report.deliverCompletion`），主线程空闲时走 `parent.followup`：记录里 01:36:49 的 `agent/inbox/spliced / target:"next-turn"` 立刻变成一条 `user/message`（「⏹ 已取消」），主线程 01:36:52 就开始推理「The run was cancelled by someone/something」。② **但它"去找原因"是被我们的 bug 引的**：会话里全部 teamflow 工具调用只有 `start/status/backlog/cancel`（**没有任何 `teamflow_resume`**），那几轮续跑全来自 **界面「↻ 从断点重跑」按钮**；而它越查越糊涂是因为 02:03:54 那条汇报写着「状态：running」却 `cancelled=true`（它据此怀疑 host 内置自动续跑——其实插件与宿主都没有）。**根因**：`status='cancelled'` 只在 `catch` 块里设置，而流水线那 9 处 `if (journal.cancelled) return` 是**正常返回**、不抛异常 → catch 被跳过 → finally 只更新 endedAt/归档/汇报，**从不改 status** → run 永久停在 `status='running' && cancelled=true`：工作台一直「运行中」+ 中断按钮（再按取消也无效：门禁只认 running、恰好放行，但已无在飞子代理可停）→ 界面又给「↻ 从断点重跑」→ 点一次重跑一轮 dev（5 个失败任务）→ 再取消…循环；同时归档/孤儿收口（按 `status==='cancelled'` 分支）也全被跳过（所以连 backlog 的「流水线已取消」事件都没写）。**修法**：`executePipeline` 的 **finally 首句终态归一**——`if (journal.cancelled && journal.status === 'running') → status='cancelled' + error||=运行已取消`（幂等；throw 路径已置位时无副作用；completed 不受影响）。门禁：smoke 两条（归一语句存在 + 其后第一个 endedAt 就在同一 finally 块内，即先归一后收尾）。
  **⑬ 第五处：主线程不知道「谁停的」（取消来源）+ 旧文案把续跑当"下一步"教给模型**。维护者贴出主 agent 抛给用户的选项题——它写着「外部续跑源在另一个会话（DSH-Plugin），我无权限操作」，并给出「要不要硬阻断这条 run / 继续观察」等选项，**前提全错**（那几轮续跑是维护者自己点界面按钮，插件与宿主都没有自动续跑）。复盘信息缺口：① 我们的汇报只说「⏹ 已取消」，**不带任何来源信息**；② `report.next`（通用下一步）把 `teamflow_resume` 当"继续处理"的手段教给模型 → 它自然会去想"要不要续跑/谁在续跑"；③ 加上 ⑫ 的卡 running，三者叠加就长出了这道错前提的选项题。**修法**：`cancelRun(runId, source)` 显式带来源（`'ui'`=界面按钮人工 / `'tool'`=模型工具 / `'unknown'`=历史），落 `journal.cancelSource`+`cancelRequestedAt`（`store.serializeJournal` 一并持久化）+ journal.logs 记「收到中断请求（来源：…）」；`host/index.ts` 两处调用点分别传 `'ui'`（Remote/界面）与 `'tool'`（模型工具）；完成汇报新增「中断来源：{source}」，并把**取消态的下一步换成 `report.nextCancelled`**：明写「本 run 不会自动续跑（host 无该机制）；如需继续请**让用户**点工作台『↻ 从断点重跑』；取消是人的决定，不要自行调用 `teamflow_resume`」。词典新增 zh/en 六条（`report.cancelSource`/`report.nextCancelled`/`cancelSource.{ui,tool,unknown}`/`log.cancelRequested`）。门禁：`test/cancel.test.js` 新增 7 条断言（来源三态 + 时间戳 + 日志留痕 + 落盘 + host 两处调用点来源可辨 + report 措辞切换）；全套测试/typecheck/bundle 通过。
  **⑭ 端到端验证（维护者在 slugkit-en 实测，全部命中）**：重启后（web PID 35468 / 03:16:54 启动 > 修复部署 02:53:05）在 `tf-mu2ye559-obtauw` 上点「⏹ 中断」——主线程收到的汇报新增「**中断来源：工作台界面按钮（人工点击）**」与「不会自动续跑…不要自行调用 teamflow_resume」，主 agent 当场照此收敛（不再猜「另一会话在续跑」、不再自行 resume，回复明确「本轮不动：不用 teamflow_resume、不改工作区、也不碰 run journal」）。journal 侧逐条印证（读盘核对，非目测）：① `status=cancelled` + `cancelled=true` + `cancelSource="ui"` + `cancelRequestedAt/endedAt` 同为 02:19:28 + `error=运行已取消` → **终态归一**生效（不再卡 running 半态）；② logs 有「收到中断请求（来源：工作台界面按钮（人工点击））——在下一个阶段边界生效，随后收尾为「已取消」」→ **取消来源落盘**生效；③ **3 路 dev 子代理 02:19:22→02:19:28 同秒结束** → 多路一次全停生效（旧实现只停最后一路，其余跑到自然结束）；④ 日志写「补跑 **5** 个失败任务」却只起 3 路（并发上限），取消后另 2 个**再未启动**、`agentsStarted` 停在 25 → **队列不再补位**生效；⑤ 最后阶段全是 `dev`、**没有转进 QA**（阶段分布 prd 2 / tech 1 / dev 20 / qa 2，两个 qa 属更早轮次）→ **dev 收口汇合点**在 resume 分支同样生效；⑥ 全部 dev 阶段 `taskKey` 已落库。至此本批 4 个缺陷（dev 标题 / 取消来源 / 终态卡住 / resume 收口）+ 2 个硬化（多路 dispose / 队列不补位）全部有实测证据。**方法论（本次踩过）**：判断"重启到底生效没有"别靠时间线猜——`~/.dsh` 下插件副本只有一份（`profiles/web/node_modules/…`，无 .pnpm 第二份、`cache` 无插件缓存），直接调插件工具 `teamflow_status(runId)` 看**stage 里是否带 `taskKey`** 即是新旧投影的判据（旧投影无此字段）。

- 2026-09-15（第八批：收口提交静默失效 4 天 —— 真 git 复现 + ①③ 修复 + 集成测试门禁）：**维护者让英文流水线跑完后来验收改造**（`_scratch/durparse-en` / `tf-mu2pjhnt-0pjikk`，lite/en/completed）。A/B/C/D 全部验收通过（`qaRounds` 两条：r1 `blocking=1, withCheck=1, withCriterion=1, gate=true, fixCalls=39, outcome=rework`；r2 `blocking=0, resolved=1, outcome=pass`；复验报告专节 *Prior-round checkers re-run, verbatim*；归档 12 文件只留脚本+笔记、丢弃 8 个可重跑输出、项目内 `logs/` 消失；`Acceptance verdict: ✅ Pass` 逐码位核过），**但验收时顺手发现：run 跑完了却没有 commit**。
  **① 现场与归因（不是这批改造引入的）**：`durparse-en/.git` 无 ref、无 reflog、无 `COMMIT_EDITMSG`，HEAD 未出生，而 12 个 blob 与索引的写入时间正是 21:47:19（run 收尾那一刻）→「文件 staged 但没有 commit」；journal 里 `commitDone`/`commitSkip`/`commitFail` **三条一条都没有**。全量扫 53 个 run：最后一次 `统一收口提交完成` = **2026-09-10 16:56**，此后 4 个 run（assetd 09-11 16:59、插件仓 09-15 11:15、slugkit-en 12:08、durparse-en 13:47）全部静默不提交——**slugkit-en 那次在 A/C/B/E/D 落地（20:29 起）之前**，`git blame` 也指向 09-11/09-12 的「收口提交面」批次（`59893959`/`37672c43`/`fdc32b0b`）；插件仓 11:15 那次是 `humanIntervention`（不提交是对的）。
  **② 根因（真 git 复现，不是推断）**：上一版的两处写法互相拆台——`ensureLogGitignore()` 先把 `logs/teamflow/` 写进 `.gitignore`，紧接着 `tfAddArgs()` 用负 pathspec `:(exclude)logs/teamflow` **点名**这个「显式点名且被忽略」的路径，git **报错退出 1**（`The following paths are ignored by one of your .gitignore files`；`-- 12 个文件仍然 staged`）；而 `add === null ? null : git commit(...)` 用 add 的结果把提交**短路**，`if (cm !== null) … else if (add !== null) …` 又让三条日志一条都不触发 → **故障完全不可见**。把 `.gitignore` 那行删掉，同命令 exit 0——这正是它 09-10 能用、之后必然失败的分界。变体矩阵（temp 仓库实测）：`:(exclude)logs/teamflow` / `:(exclude)logs/teamflow/**` / `:(exclude,glob)…` / `:(exclude,literal)…` / 换序 / `-c advice.addIgnoredFile=false` **全部 exit 1**；只有「无负 pathspec」或 `:(exclude)logs`（会误伤用户自己的 `logs/`）是 exit 0。
  **③ 修复（①+③，用户拍板）**：① `tfAddArgs()` 收敛为 `git add -A -- .`（忽略交给刚写好的 `.gitignore`，它才是整树 add 的唯一依赖），新增 `sanity.tfUnstageArgs()`（`git rm -r --cached --ignore-unmatch -- logs/teamflow`，只动索引不删文件）紧随 add 执行作为**索引兜底**——真摘出东西会记 `log.logsUnstaged` warn（那是 `.gitignore` 防线失效的信号，不能静默）。③ 提交不被 add 结果短路：**永远尝试提交**，由提交结果分派 `commitDone`/`commitSkip`/`commitFail`（「无事可做」先用 `git status --porcelain` 空判定 = 确定性，措辞兜底 `GIT_NOTHING_TO_COMMIT` 覆盖实测三种英文措辞 + 中文 git）；`gitCmd`（null-only）之外新增 **`gitRun` 返回 `{ok,out,error}`**，把 git 的 stderr/stdout 真实原因写进日志——**失败可以处置，但不能不可见**（这是本次真正的教训：坏了 4 天，日志上一个字都没有）。两处提交点（收口 + `preAction=commit`）同步改造，`log.preCommitFail` 补 `{msg}`。
  **④ 门禁（缺的就是这一层）**：新增 **`test/commit-path.test.js`（真 git 集成测试，23 断言）**——①防回退：负 pathspec 在规则存在时必须 exit 1 且报「paths are ignored」（谁写回去立刻红）；②修复路径：写规则→整树 add→索引兜底→commit **真跑通**，断言暂存区/提交树里都没有 `logs/teamflow`；③兜底与判据：强制把自有日志塞进索引后能摘干净（且工作区文件没被删）、`status --porcelain` 空判定、第二次 commit 的失败被正确分类。**为什么以前没有**：`test/gitignore.test.js` 只断言 pathspec 的**参数形状**，恰好把 bug 形态锁成了契约（已改）。`test/smoke.js` 同步（新增「零回退」断点：`add === null ? null` 与 `:(exclude)` 出现即红），`package.json` test/prepublishOnly 挂上。环境说明：该测试需要能 spawn git 的终端，受限沙箱下打印 SKIP 不失败。typecheck + 全套 14 文件测试通过（git 集成 23/23、gitignore、smoke、L1/L2 全绿）。
  **⑤ 遗留**：失效期内产生的未提交 run（assetd 09-11、slugkit-en、durparse-en）需人工 `git add -A -- . && git commit`，插件不回头补提交；用户级 profile 副本需 `node deploy.mjs` + 重启 `dsh --profile web` 才生效。
  **⑥ 同批收尾（2026-09-16 发版前）**：① 起跑自愈清扫首次真实触发（slugkit-en 里 09-15 那次 run 的 49 个旧文件 → 保留 26 / 丢弃 23 共 401 KB，项目内 `logs/` 消失）；② en/zh 两条 patch run 实测提交面（`Unified closing commit done` / `统一收口提交完成`；中文 commit message 46 个 CJK 码位、0 个 U+FFFD）；③ **发现并订正一处文档错误**：en 验收四档字面量在 AGENTS §5 / CHANGELOG / 本日志里被记成 `❌ Not passed`，而 `acceptancePrompt` 自 `37672c4` 起**实发 `❌ Fail`**（`Not passed` 只是 `parseAcceptanceVerdict` 兼收的等价写法，L2 语料与 `verdict.test.js` 各有一条）——判据层无影响，改文档对齐；④ **P3 缺陷卡生命周期**：ADR-0007 只让复验/验收自动关 P0–P2，P3 观察项刻意保留 → 人工修掉的 P3 卡会永远挂在「待认领」（看板出现假待办）。按代码现状逐张核对后关掉 4 张已修的（`bug-20` `.gitignore` 头、`bug-24` 全角连接符、`bug-25` `RETRY_SUFFIX`、`bug-27` en 档位行半角），**保留 `bug-26`**（工具描述内嵌中文，TODO 明确「先定判据再动」），未关闭 P3 从 14 张降到 10 张。
  **⑦ 发版（v0.1.9 已发布）与两条 SOP 教训**：`npm publish` 于 2026-09-16 完成（`latest=0.1.9`；已发布产物的 `dist.shasum` 与本地构建 tarball **逐字节一致**），tag `v0.1.9`（annotated，打在 main 的发布提交上）已推送，GitHub Release 正文即 `docs/releases/v0.1.9.md`（中英双语，变更清单由 `git log v0.1.8..HEAD` 生成，非凭记忆）。**教训一（我判断错了）**：registry **读路径**（`npm view` / packument / `/<pkg>/<v>`）在发布后**滞后数分钟**——我据此断言「发布没落地」并让维护者复查，而写路径早已生效；权威判据是**重发时的 403 `cannot publish over the previously published versions`**（幂等安全、无副作用），或等几分钟再读。**教训二（维护者判断对）**：`git push` 连不上是本机 **VPN/TUN 状态**所致——我曾归因为「HTTP/2 被中间设备掐断、换 `http.version=HTTP/1.1` 才好」，但同一条 h1 命令在链路恢复前**也失败过**（`Empty reply from server`），属**伪相关**；该建议已撤回，**未落任何配置或文件**。**流程变更**：合入方式自 v0.1.10 起统一走 **PR**（release 分支 → PR → squash 合入 main → 删分支），已写入 AGENTS §4。

- 2026-09-15（第七批：QA 打回闭环的返工治理 —— A/C/B 三件套）：**维护者问「QA 打回→修→打回→修 有没有解决方案」**。答之前先把 r9 那次停线的性质查清了——这一步改变了结论：
  **① 先纠正前提：r9 的停线是幻影，不是循环失控。** 现场证据：round-3 报告（`docs/teamflow/20260915-r9-host-i18n-locale/QA-REPORT.md`）第 3 节是「round-2 缺陷复验对照」表，`| R2-1 … | P2 | **已关闭** | … |`——旧位置判定把第 2 格当严重级、第 3 格当模块，于是「已关闭的一项」每轮都重生为一个 P2 阻断缺陷；而同报告真实缺陷表（§4）全是 P3，并明写「全部 P3，不阻断验收」。用**修好后的解析器现场重放这份真实报告**：`解析到缺陷 4 条；阻断级（P0-P2）0 条 ✅`。真实轨迹：round-1 **6** 条 → round-2 **1 条 P2**（R2-1）→ round-3 **0** 条 = **在收敛**。
  **② 唯一那轮真实返工的病因**：R2-1 = round-1 对 QA-3 的修复**不完整**——只改了看得见的实例，同类 4 处仍在 `prompts/index.ts:312/314/416/417`。**所以真正的病是「修复不完整 / 扫描面不完整」，不是「缺陷多」，更不该靠"少发现缺陷"去治**（QA 的对抗式独立验证是质量来源，r9 那 6 条全是真的）。
  **③ 落地三件套（A → C → B，均为小改，不动状态机语义）**：
  - **A 类别门禁**（`qaFixPrompt`）：每个 P0–P2 修复必须（a）用**一次搜索**定位整类并数全仓命中数，(b) 落**永久可执行门禁**（verify 套件断言；无套件则随检查提交 grep/脚本断言）——修复前失败、修复后通过，(c) 在摘要与证据块给出 `gate:` 与 `class sweep:`（命中数 before→after）。policy 级 + 新增 `FIX_GATE_PATTERN` 记 warn（host 证明不了门禁真存在，硬失败会退化成形式主义）。
  - **C 复验复用**（`qaPrompt` + `state.__runCtx.qaReverify`）：复验轮**先原样重跑上一轮探针**（它们就在 `logs/teamflow/<runId>/scripts/`——**上一批日志收口决定归档留存的正是这一类**，这里回本）**再补上一轮没覆盖的面并说明漏在哪**，必须重跑缺陷行自带的检测命令、**不得重造已有基线**（r9 的 round-3 重做了一整份 HEAD 副本 = 50 文件/1MB）。经既有 `__runCtx` 注入通道下发，**不改 11 个工厂签名**；首轮 prompt 逐字不变（L1 有 `QA-FIRST-PASS-NO-REVERIFY-NOISE` 守门）。
  - **B 缺陷的可执行定义**（`qaPrompt` 表头 + 解析器 + 卡 + UI）：缺陷表新增 `检测命令` + `通过判据` 两列（P0–P2 必填）——**现在就能失败的命令** + 修好后的期望输出。它一次解决三件事：修复方有验收判据、复验方有回归对象、**误报当场证伪**（r9 的 R2-2a「QA 误报」在 round-3 正是靠 `phaseLabel('en','开发')==='Development'` 一条命令自证的）。全链路：`DefectRow`/`parseDefectRows` 取两列（`parseDefects` 瘦身投影形状不变 → 冻结语料零回归）、`syncQaDefects` 落卡（幂等刷新 + 建卡两处）、`itemDetail` 下发 `defectCheck`/`defectCriterion`、两处详情抽屉渲染 + 词典 zh/en 同步。**顺带修一个真 bug**：表格行原先无条件 `split('|')`，QA 按契约转义的 `\|` 照样被切开——这正是 R3-2「实际」串进「关联验收项」的根因，而检测命令列几乎必然含 `|`；改为 `splitTableRow()` **按未转义管道符切分并还原 `\|`**。新增 L2 语料 `qa-report-check-columns.md`（含转义行 + 复验对照表 + OBS 行），conformance **19/19 → 20/20**。
  **④ 没做（需人决策，已记 `docs/TODO.md`）**：轮次上限改「收敛判据」、停线时仍产出验收视角报告、执行体数量本身的收敛——都动流程语义，质量优先，先定判据。
  **⑤ E 落地（同日追加：超限时不再整段跳过验收）**：维护者问「D+E 收益如何」→ 先给两者定价（52 个真实 run 的 journal 实测）：验收阶段 **p50 10 / p90 21 calls ≈ 一个 run 的 6%**；打回真实发生率 **7/52（13.5%）且每 run 至多 1 轮**（唯一 2 轮就是 r9，且那次是幻影）；**触达复验上限 1/52、验收未跑 1/52**。据此判：**E 做**（便宜 + 不是假设），**D 先埋点**（要防的事件 52 个 run 里从未真实发生；且"同一缺陷原样复现"按 id 判不出来——r9 三轮分别用 `QA-*`/`R2-*`/`R3-*`，稳定身份键只有 B 刚加的「检测命令」）。
  E 的实现：pipeline 里 QA 超限分支从「跳过验收 + 记 `log.accSkipped`」改为「**已知问题模式**跑一次验收」——经既有 `state.__runCtx.knownIssues` 下发（不改工厂签名），prompt 追加只读条款（必须产出逐条 AC 表 + 未闭环阻断缺陷清单含检测命令；结论只能是 ⚠️/❌；**不得重跑 QA 整套件**，说明哪些结论引用了 QA 证据；产品决策留给人工）。**host 侧三条硬约束**：① 结论**不采用**（只把模型自己的结论记进日志供参考），`humanIntervention` 保持 true、`mergeEligible` 保持 false、收口提交闸门不变——**accepted 永不放行**；② 验收失败/无文件**只记 warn**，run 结局不变（不会把"停线"变成"失败"）；③ `journal.knownIssuesAcceptance` **持久化**（`serializeJournal`），report 据此把「验收未跑、不要合回」的提示换成 `report.knownIssuesNoMerge`（否则 `acceptanceDone` 变真会让警告消失——这是本改动最容易踩的坑）。顺手删掉被取代的 `log.accSkipped` 文案。回归：L1 新增 `ACCEPTANCE-KNOWN-ISSUES` + `ACCEPTANCE-NORMAL-NO-KNOWN-ISSUES`（常规验收 prompt 零回归），smoke 新增 5 条。
  **⑥ D 的埋点落地（同日追加：先测量再立法）**：维护者拍板"先埋点"。埋点的设计要点全在**身份**上——QA 每轮重编号（r9 三轮 `QA-*`/`R2-*`/`R3-*`），所以 `util.defectFingerprint` 的优先级定为 **检测命令（B 方案起 P0–P2 必填，机器写给机器看）> 模块+实际/期望文本 > 缺陷 id**；`util.compareDefectRounds(prev, cur)` 给出 `newFps / repeats / resolved`（**repeats > 0 = 停滞信号**，正是将来"立刻转人工"的判据）；`util.qaRoundEntry(...)` 把一轮组装成记录（含 `withCheck`/`withCriterion` = B 落地率、`fixCalls`/`gate` = 成本与 A 落地率，`outcome` 区分 pass/rework/limit）。pipeline 每轮写入 `journal.qaRounds`（`slice(-12)`），`serializeJournal` 白名单加该字段（留 12 轮 / 每轮 20 条缺陷，字符串 clip）。**读侧**是只读脚本 `scripts/qa-rounds-report.mjs`（沿用 `migrate-workspace-key-dryrun.mjs` 的"只读分析脚本入库"先例）：逐 run 轮次表 + 聚合三问——收敛 vs 停滞、检测命令可用率、单轮成本。**埋点不改任何行为**（纯函数可单测；`test/verdict.test.js` 新增 11 条断言覆盖指纹优先级、增/减/消解、pass/rework/limit 三种 outcome）。
  **验证**：`pnpm run typecheck` exit 0；`pnpm test` 13 套件全绿（L1 新增 4 条契约；verdict 新增 6 条断言；conformance 20/20）。

- 2026-09-15（第六批：生成量纪律 —— 不制造输出 dump，改由宿主截尾 + spill 承担；附「是我们的流水线还是 DSH 本身」的对照）：**维护者追问「为啥会产生这么多文件？是我们流水线带来的，还是 DSH 本身也会有？」**——两边都有，但性质完全不同（下面第一条是本次查清的产品级事实，值得长期记住）。
  **① DSH 原生就会产生文件，且是有设计的**：过大的工具结果由宿主**截成 tail**，全文 **spill 到会话级私有文件**并把路径回报给模型（工具描述原文：`Long output is truncated to its tail; the full output is saved to a file whose path is reported when available`，`packages/shell/tool-pwsh/src/index.ts:112`／`tool-bash/src/index.ts:78`）；另有一等公民的 spill 子系统（`@deepseek-ai/dsh-spill` seam + `dsh-spill-local` 后端 + `dsh-spill-policy` 策略，base 组合包已挂载），落点是 OS 临时区、名称不可预测、并有**启动 TTL 清扫**（`spill-local/src/cleanup.ts`）。本机实测 temp 下 149 个 `dsh-*` 目录共 8.4 MB（最早 8/17）——**其中 4.8 MB 是 Node 编译缓存**，真正的 spill 只有零头（如 `dsh-subprocess-euDjmm` 311 KB/1 文件）。**关键：DSH 的这些文件不在你的仓库里。**
  **② 我们额外加的，是「把输出落到项目内」这条约定 + 数量乘数**：旧 `TOKEN_HYGIENE` 明令「把命令输出重定向到 `logs/teamflow/<runId>/regression-<phase>.log`」，而一次 run 会起**多个执行体**（r9 = 10 个：PRD 1 + 技术 1 + 开发 T1–T5 三个并发 + QA 首轮 + 两轮打回修复 + 两轮复验；**718 次调用 / 59 分钟，61% 花在打回闭环**），每个执行体都各自 dump 一遍 → 23 个 `.out` + 5 个 `regression-*.log`（1.2 MB，占其产出文件 **41%**），另加 50 个 HEAD 源码快照（35%）与 508 KB prompt JSON dump（17%）。**所以「文件海」主要是流水线约定 + 执行体数量的产物，不是 DSH 的行为**（DSH 只会在输出真的很大时给一个临时文件，且不进仓库）。
  **③ 落地（删约定，不新增机制）**：`TOKEN_HYGIENE` 新增 `[No dump manufacturing]`——**禁止**把命令/套件输出重定向进文件（长输出由宿主截尾 + spill，需要细节时读它报告的路径），并把 `[Log layout · policy]` 收窄为「只放要留存的三类」：一次性检查脚本 `scripts/`、**不可重跑**的命令载荷 `captures.json`、结论 `.md`；同时要求**同一用途不得新增编号变体**（`-run2`/`dbg-repro2`/`dbg-scan3` 就地覆盖）与**改动前基线只物化一次共享**（`probe/head/`，实测一次 run 因后一阶段重做而留下两整份）。三处 `[Log discipline]`（dev/qa/qaFix）与 AGENTS/memory 模板、`productCtx` 的 Doc boundary 同步改写；[Verification evidence] 的核对口径从「对照 logs/ 里的输出」改为「重跑命令（+ 宿主 spill 路径）」。**没有新增任何 host 机制**（曾考虑由 host 预取 HEAD 基线，评估为「省 1 MB 却引入 git 管线 + 生命周期 + 3 处 prompt 面」，按「能删就删」否决）。
  **④ 回归门禁**：L1 契约改写为 `LOG-NO-DUMP-MANUFACTURE`（含**防回退** exclude：旧写法 `APPENDED on re-run`／`redirect command output to a file` 出现即失败）、`LOG-KEEP-ONLY-DURABLE`（三类留存物 + 无编号变体 + 基线只物化一次）、`LOG-DISCIPLINE-NO-REDIRECT`（dev/qa/qaFix 各自重申，排除 `regression-dev.log` 等旧文件名）；smoke 的证据块断言同步改口径。
  **⑤ 留给人的那一半**：执行体数量（阶段 × 子任务 × 打回轮次）才是成本与文件数的**乘数**——已作为「需人决策」条目写进 `docs/TODO.md`（含 r9 实测数字与三个待定判据），本次**不动流程**（质量优先，先定判据）。
  **验证**：`pnpm run typecheck` exit 0；`pnpm test` 13 套件全绿（conformance 19/19、prompt-contract 全通过）。

- 2026-09-15（第五批：归档只留有用的 —— 白名单过滤，`logs` 93% 是噪音）：**维护者一句「logs 里面的文件基本都是没用的吧」**。实测确认：**确实如此**——把刚归档的 r9 run（130 文件 / 3.03 MB）按扩展名与目录拆开看：**41% 命令输出**（`regression-*.log` 5 个 859 KB + `*.out` 23 个 309 KB）、**35% 源码快照**（`probe/head/**` + `scripts/_head-*.ts` 共 48 个 `.ts/.tsx` ≈1.0 MB，**内容是仓库里 git 已跟踪文件的副本**）、**17% prompt before/after JSON dump**（508 KB，由已被保留的 `.cjs` 重新生成）、**7% 一次性校验脚本与合并提交信息**——而**唯一不可重跑、且真被用过的就是这 7%**（r9 的人工验收探针、幻影缺陷回放脚本、缺陷回填/关单脚本）。第四批只做了「搬走」，等于把 2.7 MB 噪音从用户项目搬进 `$DSH_HOME`。
  **结论（判据）**：命令输出是**运行期的上下文卫生手段**（把几百行输出挡在上下文之外，TOKEN_HYGIENE 的真正价值），**不是审计资产**；审计资产是「你查了什么、怎么查的」＝脚本本身 + 回复里的 `[Verification evidence]` 块（后者随 journal 落盘、工作台可见）。故归档 = **只留可重跑/人可读的东西**。
  **落地**：`runlogs.ts` 新增白名单 `keepInArchive`（`KEEP_EXT = .mjs|.cjs|.js|.sh|.ps1|.py|.md` + `KEEP_NAME = captures.json`，大小写不敏感），`archiveOne` 改为**逐文件过滤复制**（命中白名单才 `cpSync`，其余只计数丢弃；**无保留文件时不建归档目录**），散落文件同规则（`<runId>.log` 直接丢弃——内容与 `runs/<runId>.json` 的 journal.logs 同源；笔记类归档为 `logs/<stem>/<原名>`）；journal 日志行带上丢弃计数（`log.logsArchived` / 新增 `log.logsTrimmed` / `log.logsSwept`，zh+en 同步）。prompt 的 `[Log lifecycle · policy]` 同步写明「**Only your checkers and notes survive**… a dump is not evidence that outlives the run — the durable claim is the `[Verification evidence]` block」。
  **存量瘦身**：本工作区已归档目录就地按同一白名单处理——**136 文件 / 3.03 MB → 43 文件 / 278 KB**（丢弃 93 个可重跑输出/快照，2.7 MB）。
  **验证**：`pnpm run typecheck` exit 0；`pnpm test` 13 套件全绿（`test/runlogs.test.js` 扩到 60 断言，新增白名单矩阵与「全是输出的 run 不留归档目录」用例；L1 新增 `LOG-LIFECYCLE-FILTERED`，conformance 19/19）。
  **教训**：**「搬走」不等于「解决」**——上一批把噪音从用户项目搬进 `$DSH_HOME` 就收工，等于换了个地方堆；**归档面必须显式定义**（留什么、丢什么、依据是什么），否则保留策略只是延迟清理。另一个观察：按扩展名过滤仍会漏掉「恰好是 `.js` 的快照副本」（`probe/head/test/*.js` 6 个仍在归档里）——**这里刻意选择「宁可多留代码、不可误删脚本」**（漏留检查脚本的代价 > 多留几份快照）。

- 2026-09-15（第四批：日志根离开用户项目 —— B 方案，两段式暂存 + 归档 + 自愈清扫）：**维护者追问「`logs/teamflow` 什么时候清理？直接放在用户的项目工作空间会不会有干扰？」**实测回答：**没有任何清理逻辑**（该路径上 grep `rmSync|unlinkSync|retention|TTL|cleanup` 零命中），规模已不小——`_scratch/slugkit-en` 49 文件/498 KB、本插件仓 136 文件/3.0 MB、`products/tetris` 1042 文件/17.3 MB；干扰面 = 无界增长 + 文件树遍历类工具（eslint/prettier/IDE 索引/agent 全局 grep/无 `.npmignore` 的 npm publish）。给四个选项，维护者选 **B（日志根移到 `$DSH_HOME/teamflow/<workspace>/logs/`）**并指示「直接做，不开流水线」。
  **动手前先证伪 B 的隐含前提**（否则落地即事故）：子代理能直接写 `$DSH_HOME` 吗？**不能**——DSH 沙箱 `writableRoots`（`packages/sandbox/sandbox/src/roots.ts:52`）= 工作区根 + `/tmp` + `os.tmpdir()`，子代理会话继承父会话 cwd（`subagent/src/child-agent.ts:146,244`）。两次实测：① 子代理写 `C:\Users\gyech\.dsh\teamflow\_probe-outer\a.txt` → `Access to the path is denied` + `[sandbox: file access denied under workspace-write mode]`，三步全拒；同构命令写会话工作区内 → exit 0。② 临时区也不行——DSH 把 `$env:TEMP` 指到**每会话**子目录（`...\Temp\dsh-SoyVl9`），不可预测且随会话回收。宿主进程不受该约束（journal/backlog 一直写 `$DSH_HOME`），故正解是**两段式**。
  **落地（1 个新领域文件 + 4 处接入）**：① `host/core/runlogs.ts`（新）——`archiveRunLogs`（run 终态把 `<workspacePath>/logs/teamflow/<runId>/` 合并复制到 `$DSH_HOME/teamflow/<workspace>/logs/<runId>/` 后删源，空掉的 `logs/teamflow`、`logs/` 一并回收）、`sweepWorkspaceLogs`（起跑自愈：残留目录 + 历史散落 `<runId>.log`／其他文件逐个归档走，名字当 runId，**合并语义、内容只搬不丢**，白名单外的目录名一律跳过不动）、`pruneLogArchives`（每工作区保留 `LOG_ARCHIVE_KEEP=20` 次，按 mtime 淘汰；**正在运行的 run 的暂存目录与归档一律跳过**；任何失败只 warn）。② `store.ts`——`logsArchiveRoot`/`runLogArchiveDir`/`runLogStagingDir`（workspace 槽位缺失时按 `workspacePath` 派生，与 backlog 同键），**host 自身事件日志 `run.log` 直接写归档位**（不再落项目）。③ `host/constants.ts` 新增 `TF_LOG_DIR` + `LOG_ARCHIVE_KEEP`（`sanity.ts` 转出，既有调用点/测试零改动；store 是独立 entry、刻意不引 host 代码，路径段一致性由 `test/runlogs.test.js` 守门）。④ `pipeline.ts` 起跑清扫 + 终态 `finally` 归档（放在收口提交**之后**——pathspec 排除与 `.gitignore` 幂等补写保留，覆盖「用户在 run 进行中自己提交」的窗口）。⑤ `prompts/index.ts` 新增 `[Log lifecycle · policy]`（三处 `[Log discipline]` + `TOKEN_HYGIENE` + 资源表 + AGENTS/memory 模板 + `productCtx` 的 Doc boundary 同源），定性「项目内只是暂存、host 负责搬运——**不得提交、不得自行清理、不得当项目产物**」；`[Verification evidence]` 两处补归档映射（证据块与归档文件可逐条对照）。
  **存量迁移**：本插件仓 `logs/teamflow/`（129 文件目录 + 7 个散落文件，3.12 MB）一次性归档到 `$DSH_HOME/teamflow/ws-dsh-plugin-teamflow-17e2029c/logs/`，仓库内 `logs/` 已回收（`Test-Path logs` = False；归档 136 文件/3.12 MB 已核）。其他项目由各自下次 run 的自愈清扫处理（不越界改别人的工作区）。
  **验证**：`pnpm run typecheck` exit 0；`pnpm test` **13 套件全绿**（新增 `test/runlogs.test.js` 36 断言；`test/journal.test.js` 与 smoke 断言改到新落点；L1 契约新增 `LOG-LIFECYCLE-ARCHIVED`，conformance 19/19）。
  **教训**：① **跨层方案先证伪边界**——B 的隐含前提是「子代理能写 `$DSH_HOME`」，实测直接否掉；不测就写 prompt 的话，子代理会在每条日志命令上撞沙箱（还可能触发升权审批），代价比现状更糟。**读宿主源码只能得到假设，跑一次子代理探针才是证据**。② **临时区不是备选**——DSH 的 `$env:TEMP` 是每会话子目录，看着优雅实则不可依赖。③ **自愈要覆盖历史形态**——旧版本 host 把日志写成 `<workspace>/logs/teamflow/<runId>.log`（散落**文件**，不是目录），只清扫目录的话这类残留会永远留在用户项目里。

- 2026-09-15（第三批：缺陷卡「看不出缺陷是什么」）：**用户实锤——global panel 里 R3-1..R3-4 几张缺陷卡点开只有「QA 缺陷：R3-N」+ 关联 run 的原始需求 + 任务夹，缺陷内容一无所见。**
  **根因三层**：① `parseDefects` 只留三要素（编号/严重级/模块），QA 报告里写好的**复现步骤/期望行为/实际行为/关联验收项四列被整列丢弃**；② `syncQaDefects` 建卡时把 `reproduce/expected/actual/ac` **硬编码成空串**（schema 有字段、没人填）；③ `itemDetail` 下发面**根本没这四个字段**，客户端即使想渲染也拿不到。
  **修法**：新增 `parseDefectRows`（富行：三要素 + 四列 + 原始「表头→单元格」映射），`parseDefects` 退化为同实现的**瘦身投影**（冻结语料逐字节比对的形状不变，注释里写明"加字段请加到富行"）；pipeline 的 QA 缺陷改走富行 → **qaFix prompt 的缺陷清单也从三要素升级为完整描述**（打回修复时开发能直接看到复现/期望/实际）；`syncQaDefects` 幂等刷新四列、标题自解释化（`R3-1 · 日志/注入面（en 残留）`，不再是「QA 缺陷：R3-1」）；`itemDetail` 补下 `defectId/module/reproduce/expected/actual/ac`；客户端**两处**详情抽屉（全局面板 + 会话内工作台）为缺陷卡新增「缺陷详情」块（编号/严重级/模块/复现/期望/实际/关联验收项），并把关联 run 的原始需求降为上下文。
  **存量数据**：一次性脚本 `logs/teamflow/tf-mu2ioilr-95l4th/scripts/backfill-defect-fields.mjs` 用同一 run 的 `QA-REPORT.md` 重放回填（只补字段、不建卡、不改状态）——req-9 的 4 张 R3-* 卡当场可读。
  **顺带发现并堵住**：QA 表格里含字面量 `|` 的单元格（如 `` `retry \d+|follow-up run` ``）会**切开单元格使后续列错位**（实证：R3-2 的「实际」串进了「关联验收项」）——契约三要素在前列不受影响，但值得在 prompt 里禁止：qaPrompt 的 HOST-ENFORCED 缺陷格式条款新增「单元格内字面量管道符一律转义 `\|`、多行文本压成一行」。旧报告不回改（历史事实），解析器对错位行的前列仍正确。

- 2026-09-15（第二批：首次英文 run 实测暴露的三个漏点）：**首个英文 lite run（slugkit-en / `tf-mu2m1r2p-t1gfh1`）当场暴露三处 P2 漏做——host 文案确实全英文（日志实测：`Entering stage: PRD (product requirements)`、`Task folder ready`、`Mode set for stages`…），但"人能看到的三处"仍是中文。**
  **① 阶段子代理的回复语言无人管（最要紧）**：P2 只约束了**产物语言**（`langDirective` → `English Markdown`），**回复语言**只有 qa/qaFix/acceptance 三处带了 `replyLang(state)`，prd/dev/design/scaffold/architect/tech/patchConfirm **全都没有语言字样** → 子代理按上下文（宿主与用户级 `AGENTS.md` 都是中文）**用中文回复**：文件是英文、工作台「阶段性产物」却是中文（实锤：PRD 阶段 output =「**PRD 已交付**：… ## 关键判断（附依据）」）。修法收口在 `productCtx`（11 个工厂共用前缀）新增 `[Reply language · policy]`，一处覆盖全部阶段（不再逐个 prompt 补）。门禁：`test/locale.test.js` 断言 11 个工厂 en→English / zh→中文。
  **② 给模型的工具返回/注入没点名回复语言**：主会话模型拿到英文工具返回后仍用中文回答用户（同一根因：模型跟上下文走）。修法：`tools.ts` 新增 `tool.replyLang` 指令，并在 `tool.start.started`、`tool.ctx.team` 里显式写「用 English / 中文 回复用户」。
  **③ 团队展示名是用户数据、没有英文来源**：英文界面下拉里仍是「软件开发 / PRD→设计→技术→开发→QA→验收」。修法：`TeamConfig` 增 `nameEn`/`descriptionEn`，内置 dev 团队补英文，**存量 teams.json 不改写**（用户数据）→ 缺失时按 id 回落 `BUILTIN_EN`（`teamNameOf`/`teamDescOf`，永不返回空）；host 在 `listTeams`/`selectTeam`/`getActiveTeam` 与 `tool.ctx.team` 注入里按语言本地化后下发（语言仍只有一个读取点，client 不翻译）；client 团队选择器把当前语言加进重取依赖。
  **④ 顺带**：run 日志里的团队名与阶段名改按 run 语言（`pipeline.ts` 原直出 `teams.json` 的中文 `label`，现走已有的 `phaseLabel(locale, key)`——阶段名一直可翻，只是这行没用上）。
  **结论（对验证方式的修正）**：这三处只有真跑一次英文 run 才会暴露——零 LLM 成本的门禁查得出"字面量漏搬"，查不出"模型回复语言"与"用户数据无英文源"。英文端到端验证的价值在此被证实，不能只靠语料/单测替代。

- 2026-09-15（补）：**修掉本轮暴露的两处 host 缺陷 + 顺手清 4 项 P3**（承接同日 host 双语 P2 / `tf-mu2ioilr-95l4th`）。
  **① `parseDefects` 改为按表头认表（停线根因）**：旧实现「任一含 `|` 的行 + 第 2 格 ∈ P0-P3」是纯位置判定，QA 报告里的**复验对照表**（`| 编号 | round-2 级 | 复验结论 | 独立证据 |`，R2-1 行第 3 格写「已关闭」）被登记成一个新 P2 → 每轮复验重生一个阻断缺陷 → 第 3 轮必然超限停线并跳过验收（**自我实现的停线**）。现规则：`defectHeaderCols()` 要求表头**显式声明严重级列**（`严重级/严重度/等级/级别/Severity`），列位置由表头决定（不再假定 1/2/3 列），无严重级表头的表格**整表跳过**，非表格行结束表格上下文（多表互不污染）；仍容忍 `**P1**` 加粗、跳过表头与 `OBS` 观察项。回归覆盖：L2 语料新增 `qa-report-en-reverify-table.md`（真实停线形态，对照表 + 标准缺陷表并存，期望只出 R3-1/R3-3 两条 P3）+ `test/verdict.test.js` 5 条断言（对照表→0 缺陷、zh/en 标准表、严重级列不在第 2 位、多表并存）。conformance **18/18 → 19/19**。
  **② needs-human 的 run 不再邀请合回**：`report.mergeHint` 旧条件只有 `status === 'completed' && !error` → QA 超限时流程 break、验收被跳过、status 仍是 completed，汇报照样写「验收已通过，请询问用户是否合回」（本轮实锤：差点在未验收时合回 main）。现改为 `mergeEligible = completed && !humanIntervention && 验收阶段真 done && !error`，并新增反向守卫：需人工介入且验收未跑时在汇报里显式写「本轮未完成产品验收…不要据此合回 main」（`report.needsHumanNoAcceptance`）。
  **③ 顺带清掉 4 项 P3**：`runner.ts` 护栏中止摘要的连接符改走词典（en 出半角冒号，zh 逐字不变）；`store.ts` 运行日志**文件头**随 run 语言（持久化层是独立 entry，不引 host 词典，只本地化这一行）；en 验收档位行全角 `／` → 半角 ` / `（zh 侧 ／ 逐字不变）；`RETRY_SUFFIX` 补 `attempt \d+`（词典 `dev.taskRetry` 的实际产出，无 `taskKey` 的 label 兜底路径上标题归一才不会漏剥离）。smoke 新增 3u 段 9 条断言。
  **④ 数据收尾**：`req-9` 的 27 张缺陷卡此前全部停留 open（停线导致 `verifyReqBugs` 从未执行）——用 host 自己的关单函数补做（一次性脚本 `logs/teamflow/tf-mu2ioilr-95l4th/scripts/close-req-bugs.mjs`，不手改 JSON）：13 张 P0-P2 → `verified`，14 张 P3 观察项按设计保留 open。
  **仍未做（留 TODO）**：`clip()` 截断标记本地化（41 个调用点，需加 locale 参数，风险在 zh 逐字平价）、`teamflow_*` 工具描述内嵌中文（插件加载期注册 = 无语言源，且中文短语对中文需求的工具触发有正向作用，去掉反而有害——需先定方案）、R3-5 zh prompt 文本口径确认、R3-6 备案。

- 2026-09-15（r9 / `tf-mu2ioilr-95l4th`）：**host 侧响应文案与流水线产物语言跟随界面语言（双语 P2）**。承接同日的客户端双语 P1：英文用户此前看到的工具返回/完成汇报/流水线日志/产物正文仍是中文，产品呈现割裂。
  **语言源（决策 A + 默认 en）**：新增 `host/core/locale.ts` 作为**唯一读取点**（PRD 明令禁止各消费方各自读 settings）——`noteClientLocale()` 收客户端经既有 Remote 面 `teamflow/setLocale` 推上来的浏览器当前语言（含系统探测结果，用户没在宿主显式选过时**只有这条通道**能拿到真实语言）> `settingsPort`（`ctx.get('settings').get('locale').preference`，只读不写、服务缺失/异常一律 null）> `en`。`ambientLocale()`（工具返回/Remote 错误用）与 `newRunLocale()`（起跑快照）分离；`runLocaleOf(journal)` 纯读快照**绝不重解析**，历史 journal 无 `locale` 字段按 `zh`（存量文案本就是中文，用 en 渲染会割裂）、`localeForMissingSnapshot(resume)` 对 resume 一律 `zh`。
  **词典与门禁**：新增 `host/locales.ts`（`t(locale,key,params)` + `langDirective` + `phaseLabel`/`modeLabel` + `dictProblems()` 自检）与 `host/locales/{pipeline,tools}.ts`（两份区词典），`test/locale.test.js` 入门禁（键唯一 / zh-en 同形 / **en 无 CJK** / 值非空 / 词表只增不改）。
  **覆盖面**：11 个 prompt 工厂（经 `LOCALE(state)` 读 `state.__runCtx.locale` 注入，**不新增工厂参数**=11 处签名与全部调用点零连锁）+ AGENTS.md/memory 模板双语（`<!-- teamflow:begin/end -->`、`{{占位符}}`、路径等结构资产不翻译）+ 产物语言指令（`Chinese Markdown` / `English Markdown`）+ 工具返回/分支决策/完成汇报/日志/诊断/state 注入块/triage 理由。
  **判据层只增不改**：结论行 zh 四档逐字保留并**新增** en 四档（`Acceptance verdict: ✅ Pass / ⚠️ Conditional pass / ❌ Fail / 📝 Not applicable`；原文误记为 `Not passed`，2026-09-16 发版收尾订正——prompt 自 37672c4 起实发 `Fail`，`Not passed` 只是解析器兼收的等价写法），`parseAcceptanceVerdict` 的否定词表新增 `fail/failed/not pass/not passed`（**刻意不收裸 `rework`**——en 模板 `⚠️ Conditional pass (rework items listed)` 含该词会误判打回）+ `no failed/no failures` 否定保护；缺陷表 en 表头（`ID/Severity`）可解析，zh 侧与 `**P1**` 加粗容忍零回归；triage 关键词**新增**英文项（`ARCH_SIGNALS`/`UI_SIGNALS` 中文项一字不动，实测 19 个中文样本档位 0 漂移）。L2 语料**只增** 6 条 en 样例 + manifest 追加。
  **验收与停线（重要教训）**：本轮**产品验收未运行**——QA 第 3 轮复验被判「仍有 1 个阻断缺陷（R2-1 en 分支 5 处中文章节名/标签）」而超限停线，但该缺陷是**幻影**：QA 终版报告 §1 写「通过，可交付验收」、§7 仅 6 项 P3，而停线依据来自报告 §3「round-2 缺陷**复验对照**」表（第 3 列「已关闭」）被 `parseDefects` 按列位置误登记为 P2 缺陷 → 每轮复验重生一个 P2 → **自我实现的停线**（`docs/TODO.md` 已立待办：解析限定在缺陷表节内 + 对照表结论列跳过）。
  **人工验收（维护者，替代被跳过的验收阶段）**：`typecheck` exit 0、`pnpm test` 12 套件全绿（conformance 18/18）、**独立抽查 46/46**（自写 `logs/teamflow/tf-mu2ioilr-95l4th/acceptance-probe.mjs`，不复用 QA 的 r3-verify/r3-scan）：语言链（client>settings>en、settings 抛异常不冒泡）、快照语义（历史→zh、resume→zh、新 run→环境）、**11 工厂 + 2 模板 en 产出 CJK 全 0**（4795–10269 字符）、zh 侧仍中文且 `langDirective('zh')` 逐字不变、判据层 zh/en 双四档 + 空结论行/无结论行→needs-human、en/zh 缺陷表均可解析、并**独立复现**幻影缺陷（4 列表喂 `parseDefects` 得 `{id:'R2-1 …',severity:'P2',module:'已关闭'}`）。验收记录：任务夹 `ACCEPTANCE-HUMAN.md`（`docs/teamflow/` 在本仓库按 `.gitignore:28` 不入提交）。
  **遗留**：6 项 P3 观察（`runner.ts` 全角「：」、`clip` 截断标记、`store.ts` 日志文件头、工具描述内嵌中文、en 验收档位行全角 `／`、`RETRY_SUFFIX` en 分支与 `dev.taskRetry` 不同形）+ 同轮暴露的第二个 host 缺陷（needs-human 的 run 仍发"已通过验收请合回"邀请）——均记 `docs/TODO.md`，建议另立 lite 需求；英文 lite/full 两次端到端 run 按 PRD §9 由维护者线外执行。

- 2026-09-15：**客户端界面中英双语（P1：只做客户端展示层）**。起因：用户问「DSH 本身支持中英，我们所有露出内容做双语改造有多大」——评估结论分层：客户端 UI ≈2 人日（宿主机制现成）、host 生成文案 3–5 人日（**DSH 的 zh/en 只在浏览器侧，host 侧零 i18n**——grep `packages/core/tools/src` 全库仅 2 处中文且都在注释；但 `SettingsProvider.get(ns)` 对任意插件开放，故 host 读 `locale.preference` 可行）、产物语言 5–10 人日且高风险（**验收结论是 host 解析契约**，改语言 = 重设计解析器 + 冻结语料）、仓库文档 ~6 万字纯翻译。本轮只落 P1，P2/P3 待拍板。
  **机制（全部复用宿主，不自建 i18n）**：`ctx.locale.register(NS, {zh, en})` 注册词典（`packages/client/locale/src/client/index.ts:370`）+ `ctx.locale.bind(NS)` 取翻译函数（调用时读当前语言，故可常驻模块变量，`:436`）+ slot 注册项声明 `locale: NS`（`ui-slots/src/index.ts:90`；宿主 `useLocaleRevision` 让**每个 outlet 在切语言时重渲染**，`ui-renderer/src/client/scoped-slots.tsx:707`）+ 面板/tab 名称用 `SlotLabel` thunk（`ui-slots:519`；`ui-sidebar` 与 `ui-conversation` 都订阅 locale 后重解析标签）。
  **落地**：① 新增 `client/locales.ts`（NS='teamflow'，247 条 key，zh 为单一事实来源、en 用 `Record<keyof typeof zh, string>` 同形约束）；② 新增 `scripts/i18n-client-codemod.mjs` 做机械改写（**186 处**中文字面量 → `t('key')`；脚本按 old 串长度降序替换、CRLF 归一、命中 0 次即报错——留痕映射即「中文原文 → key」对照表）；③ `client/shared.tsx` 词表**函数化**：`STATUS_TEXT`/`RUN_STATUS_TEXT`/`KIND_TITLE`/`PHASE_NAME`/`ROLE_NAME` 常量表删除，改 `stText`/`runStatusText`/`kindTitle`/`roleChip`/`phaseNameOf` 按当前语言查表（常量表在切语言后会变陈旧），`usageDetail`/`FoldableText` 同改；④ `setTranslator(t, () => ctx.locale.getSnapshot().active)` 把翻译函数注入纯函数层（**词表/格式化/折叠件拿不到组件 prop**，这是选模块注入而非 props 透传的原因）+ `localeTag()` 取代硬编码 `toLocaleTimeString('zh-CN')`；⑤ `apply()` 注册词典 + 5 处 slot 声明 `locale` + 两处名称改 thunk。
  **门禁（test/smoke.js 3o-2 段）**：zh/en key 集合逐条一致（**en 是兜底语言，漏 key 会让用户直接看到 raw key**）+ 词典无重复 key + 客户端**除 console 诊断与存量 `phaseKeyOf` 中文映射外零中文字面量**（漏改 = 界面中英混排，这条是防回退的主闸）+ `localeTag` 无写死 + 5 处 `locale: NS` + 名称 thunk。改 3 条旧断言：原断言中文字面量在 client 源码里，现改为断言「用了哪个 key + 词典里有该 key」（更强：证明可见**且**走词典）。
  **边界（刻意未做，勿当成半成品）**：host 侧（完成汇报/工具返回/流水线日志/分支决策问句）与流水线产物文档仍是中文。原因：前者 DSH 无 host i18n 先例，须先定「语言源」（读 `settings.get('locale')` 是软依赖且只在用户显式选过语言时才有值 / 或新增插件级设置）；后者改语言 = 重设计 `parseAcceptanceVerdict` 的结论字面量契约 + L1/L2 语料（只增不改），属 P3，见 `docs/TODO.md`。

- 2026-09-12（补 20）：**全局面板状态徽章可点筛选（多选）**（用户提议："这块要不要做成单击筛选？"）。落地：① 新增 `filterChip`（按钮版徽章，选中态实心 + 状态色边框）——分组行上的每个状态计数都可点，**多选 toggle**（真实问法是「还没结束的有哪些」= 进行中/待验收/需人工的并集，单选会逼人来回点）；② **筛选优先于折叠**：选中含终态时自动展开（否则点了「已验收 19」却看不到卡片），清掉筛选回到默认折叠；③ **状态可见**：行尾显示「筛选中 N 项 · 显示 x/y × 清除」，无筛选时不出现（不加噪音）；④ **作用域**：backlog 每组独立（chip 本就是分组计数），run 标签加同款一行（`running/pending/interrupted/failed/cancelled/completed/superseded`）——run 的折叠（最近 8 条 + 进行中置顶）**只作用于筛选结果**；⑤ 切产品线清空筛选与展开态（面板不重挂载，必须显式 reset：`useEffect([productKey])`）；⑥ **纯客户端过滤**，host 数据面与 slice 上限一律不动（数据不丢，清除即见全部）。冒烟补 3 条断言（filterChip/toggleRunStatus、每组独立 + 优先于折叠 + 切产品线重置、筛选状态可见 + ×清除）。**教训**：计数徽章天然是"筛选入口"——展示与操纵同一处，比另开一排筛选控件更省认知；但必须同时解决"筛了却看不到"（折叠与筛选的优先级）与"筛了不知道在筛什么"（可见状态 + 一键清除）。

- 2026-09-12（补 19）：**第三次 A/B 基准入档 —— 需求清晰度 × 质量门禁**（新增 `docs/benchmarks/assetd-clarity-vs-gate.md`；`hold-pipeline-vs-native.md` 加配套指针，两篇互链）。
  **设置**：assetd 手写中立基底（既有代码 + 跨 8 模块 + v3→v4 旧格式兼容），同需求 / 同基线 / 同模型 / 同提示词跑 TeamFlow 与原生单会话；**主指标 = 23 条 held-out 盲测**（对两臂都不公开），另有 26 条可见验收 + 回归硬门槛。
  **结果：平局（盲测 23:23、零假交付）**，代价 **≈3.6× token / ≈1.7× 时间**（A' 复跑实测；首跑原始 3.87×/3.07× 含产品 bug 死时间，已去污染）。
  **这是本项目第一次在「判定集对两臂独立」的条件下测质量** —— 前两次的「质量差异」实质是「原生那条没写专项测试」，**判定与实现耦合**；本次是真盲测。结论按负面结果照原样入档。
  **根因**：需求书把验收边界全写死了（v1/v2 直达、迁移幂等、`(ungrouped)` 空桶恒存在、`-` 保留字、改名不动归属…）→ 23 条盲测点**全部有明确依据**（合法盲测的必要条件）→ 测的成了「有没有老实执行写了的」而非「有没有想到没写的」→ 两边都老实执行。
  **与 hold 基准并置得到本篇最大增量**：**流水线质量门禁的边际价值 ≈ 需求书的歧义存量** —— hold（需求书留白）A 质量更强（原生漏 AC-16/17）；assetd（陷阱写死）平局。**单看任一篇都会得出片面结论**，故两篇互链，并在 hold 篇注明两篇质量判定方式不同级。
  **顺带量化了「改需求书」的天花板**：23 条盲测点中 **8 条已被可见用例直接示范**（改需求书零收益）、**12 条可从目标唯一推导**（收益面）、**3 条不可推导必须明写**（`(ungrouped)` 空桶 / `set --group` 接口选择 / `-` 保留字）⇒ **只改 REQUIREMENT.md 的上限 = 12/23 ≈ 52%**，要突破必须**同时**降低可见用例的陷阱示范密度。下一版基准的两支柱据此定。
  **对外口径同步**（`docs/announcement/github-discussion-show-your-plugin.md`）：原写「前两次不代表当前版本、**尚未重测**」——现已重测，故新增第三条基准并改掉该句；产品缺陷计数由 3 个（`affdc21`）更新为 **4 个**（+`1a4de5a` 日志路径作用域）；`release-v0.1.8` 与 `main` 的分支归属纠正（全局面板在 release 线，不在 main）。
  **未做**：不改需求书与判定集（改 = 新基准，两臂数据作废）；v2 需求书草稿仍留在工作台 `v2/`，未纳入本轮。

- 2026-09-12（补 18）：**同需求重跑验证 + 修掉（补 17）引入的路径作用域 bug**。
  **验证**（assetd 同需求重跑，run `tf-mtx6fi2a-ibi7lu`：8 阶段 / 25 分钟 / **零失败 / 零人工介入 / 零 QA 打回**，对比上一轮 T5 误判 → 停线 16 分钟 + 人工 resume + QA 打回）：
  - **交付判定**：全程无「命中拒绝词」误杀，`insubstantial` 失败 0 次 —— 上一轮那种"如实汇报环境限制被判未交付"没有复现。
  - **熔断口径**：0 次触发；各阶段新增 token 60–149k（tech 129k / T1 106k / QA 149k）——这个量级在**旧 60k 口径**下任何一次失败都会立刻熔断取消重试。
  - **收口提交面**：提交 32 文件、**`logs/` 命中 0 个**（上一轮 227 文件里 208 个=92% 是日志）；journal 留痕「工作区 .gitignore 已补忽略 logs/teamflow/」→ 幂等补写真的在生产里执行了。
  - **日志布局**：run 目录 **207 → 30 文件 / 618.6 → 328.3 KB**；**51 份重复套件输出（占 .log 78%）消失**，收敛为 `regression-dev.log`(58 KB) / `regression-qa.log`(66 KB) / `regression-accept.log`(31 KB)；其余 `.log` 各有独立用途（负控制/蓝图一致性/探针/spawn 探测），**无一以全量套件输出结尾**（旧病消失）。
  **但暴露（补 17）的作用域 bug**：prompt 里我把布局写成 `scripts/`、`probe/` 这种**未限定的相对昵称**，模型解析成**项目根约定** → 在项目根建了 `scripts/`(5 文件) 与 `probe/`(1 文件)，且这 **6 个一次性草稿被收口提交进去**（`A scripts/check-t3-ac.mjs` / `check-t4-cli-groups.mjs` / `run-tests-subset.mjs` / `spawn-inprocess.cjs` / `check-cli-group-e2e.ps1` / `probe/spawn-stdio-probe.mjs`）——正是本改动要消灭的那类污染，只是换了个位置（占该提交 32 文件中的 6 个）。
  **修复**：布局四条路径全部改写为**完整限定路径** `logs/teamflow/<runId>/...`，并在段首加粗「Every path below is INSIDE logs/teamflow/<runId>/ — **never create scripts/ or probe/ at the project root**」；dev/qa/qaFix 三处 `[Log discipline]` 各自重申该禁令；资源表同步；L1 契约新增 `LOG-LAYOUT-SCOPED`（断言完整路径 + 项目根禁令，`exclude` 未限定的旧写法防回退），`LOG-LAYOUT-PER-PHASE` 加禁令锚点。附带修一处契约锚点 bug：`<runId>` 是工厂插值，断言须匹配渲染后的实际值（`logs/teamflow/[^/\s]+/scripts/`）。
  **教训**：**给模型指路径必须给"完整限定路径"，不能给"通用昵称"**——`scripts/`、`probe/` 这类项目常用目录名会被按最宽松、最像"项目约定"的方式解析。这与「措辞不能当交付判据」同源：**歧义表述总是被往宽松处解释**。

- 2026-09-11（补 17）：**日志布局收口——每用途一个文件，套件输出追加不新增变体（用户提问驱动：「src 才 50k，日志为啥一次任务 1M？」）**。
  **实测**（assetd `tf-mtwvwpxa-p3vw08`）：`logs/` **623.8 KB / 208 文件** vs `src/` 57.1 KB / 10 文件 = **11×**；其中**插件自己只写了 5.2 KB**（host run 日志），**618.6 KB 全是子代理产出**——所以这不是"日志级别开太细"，是 agent 的工作现场。三类构成：`.log` 99 个 342 KB（命令输出落盘 = prompt 强制用来省 token 的手段）、`.mjs`/`.cjs` **49 个 198 KB**（子代理**现写现用**的一次性校验脚本，最大那个 23.6 KB 是 QA 自写的独立黑盒验收脚本）、`.json` 52 个 69 KB（每次 CLI 调用一个载荷快照）。
  **真浪费点**：99 个 `.log` 里 **51 个是完整回归套件的输出**（`69/82 passed`、`82/82 passed`、`57/64 passed`…），合计 **267.5 KB = 全部 .log 的 78%**；单看一个任务（T5）就有 15 个文件（`t5-run` / `t5-run-nopipe` / `t5-tests` / `t5-tests2` / `t5-verify` / `t5-verify-nopipe` / `t5-verify-nopipe2` / `t5-spec` / `t5-spec2` …）。附带发现：**同一个沙箱绕行被各 agent 重新发明 6+ 次**（`spawn-fd-shim.cjs` / `qa-spawn-shim.cjs` / `qa-stdio-shim.cjs` / `nopipe-loader.mjs` / `nopipe-register.mjs` / `nopipe-spawn-shim.mjs`）——`logs/` 是 per-run 且不跨 run 复用，脚本本身无法传承。
  **为什么不做"清理"**：① 磁盘 0.6 MB/run 不是问题，真正贵的是**重跑套件的 token**（单任务新增 45–80k）；② 不该一验收就删——`[Verification evidence]` 块写「cmd → exit 0, 34/34」，其审计价值**恰恰依赖这些日志能对上**，删了等于拿可复核性换 0.6 MB；③ 不合并「每个 dev 任务各跑一遍回归」——那是质量要求（AGENTS 已判「测试任务合并」为假优化）。
  **落地（prompt 层 · policy）**：① `TOKEN_HYGIENE` 新增 `[Log layout · policy]` 段（含实测代价，让模型知道违规的真实后果）：**每用途一个文件**——套件输出 → `regression-<phase>.log` 且**重跑时追加**带 `--- <timestamp> <task> ---` 表头的段（禁止 `-run2`/`-nopipe`/`-shim` 同名变体）、一次性校验脚本 → `scripts/`、命令载荷 → 合并进 `captures.json`、探针/草稿 → `probe/`；② AGENTS.md 资源表补布局约定；③ dev/qa/qaFix 三处 `[Log discipline]` **指向该布局**并各自点名文件（`regression-dev` / `regression-qa` / `regression-devfix`）。
  **测试**：L1 prompt-contract 新增 2 条契约——`LOG-LAYOUT-REUSE`（四阶段共享锚点，含 `exclude: /qa-out\.log/` 防回退）+ `LOG-LAYOUT-PER-PHASE`（三处指向）；typecheck + 全套 11 文件测试通过。
  **刻意未纳入**：README 不改（避免与工作区里未提交的补 14 文档改动纠缠）；host 侧不加"日志体量观测/告警"（属新功能，非本轮问题）。**代价诚实说明**：本改动是 policy 级——模型不遵守时无硬拦截，收益取决于遵守率；host 侧目前只有既有的 token 观测 warn。
  **教训**：**「落盘省 token」和「落盘堆垃圾」是同一机制的两面**——落盘是给上下文减负的，但规则若只写"写到某目录"、不写"复用与归处"，模型每次都会开新文件；**可复核性要的是"少而稳"的文件集，不是"多而全"的倾倒场**。

- 2026-09-11（补 16）：**收口提交面收窄——插件自有日志不再进提交（用户发现：「logs 没忽略的话，默认会把工程噪音全部提交上去」）**。
  **实证**（assetd 仓提交 `030f65c`）：一次收口提交 **227 个文件，其中 208 个（92%）是 `logs/teamflow/` 噪音**（100 `.log` + 52 `.json` + **44 个临时 `.mjs`** + 5 `.cjs` + 4 `.txt` + 2 `.out` + 1 `.patch`，合计 623.8 KB），真交付只有 19 个（9 `src/*.mjs` + 3 `tests/*.mjs` + README + 任务夹 6 件）。
  **根因（契约在 host 这侧破的）**：① prompts 用 `[Log discipline]`/`TOKEN_HYGIENE` **强制**子代理把命令输出与临时验证脚本写进 `logs/teamflow/<runId>/`，而 prompts 自己的资源表把它定性为「运行日志…**日常不读**」= 非交付物 → 这批文件是插件**必然生产且自称不该交付**的；② `pipeline.ts` 收口提交用**裸 `git add -A`**（注释原文「统一 add -A 必然全带」——本意治 r16 漏提交 QA-REPORT/ACCEPTANCE，代价是把工作区一切脏物卷进来）；③ 唯一屏障是目标仓库自己的 `.gitignore`，而 README 只宣传「日志收口到 logs/teamflow/」，**从没说要忽略它，host 也从没检查/写入**。反讽：T11 的交付报告写着「docs/ and logs/ remain untracked as required」——交付前完全属实，是 host 在最后一刻扫进去的，**子代理的 git 纪律没有任何问题**。
  **修复（两道防线，缺一不可）**：① `sanity.tfAddArgs()` = `git add -A -- . ':(exclude)logs/teamflow'`——**magic pathspec 强制排除**，不依赖目标仓库有没有配 .gitignore、也不怕用户删回去；`-- .` 同时把提交面收敛到工作区（旧 `add -A` 在根目录等价，但不再波及工作区外）。两处提交点（收口提交 + `preAction=commit`）统一走它，**禁止再出现裸 `add -A`**。② `util.mergeGitignore(existing, entries)` 纯函数 + `pipeline.ensureLogGitignore()`：**提交前**幂等补写工作区 `.gitignore`（覆盖判定含更宽规则 `logs/`、`logs/**`；`changed=false` 时**不落盘**，不留无谓 diff；写失败只 warn 不阻塞——pathspec 仍兜底）；只在**即将提交**时写，跑失败/取消的 run 不留未提交的 .gitignore 改动。
  **实测 git 语义**（一次性临时仓库，old vs new）：改动集 = 新代码 + `logs/teamflow/` 新日志 + `logs/` 下非 teamflow 文件删除 + 交付物 PRD 删除。`git add -A` → 连 `logs/teamflow/tf-x/new.log` 一起 stage；`add -A -- . ':(exclude)logs/teamflow'` → 该日志不进，而 `logs/app.log` 的删除、任务夹 `PRD.md` 的删除、新代码 `src/y.mjs` 全部照常 stage（排除是**命名空间级**，不是"忽略所有 logs"）。
  **测试**：新增 `test/gitignore.test.js`（22 断言：提交参数形态 + `mergeGitignore` 创建/追加/幂等/CRLF/注释行不算忽略/兄弟目录不误覆盖/更宽规则已覆盖）+ smoke 3t 断言组（8 条：常量同址、pathspec 排除、**无裸 add -A**、两处提交点、幂等不落盘）+ `pnpm test`/`prepublishOnly` 接入。README/README.en 补「交付面与噪音隔离」条目（含已污染仓库的 `git rm -r --cached logs/teamflow` 处置），AGENTS §2/§4/§5 同步。
  **未纳入本轮**（用户决策另开一轮）：裸 `add -A` 的**同源风险**——它也会把**运行前就存在的场外改动**（用户未提交的工作）卷进这次 run 的 commit，sanity 只 warn 不拦。assetd 本次侥幸未踩（开跑时只有 `docs/`、`logs/` 未跟踪），机制上仍存在。
  **教训**：**插件自己造的文件，别指望宿主项目替它配 ignore**；「写到某目录」和「该不该交付」是两件事——声明了非交付物就必须在**提交面**上强制体现，靠目标仓库配置兜底的契约等于没有契约。

- 2026-09-11（补 15）：**交付判定信号换轨 + 熔断口径修正（假阳性误杀实锤；用户提问驱动：「assetd 空间的 T5 开发为啥成功了还提示失败」）**。
  **实证**（run `assetd/tf-mtwvwpxa-p3vw08` dev seq7）：子代理 `stopReason=completed`、41 次工具调用、产出 4014 字符（clip 4000，另 947 字符被截断）、`[Verification evidence]` 与 `<!-- state -->` 块齐全、`src/query.mjs` 已落盘（resume 状态核对 15 处改动），自述「定向验证 22/22 通过」。它唯一"过错"是**如实汇报沙箱限制**：「`tests/run.mjs` 的 7 个 runCli 用例与 `spec/verify.mjs` 全部 26 例无法执行，属环境性失败」——`REFUSAL_PATTERN` 含「无法执行」，全文命中即否决。
  **判定链（两闸连爆）**：① `util.hasSubstance` 全文拒绝词 → false → runner 判 `insubstantial`「视为未交付」；② `withRetry` 下一道关是熔断——`totalTokensOf` 含 cacheRead = 1,885,583 ≥ 60k（其中 cacheRead 1,830,144，**真实新增仅 55,439**）→ 熔断 + humanIntervention，`RETRY_LIMIT=2` **连一次重试都没走到**；③ `pipeline` 提测门禁（1/11）停整条线 → 人工 resume（11:52→12:08，16 分钟）+ seq14 补跑（补跑 agent 自述「T5 was **already code-complete** from the prior attempt」，即重复劳动）。
  **连带损伤（同一次误杀的次生）**：`devR.text=null` → 证据块**未存证**并派生误导 warn「开发 回复缺少 [Verification evidence] 块（契约未兑现）」（块明明在）；`mergeStageState` 拿到的是假文本「开发失败（Agent 未产出结果）」→ 该任务 state/memory **未合并**。
  **修复 A（信号换轨，非调词表）**：`util.judgeDeliverable` 三级判据——① 客观形态（非空 + 阶段长度下限）→ ② 真交付信号（`DELIVERY_EVIDENCE_PATTERN`：prompt 强制的证据块 = 命令+退出码+断言计数的具体自述，拒绝/放弃类产出给不出）→ ③ 措辞兜底（无证据块且命中拒绝词才否决）；`REFUSAL_PATTERN` 降级为**兜底 + 留痕**（`ok=true` 时也回传 `refusal`，成功路径记一条 warn 说明「为何这句没判失败」，审计有据）。删 `hasSubstance`（含 index.ts 两处死导入）。**教训：措辞不能当交付门禁**——模型汇报环境限制是本分；判据要用**结构件**，不用散文。
  **修复 A'（失败也要留住事实）**：`pipeline.stageTextOf(r) = r.text || stage.output`，dev 主路径/补跑/qaFix 三处证据存证 + 子卡产物 + dev 结果载荷 + state 回写全部改用真实文本；成功与否仍只由 `text` 判定（不松门禁，只止损连锁）。
  **修复 B（熔断口径）**：`metering.freshTokensOf` = input + cacheWrite + output（**排除 cacheRead**）；`FRESH_TOKEN_BUDGET = 200k`（依据：实测单次尝试新增 T5 55.4k / T8 49.3k / T9 79.6k / T11 45.9k → 容纳 RETRY_LIMIT 两轮，真跑飞 3 倍量级才熔断）；**汇报/展示口径 `totalTokensOf` 不变**（官方 billed 口径，AGENTS §4 不动）；字段 `stageTokens → freshTokens`，日志/`stageFailError` 文案明确「不含缓存命中」（旧文案把缓存重放说成"超出阶段预算"，误导排查方向）。顺序仍为「不可重试/外部中止/护栏中止 → 预算门 → 自动重试」，预算合理后**重试优先于熔断**恢复。
  **真实数据 before/after**（重建 T5 完整产出 = 落盘产出 + 宿主记录的命中原文）：旧判据 `REFUSAL_PATTERN=true` → 误杀；新判据 `ok=true reason=ok` 且措辞「无法执行」仍回传；去掉证据块 → `ok=false reason=refusal`（**兜底未被削弱**）。熔断：旧口径 1,885,583 ≥ 60k（不重试）；新口径 55,439 < 200k（走第 2 次重试）。
  **测试**：`diagnostic.test.js` 增 10 条 `judgeDeliverable` 断言（含长度门先于证据块豁免的顺序语义）；conformance 增 2 份语料（`dev-reply-env-limitation.md` / `dev-reply-refusal-bare.md`，**只增不改**）+ 新 parser `judgeDeliverable`（支持条目 `phase`）；smoke 更新旧断言（`hasSubstance`/`STAGE_TOKEN_BUDGET`/`noteVerifyEvidence` 形态）并新增 `freshTokensOf`/`stageTextOf` 断言。typecheck + 全套 10 文件测试通过（conformance 12/12）。

- 2026-09-11（补 14）：**对外口径复核：删掉 README 里无法复核的 lite 省 token 数字**（用户质疑「为什么做这个是不是依赖很早版本的记录」——质疑成立）。**核实**：① 宣传稿原本引用的「省 39% token / 慢 29%」出自 run `tf-mt1es4en-5cekmf`（2026-08-20，内部 v0.10.2，lite 档 4 阶段），数字与 journal 对账一致，但**版本极老**；② 7 天后的第二次基准（`hold-pipeline-vs-native.md`，小需求）**结论相反**——多花 44% token、慢 40%，说明「省 39%」从来不是通用属性；③ v0.11（M0 状态核对 / 蓝图全模式 / M2 拆任务 / M3 架构核验）、v0.12（QA 打回闭环）、v0.1.0（任务夹文档制）**全是质量优先的加法**，只会推高消耗，**自 2026-08-27 起未再测**；④ README 那句「lite 省 ~64% 时间 / ~88% token」源头是 ADR-0004（v0.8.x 期）单样本，无 runId、无从复核。**处置**：`README.md` / `README.en.md` 删除百分比，改为讲「按需求规模裁剪阶段集」的机制（顺带修 README.en `**lite mode **` 加粗失效）；对外宣传稿同步改为「机制 + 两次基准表格（含我们输的那次）+ 明确标注跑在发布前内部版本、当前版本尚未重测」。**历史记录一律不动**（`docs/releases/v0.1.0.md`、`docs/adr/0004`、本文件旧行）——已发布的 release note 与已接受的 ADR 是**记录而非活文档**，追溯性改写比留一个陈旧数字更糟；口径更正走新记录。**教训**：**能对外说的数字必须带可复核出处（runId / benchmark 文档）；ADR 里的单样本实测一旦脱离上下文，就会在 README 里变成营销语。** 核查手法沉淀：直接拉 `$DSH_HOME/teamflow/*/runs/<runId>.json` 对账 billed / calls / 耗时 / 阶段数，比只读文档可靠（文档会滞后，journal 是当时的原始记录）。

- 2026-09-11（补 13）：**全局面板右栏入口改为「去发起会话」**（用户提问：看 tetris 的 run 点右栏却跳回 dsh-plugin-teamflow 会话，"跳转的意义在哪？"）。**根因**：右侧栏是**会话级**的（`RightbarRoot` 只渲染"当前会话"），而我在补 6 里只实现了"切回对话 + 小步重试"——切回去的仍然是**用户来时的那个会话**，与 run 无关；内容虽有 host 按"产品线 + runId"取数所以正确，但**位置**挂在无关会话上，语义为零。**修复（用户选定方案 B）**：把"跳回对话"升级为"**跳到资源所属会话**"——① host 侧 `runBrief`/`snapshotOf` 补 `ownerSession`（journal 早有该字段，只是没透出），`itemDetail.runInfo` 同样补上，并把**产物地址的会话段改用 run 的发起会话**（`artifactSession = ownerSession || 传入 sessionId`），使全局面板（无会话上下文）里的产物也能挂到对的会话，会话内工作台路径行为不变；② client 新增 `goOwnerSessionAndOpen(target)`：先 `sessions.open(ownerSession)`，**等 `sessions.list.getSnapshot().current === ownerSession`**（最多 6 次）**且**对话 seat 挂载 bind 之后才 `openResource`（两个条件都要等，故带就绪判据的小步重试）；会话已被清理（`open` 抛错，宿主按 ISessions 契约对未知 id fail loud）时**只提示不跳转**，老数据无 `ownerSession` 时退回旧行为；③ 文案同步：run 行「去会话右栏」、run 详情浮层「去发起会话」、条目详情的产物按钮也先跳会话再开文件。测试：product-scope 增 `ownerSession` 断言（缺省 null 不伪造）、smoke 增 `goOwnerSessionAndOpen` 链路断言。**教训**：**跨会话的资源 + 会话级的容器 = 必须显式迁移容器归属**；"把用户送回他原来待的地方"不是跳转，是把他扔在原地——跳转目标必须是"资源的主人"（这里就是 `ownerSession`）。另：host 侧的 `ownerSession` 一直在 journal 里，早该透出——**能定位归属的字段没进载荷，前端就只能靠猜**。

- 2026-09-11（补 12）：**全局面板第三版：标签页 + 覆盖式浮层（第二版被用户判定「太混乱」）**。第二版把 **rail + run 栏 + backlog 栏 + 详情栏**四栏并排，还叠了 `grid auto-fit`（够宽并排/窄了堆叠）这套自适应——在用户实际的 1100–1400px 窗口里**必然超载**：每多一栏就挤压其余栏（卡片被压到 ~200px、run 行折成多行、详情栏还和列表抢宽度），触发了两次连环故障（`flex-wrap: wrap` 多行 flex 行高随内容 → 列被撑高、`overflow` 永不触发 → 「展开后无法滚动」；`grid auto-fit` 在超宽窗口下的轨道数计算同样不可预期）。**第三版做减法**：① 主区改为**标签页**（🚀 流水线 run N ｜ 📋 Backlog M），**一次只显示一个列表**——宽度全给它、只剩一个滚动区（run/backlog 的折叠、进行中置顶、终态收起、需人工不折这些已确认行为全部保留）；② 详情改为**覆盖式浮层**（`position:absolute; top/right/bottom + width:440 + overflowY:auto`，与会话内工作台的两个抽屉同款）——不再参与横向宽度分配；③ 删掉 panel 级的 grid/flex 两栏自适应（backlog 卡片自身的 `auto-fill` 网格保留）。冒烟断言改为「标签页存在 + **禁止** panel 级多栏自适应（`auto-fit`/`flex-wrap` 组合）+ 详情为绝对定位浮层」。**教训**：**并排面板的数量必须由可用宽度决定，而不是由信息架构决定**——窄窗口下正确做法是"同一区域切标签/浮层叠加"，不是继续加栏 + 写自适应赌注；宿主 DSH 自己的对话区（对话/轨迹/工作台）就是标签页，不是并排。

- 2026-09-11（补 11）：**全局面板密度优化（用户选定「分栏 + 默认折叠」）**——用户实测 tetris 产品线：run 列表与 backlog 都太长（run 上限 50 条平铺、backlog 需求 20+任务 80+缺陷 30 全渲染成卡片），且两段**上下堆叠在同一个滚动列**里，只能一路往下滚。**落地**：① **左右分栏**——内容区 `flex row + flexWrap`，左 run 列表 `flex:1 1 300px`、右 backlog `flex:1 1 420px`，各自 `overflowY:auto` **各滚各的**；窄屏或打开详情栏时自动退回上下堆叠（各占一半高度，靠 flex 行拉伸）；产品头移出滚动区固定在两栏上方。② **run 默认折叠**：只渲染最近 `RUN_PREVIEW=8` 条 + 「展开全部 N 条」切换；**进行中/未完成的 run 无条件置顶**（`pinnedActive`——不许把正在跑的折叠掉）并显示「已置顶进行中 N」chip；切产品线重置折叠态。③ **backlog 默认折叠终态**：每组把 `accepted/closed/verified/cancelled` 的卡片收起（表头仍显示全量状态计数），按钮「已完成 N ▸ / 收起已完成 N」按组切换；**`needs-human` 是独立标记，始终展开**（需要人动手的不能被藏）。**取舍**：host 的 slice 上限（20/80/30/50）**不动**——折叠是呈现层的事，不该靠丢数据解决（数据仍在，展开即见）。冒烟补 3 组断言（折叠常量与置顶判据 / 终态收起但需人工不折 / 分栏 flex 基准）。**教训**：`vh` 与"平铺全部数据"是密度问题的两个常见来源；默认视图应该回答"现在需要我做什么"（活动项 + 进行中 + 需人工），历史/终态收纳成计数可展开。

- 2026-09-11（补 10）：**工作台顶出外层页面滚动条（backlog 界面三层滚动）**（用户真机截图反馈）。**根因**：宿主 `conversation.view` 的容器 `ConversationRoot.module.css` 的 `.viewArea` 是 `display:flex; flex:1; flex-direction:column; min-height:0` 且**不设 overflow（不滚动）**——也就是说「占满剩余高度并自行滚动」是插件侧的责任；我们的 `TeamFlowView` 根容器只写了 `display:flex; flexDirection:column; padding`，**没有高度约束**，内容一高就溢出可视区 → 最外层（页面）多出一条滚动条；叠加看板自身的 `maxHeight:72vh` 滚动与列内 `maxHeight:340` 滚动，就成了用户看到的"三层滚动"。**修复**：① 根容器 `height:100% + minHeight:0 + overflow:hidden`；② 内容区（流水线/看板）外包 `flex:1 + minHeight:0` 容器，看板自身改 `flex:1 + overflowY:auto`；③ **删掉 72vh 与列内 340 限高**——看板只保留一层纵向滚动（列不再各自出滚动条，卡片不再被塞进小盒）；④ 流水线画布从固定 `height:560` 改为 `flex:1 + minHeight:320`（自适应宿主剩余高度；`fitNow()` 本就按 `clientHeight` 计算，自动跟随）；⑤ 绝对定位的条目详情抽屉移到看板根（`position:relative`）的**兄弟**位置，滚动画板时不再跟着滚走。冒烟补高度契约断言（根填满 + 内容 flex:1 + **禁止再出现 72vh**）。**教训**：宿主给的是「flex 容器」不是「滚动容器」时，插件必须自己闭合高度链（`height:100%` → `flex:1/minHeight:0` → `overflow:auto`），任何一处漏掉都会以"外层多一条滚动条"的形式暴露；同理**禁止用 vh 限高**（与宿主 header/composer 占用的高度无关，必然溢出）。

- 2026-09-11（补 9）：**React #310 白屏 —— `FoldableText` 被当普通函数调用**（用户真机反馈：点阶段后 console 报 `Minified React error #310`，栈 `FoldableText 的 useState ← RunDetailPane`，紧接着宿主 `slots` 报 `slot entry crashed in 'main'`，面板整块空白）。**根因**：`client/panel.tsx` 三处写成 `FoldableText({ text: … })`（**直接调用组件函数**），于是它的 `useState` 被算作**父组件 `RunDetailPane`/`ItemDetailPane` 的 hook**；而这三处都在条件分支里（`det.spec ? … : null`、`sel.output ? … : null`），一次渲染有、一次没有 → React 判定「渲染间 hook 数不一致」→ 抛错并卸载整个 slot（这就是"啥都不显示"）。`index.tsx` 里本来就是正确的 `h(FoldableText, {…})`，只有新写的 panel 踩了。**修复**：三处改 `h(FoldableText, {…})`；冒烟补断言——**枚举核心组件名，要求每个都必须以 `h(Comp,…)` 或作为 slot 注册实参出现**（禁止直接函数调用），把这类"hook 归属错位"挡在门禁上。**教训**：`React.createElement` 风格（无 JSX）下，组件就是函数，**少一个 `h()` 就静默把 hook 挪到父组件**——条件渲染时必炸；凡是含 hook 的"组件"一律 `h()`（纯展示 helper 才可以直呼）。

- 2026-09-11（补 8）：**「阶段详情读取失败：…未知错误」= 二次解包**（用户真机反馈，贴出报错全文后当场定位）。**根因**：`productApi` 适配器的方法**已经** `unwrap` 过（返回载荷本身），而 `RunDetailPane.openStage` / `GlobalPanel.openItem` / `showInline` 又 `unwrap` 了一次 → 拿到的是**阶段详情载荷**（`{seq,label,phase,attempts…}`）而不是信封，`!res.ok` 成立 → 报错。**定位技巧（本次的关键）**：光看「未知错误」无法分辨「信封缺 value」还是「根本不是信封」；在 `!ok` 分支把**原始 res 回显**（`JSON.stringify(res)` 兜底）后，用户贴回的"错误详情"直接就是载荷 → 一眼认出是二次解包。**修复**：三处改用适配器返回值；`unwrap` 增「`ok=true` 但无 `value`」显式报错 + `console.warn` 原始信封（不再静默返回 undefined 让调用方误报）；冒烟补回归断言 `panel.tsx 不得出现 unwrap(await api.`（防再次二次解包）。**教训**：适配器层与调用层的解包责任必须**单一归属**——要么适配器返回信封、要么返回载荷，混用必然出错；另：错误信息要带**原始返回物**（回显 > 猜因），否则一个「未知错误」能烧掉整轮排查。

- 2026-09-11（补 7）：**右栏 run tab 卡在「读取 run 详情中…」——prop 名踩坑**（用户真机反馈）。**根因**：宿主渲染器把 slot 声明里的 `hooks: { tabInfo }` 绑定成名为 **`useTabInfo`** 的 prop（约定 `hooks.<name>` → `use<Name>`，见官方 `ui-sidebar-documentpreview/TextPreview.tsx:78` 的 `useTabInfo` 解构）；我写的是 `props.tabInfo` → 恒 `undefined` → 取不到 `tab.navigation.address`，而我当时的错误分支只在「地址存在但解析失败」时写 err，**地址为 null 时 err 也是 null** → 永远渲染 loading 文案（既不报错也不重试）。**修复**：① 读 `props.useTabInfo`（保留 `props.tabInfo` 兜底），地址取 `tab.navigation.address || tab.contentId`；② 失败态**分型可见**——`useTabInfo` 缺失 / 地址未就绪（tab 刚挂载、宿主尚未提交记录）/ 地址格式不认识 / remote 不可用，各给不同文案 + 「重试」按钮；③ 地址未就绪时按 150ms × 12 次**主动重渲染自愈**（钩子在记录未提交时会抛错且可能不订阅，不能只等 store 推送）；④ 冒烟补断言（必须用 `useTabInfo`）。**教训**：`hooks` 声明的键名会被渲染器**改名**成 `use*` 才是组件 prop——不要照抄声明里的键名；以及「加载中」必须有出口（每个 early-return 都要能变成可见错误或重试）。

- 2026-09-11（补 6）：**真机实测修正——右侧栏的会话内容宿主只在「对话被选中」时渲染**（用户实测 `tf-mtvrsakj-l2vj5u`：全局面板点「右栏打开」→ `sidebarRight: no session surface is mounted`）。**根因**（补 5 里这条判断错了一半）：右栏这一**列**确实是 AppFrame 级 root slot（`ui-layout/AppFrame.tsx:227` 与 `main` 并列），但它的**会话内容被显式门控**——`ui-sidebar-right/src/client/shell/RightbarRoot.tsx:13`：`const visible = usePanelInfo(info => info.activePanelId === null); if (!visible) return null`，源码注释原文即「Render the Session-bound Sidebar only while the Conversation is selected... or no content for a global panel」。全局面板占 `main`（`activePanelId === 'teamflow'`）→ `rightbar.session` seat 不挂载 → `bindService` 从未发布 → `SidebarRightController.require()` 抛 `no session surface is mounted`（binding === undefined）。**修复**：全局面板开右栏改为**先切回对话再小步重试**——`layout.selectPanel(null)`（`AppFrame.MainPanel` 以 `entryKey: panelId ?? 'conversation'` 派发，null 即回对话）→ seat 在切换后的 `useEffect` 里才 `bindService`，故按 120ms × 最多 12 次重试；`openResourceSafe(address,label,quiet)` 增 quiet 参数（重试期间不刷 console）；彻底失败才降级内联，并在面板里显示**可见提示条**（不再只 console.warn）。UI 语义同步校正：run 行主点击 = 面板内联详情，次按钮文案 = 「**对话右栏**」（切回对话并开右栏，与产物并排）；内联详情头部也加了同一入口。冒烟补断言（panel 含 `selectPanel(null)` + 重试链）。**教训**：验证「列存在」≠「内容存在」——root slot 的声明位置与 scope/门控是两件事，要读**门控组件**（`RightbarRoot`）才算核实到位。

- 2026-09-11（补 5）：**全局面板 + 右栏 run 详情 tab（v0.1.8 ① 第一版）**——用户选定「产品线视角 + 产品线/backlog/run 列表」（bounded 路径，未写 spec）。**四条宿主约束在动手前核实，直接决定了设计形状**：① `sidebar.panellist` 与 `main` **必须成对注册**——侧边栏点击走 `ctx.layout.selectPanel(id)`，而 `layout.selectPanel` 对未注册的 main key **直接抛错**；回到对话用 `selectPanel(null)`（`ui-layout/AppFrame.tsx:42` 以 `entryKey: panelId ?? 'conversation'` 派发）。② ~~**`rightbar` 是 AppFrame 级 root slot**（`ui-layout/AppFrame.tsx:227` 与 `main` 并列渲染）→ 全局面板占着中央区时右侧栏**依然挂载**，从全局面板开右栏可行~~（**此判断有误，见补 6**：`rightbar` 列是 AppFrame 级的，但 `rightbar.session` 会话内容由 `RightbarRoot` 门控 `activePanelId === null`——全局面板下不存在，必须切回对话才能开右栏；唯一失败态仍是 `require()` 的 `no session surface is mounted`）。③ 右栏 tab 是**两段式**：类型进 `ctx.sidebarRightTabs.register({id,kind,patterns,title})`（含 `:` 的 glob 匹配整址），正文进 keyed seat `sidebar.right.pane.tab` 的 `key = definition.id`；body 经 seat 的 `tabInfo` 钩子读 `tab.navigation.address`。④ `main`/`sidebar.panellist` 是 **root scope**，标准 props 只有 `useResource/useWorkspaces/usePanelInfo/useSessions/useSessionPendingInteraction`——**没有 `useProjection`/`useSession`**，且现有 remote 面**全部按 sessionId 寻址**（host 用会话反查产品线）→ 数据面必须 session-free。**落地**：(a) host 抽出 `core/products.ts`（`runsFor/runAddress/productKeyOf/runVisibleIn/runUsageSum/runBrief/productMetaOf/listProducts`，原 `runsFor` 从 index.ts 迁出共用），新增 **5 个按产品线寻址的 remote 方法**（`products(sessionId?)` 产品线清单 + 当前会话所属；`productView(product)` 元信息+backlog+run 列表；`productRunDetail/productStageDetail/productItemDetail`）——实现方式是给现有 `snapshot/stageDetail/itemDetail/backlog` 加**可选 `productOverride`** 复用同一实现（零重复装配、会话路径行为不变：守卫抽成 `runVisibleIn` 语义等价）；`snapshotOf` 增设 `address`（host 生成 `dsh-resource://teamflow/run/<产品线>/<runId>`，**与产物地址同一条原则**：client 不拼地址、不引宿主包）。(b) client 拆三层：`client/shared.tsx`（主题 token/状态词表/格式化从 1286 行的 index.tsx 抽出，会话内与全局共用）、`client/panel.tsx`（`GlobalPanel` = 产品线 rail + run 列表 + backlog 分组 + 内联详情；`TeamflowPanelIcon` 吃 owner props `{size,active}`；`RunDetailTab`/`parseRunAddress`/`runTabDefinition`/`productApi`）、`index.tsx` 只留注册与既有视图；`openResourceSafe` 改为**返回布尔**（全局面板据此判定是否降级内联），会话内工作台补「⇥ 右栏打开 run」入口。(c) manifest：`dsh.client.inject` 补 3 个 slot owner 包（`ui-layout`/`ui-sidebar`/`ui-sidebar-right`）+ 对应 optional peers——沿 `ui-sidebar-files` 的「注册进谁的 slot 就列谁」惯例。**测试**：新增 `test/product-scope.test.js`（33 断言：地址编码、产品线 key 白名单防穿越、过滤/排序、摘要形状含 usage 汇总、`$DSH_HOME` 扫描含两个空态），smoke 新增 2b 断言组（12 条：成对注册/id 一致/地址 pattern/降级布尔）+ 描述符 17→22 + host-pool 增 `products.ts`；全套 **10** 个测试文件通过、typecheck 干净。**已知取舍（→ docs/TODO.md）**：全局面板**只读**（未做 backlog 流转写路径）；两处渲染组件仍分叉（`shared.tsx` 只统一了词表与格式化，未收敛 `TeamFlowView/PipelinePanel` 与面板的 `RunList/BacklogGroups`）；run tab 未注册 `sidebar.right.pane.tab.title` seat（chip 标题取 `definition.title`）；点图标会换走对话中央区（面板内提供「回到对话」按钮缓解）。

- 2026-09-11（补 4）：**reasoningEffort A/B 扩样到 n=3/臂——修正（补 3）的 n=1 结论**——同需求、同档位（patch）、只比「单点确认」阶段；臂归属用日志证死（low 臂有 `推理强度：low（机械阶段降档）`，high 臂 0 条）。逐样本（in / cacheRead / out / calls / 耗时）：**high** H1 25,702/158,720/12,649/6/68.7s、H5 18,802/255,744/8,260/11/179.5s、H6 14,643/143,872/5,412/7/40.9s → 均值 **19,716 / 186,112 / 8,774 / 8.0 / 96.4s**；**low** L2 21,647/78,720/3,421/5/24.6s、L3 8,751/108,800/2,791/6/22.8s、L4 19,959/308,096/8,930/13/81.4s → 均值 **16,786 / 165,205 / 5,047 / 8.0 / 42.9s**。**均值方向一致**：output −42.5%、每次调用 output −49.5%（1,211→612）、cacheRead −11.2%、input −14.9%、**计费总量仅 −12.8%**（214,601→187,038）、耗时 −55%。**但区间大量重叠**（high output 5,412–12,649 vs low 2,791–8,930；low 的 L4 因自己探索了 13 次调用反而比 high 的 H6 更贵）——n=3 下这些差异**统计上不可分**（output：high sd≈3,640 / low sd≈3,390，差 3,727，t≈1.3）。**结论修正**：① 链路正确性已证（降档在预期的臂、预期的时间点生效，零失败零重试）；② **本任务规模下 run 间方差（模型自选探索深度，调用数 5–13）压过 effort 效应，无法据此宣称具体节约百分比**；③ 决策：降档**保留**（机制明确——推理 token 计入 output 且每回合回传；无质量退化、无成本倒退），但**文档/发布说明不写百分比**；要拿可信数字需要更大任务样本或固定调用数的评测场。方法学教训：n=1 的 A/B 在本项目这种高方差任务上**没有结论价值**（（补 3）的 −47% 即被扩样推翻）。

- 2026-09-11（补 3）：**reasoningEffort A/B 结果（修复后重跑）**——同需求（新建 calc 模块 + 断言测试）两次 patch 档，只比「单点确认」阶段（唯一降档阶段）：**基线**（默认 high）25,702 / 158,720 / 0 / 12,649 · 6 calls · 68.7s；**候选**（low；journal 已有 `工程师 · 单点确认 推理强度：low（机械阶段降档）`，证明下发成功）21,647 / 78,720 / 0 / 3,421 · 5 calls · 24.6s。billed 合计 197,071 → 103,788（**−47.3%**），其中 output **−73.0%**、cacheRead −50.4%、input −15.8%、耗时 −64%，缓存命中率 86.1%→78.4%（分母缩小所致）。**质量**：两次确认单都完整（档位判定 + 实测现状核对含 `Test-Path` 证据 + 文件清单 + 范围声明），候选略短但不缺项；dev 阶段零失败零重试，交付物 `node scratch/effort-ab/calc.test.mjs` exit 0。**口径诚实**：n=1/臂且 calls 6→5，样本差异与 effort 混在一起——output 的 −73% 与「推理 token 计入 output、推理内容每回合回传」的机制方向一致，但不能据此宣称精确收益；要收紧数字需再跑若干轮取均值。**结论**：保留 patch 单点确认 + scaffold 的降档；`dev` 暂不降（判据型阶段，且候选轮 cacheRead 302k 是最大头，需更多证据）。冒烟产物已清理（reset 掉子代理的收口提交 + 删任务夹/日志），分支回到 `677142d`。

- 2026-09-11（补 2）：**reasoningEffort 能力探测静默失效（A/B 首轮被证伪）**——首轮 A/B：基线（旧代码/默认 high）patch 第 1 阶段 25,702 / 158,720 / 12,649 · 6 calls；候选（新代码）8,869 / 69,376 / 3,808 · 4 calls，看着降 58%。**但 journal 里没有降档日志** → 复核（落盘 journal 逐条日志 + 全文匹配）确认候选**根本没下发 effort**，那个降幅与 effort 无关（主要是 calls 6→4 的样本差异）。root cause：宿主 `LlmModelReasoningInfo.efforts` 是**对象数组** `{id,name,description}`（`packages/llm/llm/src/types.ts`），实现却按 `typeof e === 'string'` 过滤 → 恒为空数组 → 永远判「路由不支持」→ **静默不下发**（无日志无报错）。修复：按 `e.id` 取值（兼容字符串形态）；**并把这类静默失败可见化**——探测失败/档位不支持时写 warn（`推理强度未降档：<原因>——保持宿主默认`），成功下发时写 info。教训：与 `currentModelSupportsVision` 同族的「能力探测」必须留痕，否则 A/B 会把噪声当结论（本次差点）。smoke 增 2 条断言（对象数组取 id + 未降档 warn）。typecheck + 全套 9 文件测试通过。

- 2026-09-11（补）：**机械阶段推理强度降档（reasoningEffort，优化第二梯队 C）**——事实前提（均已核实）：DeepSeek 路由**默认 `reasoningEffort: high`**（`llm-deepseek/README`），推理 token **计入 output**（官方 `TokenUsageProjection` 注释原文），且**推理内容每个带推理回合原样回传**（README 原文）→ 高推理同时抬高 output 与后续每一步 input。落地：`constants.MECHANICAL_STAGE_EFFORT='low'`，只在**两处机械阶段**下发——patch 档的「单点确认」（`pipeline.ts` 按 `options.mode === 'patch'` 判；注意 patch 档第一阶段沿用 phase 'prd'，所以靠调用点传 hint，而非按 phase 建表）+ `scaffold`（脚手架落地）；判据类阶段不传 = 宿主默认 high。**重试回升 high**（`resolveStageEffort`：attempt>1 → 'high'），与 ADR-0006 质量优先一致。**安全前提**：宿主对不支持的值 `UNSUPPORTED_REASONING_EFFORT` **硬失败且不降级** → 先 `llm.resolveModelInfo(provider, model)` 探测 `reasoning.efforts`，只有声明支持才下发；provider/model 任一缺失即不探测（避免拿到「provider 默认模型」的能力而误判），结果按 `provider/model` 缓存。参数链：`withRetry(..., effortHint)` → `runAgent(..., attempt, effortHint)` → `agentOptions.reasoningEffort`；下发时写 info 日志（阶段详情轨迹可见）。smoke 增 3s 断言组。typecheck + 全套 9 文件测试通过。

- 2026-09-11：**产物可见性（工作台一键预览 + present 交付卡，优化第二梯队 A/B）**——按用户确认的顺序做「产物可见」这一档，落地时**顺序对调**（原计划 present 在前）：核实发现 `present` 的交付卡渲染在**该子代理会话**的 turn tail（宿主 `ui-deliverables` 挂 `conversation.chat.turnTail`，按会话渲染），**主会话看不到** → 工作台内的预览按钮才是主路径，故先做 B。① **工作台产物一键预览**：`itemDetail` 新增 `artifacts: [{name, address}]`——host 用 `readdirSync` 只列**任务夹里真实存在**的产物（顺序见 `constants.TEAMFLOW_ARTIFACT_ORDER`），地址用官方 `fileAddressFor(sessionId, undefined, runDocs/name)` 生成 `dsh-resource://file/session/<id>/<相对路径>`。**关键取舍**：地址在 host 侧生成——client bundle 的 tsdown 配置只 external `react`（其余一律内联），在 client 引 `@deepseek-ai/dsh-util-workspace-path` 会被打包进 bundle；host 侧 `@deepseek-ai/*` 是 external（随 profile node_modules 解析），所以官方 helper 放 host、client 只把地址交给右侧栏。客户端 `openArtifact` 走 `ctx.get('sidebarRight')`（**不进 inject**：右侧栏是增强路径，缺它不该让工作台激活失败），服务缺失/地址无人认领时静默降级并 warn。新增可选 peerDep `@deepseek-ai/dsh-util-workspace-path`。② **present 交付**：新增共享 prompt 块 `ARTIFACT_DELIVERY(runDocs)`，接入 prd/tech/qa/acceptance（写文件后、state 块前调 `present({files:[{path,description}]})`），标注 `[policy]` 并诚实说明「文件仍是唯一事实源、缺文件仍是硬失败、卡片只在子代理会话可见」；L1 增契约 `ARTIFACT-PRESENT-CHANNEL`（含 exclude：禁称 mandatory、禁自称 hard constraint）。测试：smoke 增 3r 断言组（host 出地址 + 只列存在 + client 交右侧栏 + peerDep 声明 + 4 处 prompt 接入）。typecheck + 全套 9 文件测试通过（L1 全绿、L2 10/10）。

- 2026-09-10（补）：**护栏宿主适配 + 死注入清理 + 工作区 key 文档纠偏（v0.1.7 第一梯队）**——在当日 rc.2 兼容核对之后，按「优化评估」结论落地三件事（四路调研结论见 `docs/TODO.md`「优化候选」）：① **护栏轻提醒改官方通道** `run.localAgent.inject(createUserMessage(...))`——宿主 next-step 队列由 agent loop 在 `preStep` 内、tool/result 之后整批认领，旧注释声称的「`subagents.start` 句柄无 inject、插 tool_calls→tool_result 会 400」不成立（`packages/core/agent/src/runtime-types.ts` 的 `inject/steer/followup`，`run.localAgent` 是活 Agent）；手写 `__teamflowPending` 队列 + step/end flush 时序状态机整体删除。载荷 `form:'notice'` 必须带 `summary`（宿主 `ContextFormed` 判别式要求，typecheck 实测拦截）。**中止语义未动**（退化仍 dispose，未改 steer 纠偏——那是独立课题）。② **挂死检测改官方 `subagentTiming` 投影**（`active.through` = 该投影 cut 最新事件时间，宿主在已提交事件上折叠）替代「events/snapshot/own 三源取最长视图」长度启发式（r1 QA 误杀根因）；投影不可用才回退旧启发式，两条路径诊断文案区分（正常路径不再读旧事件源）。**保留 agent 活动守卫**：长工具静默执行（跑十几分钟测试无输出）属合法场景，不能只看静默。③ **死注入清理**：`static inject` 长期硬注入 `tokenMeter` 却全仓未用（只 setRuntime 存着）→ 从 static inject/setRuntime/runtime 三处移除。④ **工作区 key 文档纠偏**：`workspaceScopeOf` 的 UUID 分支**当前不可达**（宿主 `resolveByPath` 是 `async`，我们同步调用 → `ws.id` 恒 undefined），实际 key 是 `slugPath(cwd)` 路径派生；改注释与 AGENTS §5 说实话，真修（await + 存储 key 迁移，破坏性）入 TODO 待 v0.1.8。**否决**：`subagent.toolFilter` 宿主强制工具裁剪——`tools.restrict()` 对**未知工具名 fail-loud 抛错**，而子代理工具集随 preset/深度变化，跨 profile 会让 start 直接失败（`maxDepth` 同理只是抛错不传播），「QA 禁改产品代码」这类按名字裁剪无法安全表达。测试：smoke 增 3q 断言组（官方 inject 通道 + pending 队列已删 + subagentTiming 来源 + 诊断双路径 + tokenMeter 已清）。typecheck + 全套 9 文件测试通过。

- 2026-09-10：**dsh 0.1.5-rc.2 兼容核对 + 计量改走官方 Session 投影（弃用同步事件读取器）**——版本线：本机 profile 与 harness 仓库均升到 rc.2（alpha.2→rc.1→rc.2 一路核对；rc.2 只有 4 个提交，纯 Web 反馈弹窗/交付文件卡片两条体验优化）。**兼容核对**：① 插件面板 Slot——原 `conversation` 根 slot（ui-layout）迁为 `main` 下的 `conversation` key，另新增 `sidebar.panellist`/`rightbar.session`；插件的 `conversation.view`/`conversation.input.right` 声明未变（slot 树 rc.1→rc.2 零增删 56=56）；② 会话格式 V3 + Session 生命周期（`SessionHandle`、异步 `agentLoop.create()`、会话锁）——插件不读盘、不自建 session；③ `ctx.agent` 移除 / Inbox 类型化——插件走 `exec.agent`；④ 默认工具调整（SDK/Headless/ACP，minimal 仅持久 shell）——web profile + 团队预设不受影响；⑤ subprocess handle 去 pid——插件只用 `execFileSync('git')`；⑥ 直接 import 面仅 `dsh-typert-protocol`/`dsh-llm`/`dsh-tools`，rc.2 类型下 typecheck 通过。**唯一需跟进**：宿主 2026-09-09 起把 `Session.eventAt()`/`snapshotEvents()`/`ownEvents()` 标记 deprecated（Agent Note `2026-09-09-deprecate-synchronous-session-event-reads`：存储方向是不再常驻完整事件序列；存量可留、**新调用禁止**，宿主自身生产源码仍有 71 处带 line-scoped waiver 的同类调用）。**落地**：计量来源改为**官方投影优先**——`ctx.sessionProjections.stateOf(session,'tokenUsage')` 取四桶（dsh-token-meter 注册，stateVersion 2；与官方同一份 fold：`assistant/attempt` 内嵌 stream usage 计入、`llm/retry-started` 先关掉被替换的重试槽位，比旧扫描「只认 assistant/message」更准）+ `'sessionStats'.steps`（dsh-session-stats 注册）取调用数（一个 step = 一次模型请求，与旧口径 turn.step 去重等价）；投影缺失（最小 profile）/四桶全 0/`stateOf` 抛错 → **静默回退**原事件扫描（不虚报 0、不中断流水线）。`sessionProjections` 走**可选 `ctx.inject`**，不进 `static inject`——服务缺失时插件照常加载（static inject 缺服务会让插件直接不加载）。**取舍**：`guard.eventsOf` 本次不迁移——它要近期事件**内容**做复读/挂死判定，官方投影不提供该视图；替代方案（改判据为 `deriveMessages()`+增量处理送达事件 vs 自注册投影维护近期事件环形状态）需先定等价判据（判据漂移直接影响误杀率），已入 docs/TODO.md。**声明**：`package.json` 补 `dsh.manifestVersion: 1` + `engines.dsh: ">=0.1.5-rc.2 <0.2.0"`（宿主当前只在 `packages/util/package-manifest/src/types.ts` 声明类型、**不读取校验**，属作者声明性元数据；预发布匹配仍受 semver 同 tuple 规则约束）；README 版本锚点 0.1.3-alpha.1→0.1.5-rc.2（npm next=0.1.5-rc.2 / latest=0.1.5-rc.1，勿以 latest 判断）并修掉锚点段前遗留的 `opencode` 串字。测试：metering.test.js 增 5 场景（投影优先且事件扫描数字被忽略 / 缺 sessionStats 兜底 calls=1 / 四桶全 0 回退 / stateOf 抛错回退 / 未挂载回退）、smoke 增 3p 断言组（投影路径 + 可选 inject + static inject 不扩 + manifest 字段）。typecheck + 全套 9 文件测试通过。

- 2026-09-07：**token 计量宿主适配修复（r38 usage 全空——发布前用户实测发现）**——用户反馈：backlog 卡片 task-38 无「TOKEN · 官方口径」（task-37 之前都有）、流水线图卡详情 token 显示无 usage 明细。实锤：**宿主升级（session v2）后 Session 已无 `events` 属性/getter**（仅私有 eventsSnapshot 缓存 + 官方 `snapshotEvents()`/`ownEvents()` 方法），`metering.accumulateSessionUsage` 读 `session.events` = undefined → 返回 null → stage usage 全 null → 任务卡/图卡无 token（task-37 是旧宿主跑的正常，task-38 新宿主全空，时间线吻合）。修复：① **多源回退** `sessionEventsOf`（events 老快照 → `snapshotEvents()` 官方完整日志 → `ownEvents()` fork 后本 agent 事件，取含 usage 事件最多源——guard.eventsOf 同款语义，guard 早已适配、metering 失明未跟上）；② **usage 双路径** `usageOfEvent`（`data.usage` 优先，缺失时扫 `data.stream` 的 usage chunk——宿主 usageOf 同款，v2 事件 usage 可能只在 stream）。新增 `test/metering.test.js`（5 场景冻结：v2 snapshotEvents+data.usage / v2 usage 藏 stream / fork ownEvents / 老宿主 events 数组 / 边界 null）。typecheck/bundle/全套测试通过（9/9）。r38 数据未落盘无法追溯（接受缺失），部署+重启后新 run 生效。
- 2026-09-07：**迁移脚本 taskKey 假阳性修复（发布前验证发现）**——发布 v0.1.6 前对存量数据 dry-run 发现：`scripts/migrate-phase-en.mjs` 的 taskKey 清洗条件 `endsWith('）')/endsWith('（')` 把**合法中文括号结尾**的 taskKey 误判为历史残留（实锤新 run r38 3 处假阳性：`game.js ghostY rot 归一加固（单点防御）` 被标记待迁移）。影响：幂等无损（副本实跑重提取值不变）但 dry-run 计数虚高（36 处→实际真缺口仅 2 处）+ 无谓重写文件。修复：判定从「形态特征（以括号结尾即残留）」改为「**现值 ≠ 从 label 提取的干净 title 才重写**」——已正确/合法括号结尾不误判，真缺口（空 taskKey、`（第 N 次重试）`/`（补跑）` 残留）仍正确剥除归并（副本四样例验证：正确值不动/残留剥除/空值补写/重试归并）。backlog 子卡条件同步。真实验证：修复后全量 dry-run = 1 文件 2 处真缺口（json-parse 旧 run seq38/39 空 taskKey），r38 假阳性消失。typecheck/全套测试通过。
- 2026-09-07：**评测层 L1+L2 落地（prompt/注入改动收益评测，零 LLM 成本确定性门禁）**——痛点：每次 prompt/注入改动无法评估收益（既有 A/B 基准手工单次、文档自述「严格结论需多轮取均值」）。按行业「确定性优先 + golden corpus 回归门禁」范式落地两层：**L1 `test/prompt-contract.test.js`（行为级契约）**——直接调用 prompt 工厂（纯函数）产真实指令文本，断言 29 条契约锚点（验收结论字面量模板/结论行最后一行/单轨摘要/HOST-ENFORCED 分级/缺陷表格式/证据块/state 块/蓝图 JSON/git 纪律等），每条带 `{id, level(HOST-ENFORCED|policy|structural), intent}`——改 prompt 后一眼看出断了哪条、丢哪级保障（对比 smoke 散落 20+ 条无语义源码子串断言）；**L2 `docs/benchmarks/corpus/` + `test/conformance.test.js`（回放语料一致性）**——冻结 10 份真实形状产物（QA-REPORT 含 **P1** 加粗/BUG-P1-1 打回形态、ACCEPTANCE 四档结论 + tf-msytlok5 误判陷阱 + 无结论行漏报、dev 回复证据块含/缺、畸形蓝图 JSON 抢救 tf-mt85o5jj），manifest.json 声明 语料→真实解析器→期望值（parseDefects/parseAcceptanceVerdict/extractVerificationEvidence/extractBlueprint，纯 Node import 链已核安全）；语料文件只增不改（防评测过拟合：新增样本=扩回归面，不留存改历史样本）。接入 `pnpm test`/`prepublishOnly`；AGENTS §2/§4 同步。typecheck/全套测试通过（8/8 测试文件全绿）。设计文档：本次对话（L3 E2E 成对跑按用户决策不做常跑）。

- 2026-09-07：**护栏挂死误杀修复（eventsOf 多源回退 + agent 活动守卫）**——json-parse r1 三次 QA 全 10m15s stalled（#25/#35/#36）——用户质疑「QA 每次都是 10m15s 失败」。实锤：QA 子代理**全程正常干活**（#36：254 事件、43 step、工具调用直到被 dispose 前 47 秒），护栏却报「10 分钟无任何新事件」——**事件视图失明**：guard `eventsOf` 读 `run.localAgent.session.events`（宿主 Session 缓存快照 getter），对 QA 子代理拿到**不增长的视图**——10 分钟零增长误判挂死。r33/r37 的 QA done 是 <10 分钟侥幸（r37 10m 整为边缘）。落地：① **eventsOf 多源回退**：events（快照）→ `snapshotEvents()`（宿主官方 API）→ `ownEvents()`（fork 后）——取信息最多（最长）视图；② **挂死守卫 `isAgentBusy`**：事件零增长但 agent 仍非 idle（phase.kind≠'idle'）且本会话已动手（lastMutationAt>0）→ 视为视图失明而非挂死——记一次诊断 warn + 重置观察窗口，不中止（纯启动静默挂死不受影响）；③ **stalled 触发前诊断**：记录 events/snap/own 各源视图长度（journal warn「挂死诊断：…」）——若回退仍失明则诊断暴露真相。smoke 3o 增补 3 条。typecheck/bundle/全套测试通过。
- 2026-09-06：**代码级英文化改造（业务键英文，中文只留 label 展示——为 UI i18n 铺路）**——用户决策：代码判断/命名全英文，中文仅展示层（label/日志/UI 文案），后续与 dsh 中英对齐。落地：① **PHASE_ORDER 英文化**（prd/design/scaffold/tech/dev/qa/acceptance）+ `phaseKeyOf` 归一（中文存量兼容防御）；全部 `=== '开发'` 等判断改英文 key（pipeline 40 处 + client 4 处 + store 类型）；② **stage.taskKey 结构化**：withRetry/runAgent 携带任务键（dev 分支传任务 title），聚合/attempts/补跑判定按 taskKey（label 仅旧数据兜底）——**移除 label 中文正则依赖**；③ **backlog 子卡加 taskKey**（匹配走 taskKey，title 保留中文展示）；④ client PHASE_ICON/PHASE_NAME 英文键映射 + 展示名统一走映射；⑤ **存量迁移脚本** `scripts/migrate-phase-en.mjs`（journal stage.phase 中文→英文 315 处 + taskKey 补写 144+40 处 + backlog 子卡）——含坏正则残留清洗（`（第 N 次重试）`/`（补跑）` 交替组 `）` 残留修复）；⑥ 修复历史正则 bug：`（第 \d+ 次重试|补跑）$` 的 `）` 在交替组内导致聚合 key 多出 `）`/`（`——统一为 `（(?:第 \d+ 次重试|补跑)）$`。smoke 3o 断言组（PHASE_ORDER 英文/phaseKeyOf/taskKey 序列化/聚合/迁移脚本）。typecheck/bundle/全套测试通过。
- 2026-09-06：**resume 状态机化（两级状态 + 子图聚合 + backlog 解耦）**——架构级重构（用户驱动的设计评审：resume 应该像状态机，阶段单向流转不倒查）。落地：① **任务级聚合 `devTaskStatuses`**（journal 驱动）：label 去「开发 ·」前缀与（第 N 次重试/补跑）后缀分组——**有 done stage = 任务成功**（历史失败尝试不算失败）；② **resume 开发分支统一补跑**：resume 时开发 = 复用已完成产物 + 仅补跑「聚合后未成功」任务，全完成 → logSkip——**不再读 backlog 子卡**（json-parse r1 实锤根治：残留失败卡污染判定）；③ **interruptedPhaseOf 任务级聚合**：任务全 done = 阶段完成——dev 部分成功（有 done 有未完成）时 resume 起点回开发补跑未完成，dev 全 done 时起点正确落在 QA（不倒查）；④ **createSubtask 同名复用**：子卡 = 业务任务实体（同名一张卡，状态流转 + retries 计数），执行历史在 journal stages——不再因重试/补跑新建卡膨胀看板；⑤ **host stageDetail 返回 attempts 聚合**（同任务 stages 按 seq + 每次的 summary/output/usage/childId/耗时）；⑥ **client 流水线图子图卡聚合**：同任务多次尝试合成一张卡 + `↻N` 重试角标（>1 显示）+ 最新状态；⑦ **详情弹窗尝试时间线**：多次尝试 → 时间线（#seq/状态/耗时/token/摘要）+ 选中展开该次详情（默认最新）；**单次尝试 → 保持现状零回归**。smoke 4 条新断言（devTaskStatuses/补跑统一/起点聚合/子卡复用）。typecheck/bundle/全套测试通过。
- 2026-09-06：**resume 重复补跑已成功任务（done 同名排除修复）**——json-parse r1 实测：16:04 resume 从「QA 测试」继续，但 UI 又出现「开发」阶段（3 任务开发中）——重复触发开发。根因链：① `resumed('开发')` = 开发序在 resume 起点前（QA）→ true（即使开发已全部 done）；② 开发补跑分支 `failedSubs` 取**全部历史失败子卡**——15:44 resume（resume.phase='开发'，resumed('开发')=false 走正常路径）新建 done 卡 dev-10~12，**旧失败卡 dev-1~9 从未清理**；③ 16:04 补跑按 failed 卡 rerun → 重复补跑已成功任务。修复：failedSubs 过滤时**排除已有 done 同名子卡**（同名已成功 = 已交付，旧失败卡是历史残留）。smoke 3n 增补 1 条。typecheck/bundle/全套测试通过。
- 2026-09-06：**团队上下文注入经 createUserMessage（宿主 v2 校验破坏修复）**——用户重启后报「历史加载失败：session …failed validation: session event at seq 10 lacks an identified message（gateway/internal）」。定位：`tryFlushPendingInjections`/`selectTeam` 的 `agent.inject` 传裸 payload `{type, content, source}`（缺 id/role）→ 宿主透传落盘 user/message 事件缺 `id` → 宿主 `assertMessageEventShape`（core/session/src/index.ts:339-352）加载校验拒绝。宿主标准做法是 `createUserMessage({content, source})`（llm/src/message.ts:161 自动补 id/role）。修复：两处 inject 改用 createUserMessage + source.form 按宿主新枚举 `'instructions'`（旧值 'context' 不在 ContextForm 词表，TS 编译即拦截）。**存量修复**：脚本扫描全部 451 个 session.v2/v1 文件，给 28 个缺 id/role/source 的 user/message 事件补字段（source 标 `teamflow-fix` 诚实标记）——0 失败。typecheck/bundle/全套测试通过。
- 2026-09-06：**SAFE_SIGNAL 补 throwIfAborted（dsh 升级兼容性破坏修复）**——json-parse 工作区 r1（json 树图优化）实测：3 个 dev 任务 3 轮 resume 全部「启动/执行失败：options?.signal?.throwIfAborted is not a function」——**启动即炸（与任务/模型无关），resume 也救不了**。根因：宿主（09-04+ master）全仓硬依赖 `signal.throwIfAborted()`（agent-loop/subagent/e2b 等大量调用），TeamFlow 的 `SAFE_SIGNAL`（伪 AbortSignal 兜底：aborted/addEventListener/removeEventListener）缺该方法 → subagents.start 内部即抛。修复：SAFE_SIGNAL 补 `throwIfAborted: () => {}`（恒 false 永不 abort，空实现语义正确）+ normalizeSignal 判定加 `typeof s.throwIfAborted === 'function'`（伪 signal 一律降级，不再原样透传）。smoke 3n 增补 1 条。typecheck/bundle/全套测试通过。
- 2026-09-04：**resume 补跑附失败诊断（跨 run 盲试缺口修复）**——审计发现：`buildRetryDiagnostic` 只在 withRetry 自动重试（同 run attempt 2）拼接；**resume 补跑是全新子代理会话 + 原样 devPrompt——无失败诊断 = 盲试**（与 3j 修复前同构的问题）。r37 实证：两次失败都卡 PowerShell 脚本语法（#6 output「PowerShell parse error」），resume 后模型不知道上次卡点，会重复踩同一坑。落地：resume 补跑 mapper 按 title 匹配该任务上次失败 stage（`phase==='开发' && status!=='done' && label.includes(title)`），附 `buildRetryDiagnostic(2, prevStage)`（outcome/summary/产出尾部 1k）；无匹配时维持原样。smoke 3n 增补 1 条。typecheck/bundle/全套测试通过。
- 2026-09-04：**dsh 0.1.3-alpha.1 兼容性核对（三线调研：session v2 / file-upload+Web / Windows+子代理）**——结论：**零破坏，一处陈旧引用修复**。① session 持久化 v2（write-lease/JSONL/版本化导出）：`session.append` 签名未变，插件的 `tool-workflow/agent-start` 与 `user/message`(source.kind=plugin) 均在 released 词表内（v0/v1/v2），session.id/cwd 内存句柄不受影响；长期约束：未来新增自定义事件类型须带 `ignorable: true`、已知类型载荷不加 released 词表外键。② file-upload/attachment 统一 prompt 收口（ea669428be，master 非 0.1.3-alpha.1）+ Web 三件套（clickable links/skill chips/fuzzy ranker）：attachment admission 是加宽（纯文本直通），插件三条 prompt 注入通道均不经过；conversation.view/input.right/slots/openSubagent/typert 契约全未变。③ Windows/workspace/subprocess：windowsHide 只改 dsh 自己的封装（插件 execFileSync 已自带）；workspace 全限定路径校验（插件已有 try/catch+slugPath 兜底）；sandbox/tool-calls/SubagentStartRequest 零 diff；`assistant/chunk → assistant/attempt` 事件更名插件未监听。**修复**：`@deepseek-ai/dsh-client-runtime` 已于 08-23 从 monorepo 移除（替代 `dsh-client-modules`），插件 package.json 三处（client.inject/peerDependencies/peerDependenciesMeta）换新包名——旧引用会解析到 npm 远古 0.1.1-rc.2。README 新增「版本锚定」段（开发验证基于 dsh git master 2026-09-04 ≈ 0.1.3-alpha.1；npm next=0.1.2-rc.1；latest 标签滞后勿以 latest 判断）。typecheck/全套测试通过。
- 2026-09-04：**serializeJournal 白名单漏 pick verifyEvidence（r33 实测发现）**——r33（lite，lock move reset）实测：模型 3/3 按 7b 契约写了 `[Verification evidence]` 块（新 prompt 生效 ✓）、host 无缺失 warn（提取成功 ✓）、子卡 usage 按 stage 引用正确累计（并发安全 ✓）——但 journal 里 `verifyEvidence` 全空。排查链：代码/部署/进程加载全对（唯一 profile 副本 0:22:56 同步、进程 0:24:59 重启加载）；模拟实验（lib 提取函数 + 真实数据）提取/写入/序列化全成功——**root cause：`serializeJournal` 的 stage 序列化是字段白名单（pick）**，含 seq/label/phase/status/outcome/childId/startedAt/endedAt/usage/handoff/summary/output，**漏了 verifyEvidence**——内存写入被落盘丢弃（为何无缺失 warn 的自洽解释：提取成功走写入分支，写入被序列化过滤）。修复：store.ts stage pick 补 `verifyEvidence: s.verifyEvidence ? clip(s.verifyEvidence, 8000) : null`；smoke 3n 增补序列化断言防回归。typecheck/bundle/全套测试通过。另 r33 全链路确认生效：QA 单轨制（回复 5 行摘要 + QA-REPORT.md 4363B 含缺陷表）、验收结论行字面量模板（「验收结论：✅ 通过」+ ACCEPTANCE.md）、零失败零重试。
- 2026-09-03：**prompt 中英混排纪律固化（约束句英文/内容中文 + 列表分隔符随内容语言）**——审计评审 5 点（few-shot 取舍✓/productCtx 裁剪✗=与 tech 分层注入同族假优化/同构性✓/重试带次数✓=3j 已实现/混排细节✗）：仅混排一处为真问题——`[TASK TARGET FILES]` 英文标签拼中文逗号（路径为 ASCII 内容，分隔符应随内容语言）；无行为危害，属维护一致性。落地：prompts/index.ts 头部固化混排纪律一行 + 该处改英文逗号。typecheck/全套测试通过。
- 2026-09-03：**子卡 usage 并发累计修复（noteSubtaskUsage 换 stage 引用）**——审计发现：主路径 dev 的 `journal.stages.filter(s => s.phase==='开发').pop()` 取 stage 在并发下错位（同 beforeLen 问题族）。且**「总量 additive 不受影响」不准确**：`applyStageUsage` 是简单相加——错位模式下后完成的子卡反复吸收最后一个 stage（C 的 usage 被多次累计），先完成的子卡零累计——**子卡明细既错位又超计**（流水线级总量走 noteTaskStageUsage 幂等不受影响，展示层子卡数字虚高）。落地：`noteSubtaskUsage(journal, sub.id, devR.stage)` 按 withRetry 返回的 stage 引用直写（删除 filter().pop()），一处改动闭环。smoke 3n 增补 1 条。typecheck/bundle/全套测试通过。
- 2026-09-03：**client 渲染验证证据块（stageDetail RPC → UI 审计闭环）**——审计发现：verifyEvidence 已进 stageDetail RPC 但 `StageDetailDrawer` 未渲染（产出全文里有但藏在 24k 尾部，且「缺失」状态不可见）。落地：阶段详情抽屉新增「🔬 验证证据」块（仅 `phase==='开发'` 渲染：dev/qaFix）——有值展示结构化证据；**缺失置灰提示**「契约未兑现，host 已记警告；可与 logs/ 命令输出对照」——审计完整性（有没有按契约自述）一眼可见。smoke 3n 增补 1 条 client 断言。typecheck/bundle（client+host）/全套测试通过。
- 2026-09-03：**空结论行漏报修复（四档词白名单校验覆盖 accepted 分支）**——审计发现：`## 验收结论：`（结论行存在但无词）accLine 提取后为 `'验收结论：'` **非空**（前缀残留），`!accLine` 的 needs-human 不触发，四档词全不匹配 → 落 accepted——上次漏报护栏只覆盖「无结论行」，前缀残留的空行漏回默认值。且**同族隐藏 bug**：裸「不通过」（无 ❌ 前缀）含「通过」子串，rework/reject 分支都不命中 → 同样漏回 accepted。落地：① accepted 前新增四档词白名单校验 `!/通过|✅|⚠️|❌|📝/ → needs-human`（空/待定/无词结论行不猜结论，与无结论行同语义）；② rework 分支否定词扩展为裸「不通过」（`/不通过|需返工|未通过/`）+ ✅ 通过词优先 + 双重否定保护（`无 不通过/未发现不通过/未出现不通过`=通过，防「未发现不通过项」误杀）；③ verdict.test.js 新增 6 断言（空结论行/待定/裸不通过/未通过/双重否定/正常路径）；smoke 3l 增补 2 条。typecheck/bundle/全套测试通过。
- 2026-09-03：**并发 dev 证据错位修复（withRetry stage 引用并发安全）**——审计发现：`noteVerifyEvidence` 用 `reverse().find` 取「最后一个无证据同 phase stage」，并行 dev 子任务（runPool，完成顺序≠启动顺序）下 A 的证据会挂到 B 的 stage——审计特性被自身削弱；且**同根源 bug 更广**：`withRetry` 自身 `journal.stages[journal.stages.length - 1]` 取 lastStage 在并发下同样错位（A 的 runAgent await 期间 B push 新 stage），影响三处：①证据块错位；②重试诊断包读错 stage（summary/outcome 拿 B 的）；③`stageTokens` 误累计 B 的 usage（可能被 B 的消耗提前熔断）。落地：`withRetry` 调用 runAgent **前**记录 `beforeLen = journal.stages.length`（runAgent 同步 push 本次尝试 stage，在第一个 await 前），返回后 `journal.stages[beforeLen]` 即本次尝试 stage——并发安全（各自索引各自 stage）；withRetry 返回对象携带 `stage` 引用；`noteVerifyEvidence(stage, output)` 按引用直写（删除 find 逻辑）。smoke 3n 增补 2 条（beforeLen 取法 + 返回携带 stage）。typecheck/bundle/全套测试通过。
- 2026-09-03：**dev/qaFix 验证证据块（单方宣称 → 可审计的具体自述）**——审计发现：dev 阶段「run verification to ensure green」依赖模型自觉执行与如实汇报，prompt 无证据结构化要求（「how verified」仅摘要一句话）；guard 只防「重复/复读」，防不了「声称跑了其实没跑 / 跑了但简报全绿」；QA 有独立对抗探针 + QA-REPORT.md 结构化测试结果表兜底，dev 是唯一单方宣称阶段。定性：审计可见性缺口而非正确性缺口（功能质量有 QA 兜底），但「全绿自述 vs QA 缺陷」的信息不一致同构于双轨制问题，且假宣称打回多烧一轮。落地：① `devPrompt`/`qaFixPrompt` 强制回复末尾「验证证据」块（`[Verification evidence]`：命令+退出码+断言计数+失败行引用，或显式 `N/A: <原因>`）——policy 级（缺失记 warn 不中断，防误杀纯配置改动任务）；② `util.extractVerificationEvidence` 提取纯函数（到 state 块前截断）；③ `pipeline.noteVerifyEvidence` 三处接入（dev 主路径/断点补跑/qaFix），存证 `stage.verifyEvidence`（JournalStage 新字段）；④ `stageDetail` 返回 verifyEvidence（审计可见）；⑤ 证据块可与 `logs/teamflow/<runId>/` 命令输出日志交叉核对——**不是 enforcement**（模型仍可伪造），是「可审计的具体自述」：具体细节难编造一致（具体性压力）+ 伪造可发现（审计轨迹）；⑥ 新增 `test/evidence.test.js`（8 断言）+ smoke 3n 断言组。typecheck/bundle/全套测试通过。
- 2026-09-03：**prompt 约束分级（hard 自称与 enforcement 脱节 → HOST-ENFORCED / policy）**——审计发现：5 处约束自称「hard constraint」但真实 enforcement 全部弱于措辞——TOKEN_HYGIENE 第一句即自认「violations only log a warning, never interrupt」（自我拆台）；ONCE_DISCIPLINE 声称「violating it burns tokens」但文档重复 write **零观测**（guard 只观测 read/验证脚本，连 warn 都没有）；Doc/AGENTS/Git 边界为纯 prompt 约束、host 无校验。危害：措辞层面「hard」与后果脱节会训练模型对 high-signal 词脱敏（实证：17 条 warn 零削减，guard 注入闭环后才见效），且污染真硬约束（单轨产物/验收结论行）的信号空间。落地：① **分级标签**——`[HOST-ENFORCED]`（host 有真实校验/解析/硬失败后果：QA/验收单轨回复、缺陷表、验收报告与结论行；标注同时**必须**描述真实后果如「missing file = hard failure (needs-human, pipeline stops)」）+ `[policy]`（无 host 强制、靠自律 + guard warn/轻提醒：TOKEN_HYGIENE/ONCE_DISCIPLINE/Doc/AGENTS/Git 5 处）；② TOKEN_HYGIENE 诚实标注「warn + live reminder only (never interrupts)」，ONCE_DISCIPLINE 改为「repeated write/read cycles pay cache replay fees」（不再声称 violating burns tokens）；③ prompts/index.ts 头部固化分级约定注释（新增 HOST-ENFORCED 约束必须先加 host 代码再标词）；④ **禁止 prompt 内自称 hard constraint**（smoke 3m 负断言防回归）。typecheck/bundle/全套测试通过。
- 2026-09-03：**验收结论契约强度修复（漏报护栏：结论行字面量模板 + 无结论行 → needs-human）**——审计发现反向风险：`parseAcceptanceVerdict` 只认含「验收结论/整体结论」的行，但 prompt 未强制该行字面量格式；模型漏写该行时 accLine='' → 旧实现**默认返回 accepted（最乐观默认值）**——模型写「❌ 不通过」但漏前缀 → 判通过 = 质量门禁漏报=假交付。历史只修过误杀（tf-msytlok5），无结论行的默认值方向反了（验收应宁严勿松：误拦截=人工看一眼，误放行=假交付）。落地：① `acceptancePrompt` 结论行字面量模板——回复 verdict line 固定「验收结论：✅ 通过 ／ ⚠️ 有条件通过 ／ ❌ 不通过 ／ 📝 需求不适用」；ACCEPTANCE.md **最后一行必须为字面量结论行**（缺失=契约违例停线人工）；② `parseAcceptanceVerdict` 无结论行 → 返回 `needs-human`（新返回值；📝 全文命中与架构红词仍优先）；③ `pipeline.ts` 新增 needs-human 分支（对齐 reject 模式：advanceTask needs-human + humanIntervention + persist + throw）；④ verdict.test.js 更新旧「无结论行→保守 accepted」断言 + 新增漏报回归（正文 ❌ 无前缀→needs-human；📝 全文无前缀→reject 仍优先）；smoke 增补 3l 断言组。typecheck/bundle/全套测试通过。
- 2026-09-03：**输出单轨制（文件即产物：QA/验收 host 只读文件，回复仅摘要）**——双轨制实锤：日志「QA 未发现缺陷」与验收「打回 BUG-P1-1」对不上（回复正文与产物文件不一致）。落地：① `qaPrompt` 回复收敛 ≤12 行摘要（结论一句话+文件路径）、验收 ≤10 行（结论+路径），正文全部收口 `QA-REPORT.md`/`ACCEPTANCE.md`——**文件即交付物，host 直接导入**（缺陷表/补测清单/核对表必须在文件里），回复不再重复正文；② `pipeline.ts` 新增 `artifactText(journal, fileName)` 读任务夹产物，QA 后 `qa = artifactText('QA-REPORT.md')`、验收后 `acceptance = artifactText('ACCEPTANCE.md')`，`parseDefects`/`parseAcceptanceVerdict`/qaFixPrompt 注入/验收 QA 注入全部吃文件内容；③ **文件缺失 → 硬失败 needs-human + stageFailError**（不回退解析摘要：QA 回复无缺陷表，回退=「QA 未发现缺陷」静默假交付；验收回复无结论行，回退=保守 accepted 误放行）；④ state 块仍附回复尾部（host 机器元数据不进文件），`mergeStageState` 改用回复文本；⑤ resume 复用 QA 产物**文件优先、journal 兜底**（兼容存量 run）；⑥ stage.output 保持回复文本（重试诊断/详情浮层语义不变）。smoke 增补 3k 断言组（prompt 单轨话术 + artifactText + 硬失败 + resume 文件优先）。tech/PRD 等阶段不在范围（tech 已有蓝图文件 fallback）。
- 2026-09-03：**重试诊断包（盲试 → 带因重试）+ stalled 不再自动重试**——withRetry 对同一 prompt 原样重试=盲试（子代理只知道 label 变「第 N 次重试」，不知道上次为何失败）。落地：① `runAgent` 失败路径落 `stage.output`（截断 4k，重试诊断/详情浮层可查），`insubstantial` 细分——命中 `REFUSAL_PATTERN` 时 `refusalHit` 提取命中短语+原文上下文入 summary（实测最左匹配：`抱歉` 先于 `我无法完成`），否则报「内容过短 N 字符 < 下限 M」；② `withRetry` 重试 prompt 附 `buildRetryDiagnostic`（失败分类/详情/guardReason/产出尾部 1k，闭合标记收尾），诊断源全为现成字段，零新持久化字段；③ **stalled（护栏挂死/空转）不再自动重试**——修 guard 注释与实际行为矛盾（此前注释声称走预算门转人工，实际会重试），与 degenerated 同策略 needs-human 引导 resume；④ 重试日志带 outcome。新增 `test/diagnostic.test.js`（拒绝词命中点 + 诊断块结构，最左匹配语义回归）、smoke 3j 断言组。typecheck/bundle/全套测试通过。
- 2026-08-27：**分支策略基调（ADR-2026-08-27）**——启动前用户决策（needs-decision）：teamflow_start 在 auto 策略下按四情况（main+干净/main+脏/feature+干净/feature+脏）返回决策问题+选项（stash/commit/新建/沿用，自定义兜底），主线程 Agent 询问用户后带 branchPolicy/branchName/preAction/commitMessage 重发；auto=建分支 feat/<slug>（从当前 HEAD 派生）、keep=沿用；preAction 在 sanity 前执行；devPrompt Git discipline 硬约束（子代理禁 checkout main/merge/delete 主干）；验收通过后完成汇报附人工合回命令 + stash pop 提醒（host 不自动合回）。配套：护栏注入通道修复（session.append('user/message')，观测→执行闭环真正生效）。
- 2026-08-25：**任务夹文档制落地（ADR-0008，破坏性）**——按头脑风暴定稿实施：① **结构**：每需求一个自包含任务夹 `docs/teamflow/<yyyyMMdd>-r<N>[-<slug>]/`（PRD/DESIGN/TECHNICAL/QA-REPORT/ACCEPTANCE 收口其中），host 在 initBacklog 后建夹（mkdir+meta.json）并持久化 `journal.runDocs`——重试/续跑复用同夹，双归档/版本虚增/memory 堆积三类 bug 结构性消失；② **slug**：triage 输出协议新增 `slug` 字段（`[a-z0-9-]{3,24}` 校验，非法/缺失退化为 `<date>-r<N>`）；③ **prompt 全改**：TF_DOCS 拆产品层/任务夹两层，`RUN(state)` 读 `state.__runCtx.runDocs`（stateSliceFor 首行注入），删除 VERSION_SLICE_BLOCK 与全部 mv 归档/防双归档话术；PRD 头部三行声明（meta summary/基线依赖/取代）、AC 夹内从 1 编起、修订表废除；④ **SUMMARY.md 废除**（host 扫 meta 聚合，本轮只做注入不做浏览器）、memory.md 收窄为约定层（仅约定变更时幂等更新）；⑤ **VERSION 解耦**：代码头是发布版本，迭代不碰；state.currentVersion→lastRunFolder；⑥ smoke 拆旧断言加 3i 断言组。存量项目零迁移。设计文档 docs/adr/0008-task-folder-docs.md。
- 2026-08-24：**主线程停手契约 + 单调用护栏 + 工程动作承接（实锤 run tf-mt3s9ej7/tf-mt5afdch）**——三连修复：① **主线程停手契约**：`teamflow_start` 返回 render/工具描述/TeamFlow 注入三处一致约束「start 后不得自行改代码/跑验证，等完成汇报」（收敛为 `teamflowContextText()` 单一来源）+ `teamflow_status` 运行中带 `reminder`；requirement 参数强制忠实转写用户原话。实证：无踢墙需求主线程 17s 结束回合零工具调用、requirement 一字未改。② **单调用护栏（`core/guard.ts` 新文件）**：进行中退化检测——复读检测（滑动窗口内同一规范化流式片段 ≥12 次）+ 墙钟兜底 25min → `run.dispose()` 中止，outcome=`degenerated`（命名避开 isUnretryable 正则），豁免预算门允许一次干净重试。根因：熔断只在 withRetry「尝试之间」检查，单调用死循环永不返回时够不着（QA 复读 38min 烧 481 万 token 零产出）。设计原则：**进度信号而非配额**——正常阶段间调用数差 9 倍/计费差 11 倍，硬上限必误杀。③ **工程动作承接 + 杂项**：PRD prompt 新增「工程动作承接」（分支/提交类指令必须落「工程约束」小节，实锤「新起一个分支」被整条流水线静默丢弃）、tech 蓝图传递、dev prompt「先执行动作再写码」；`resumeRun` 重置 `journal.humanIntervention`（否则续跑完成汇报误标 ⚠️）；7 处阶段失败 throw 收口为 `stageFailError()`（真实次数/末次 outcome/熔断语义）。smoke 增补 3h 断言。④ **文档归档/记忆写入幂等（已被 ADR-0008 结构性取代）**：PRD prompt 版本切片加「防双归档」、memory.md 幂等替换语义、DESIGN/TECHNICAL/QA-REPORT 归档同需求草稿跳过条款——本日全部随版本切片制度一并删除。
- 2026-08-21：**QA 打回修复闭环（ADR-0007）**——对照实验 run tf-mt317a5e 实锤流程双缺口：① `parseDefects` 旧正则要求严重级后紧跟 `|`，QA 报告写 `**P1**`（markdown 加粗）→ 缺陷解析为 0 →「QA 未发现缺陷」日志 + 静默进验收（验收源码复核才拦住 BUG-P1-1）；② QA 阶段无「缺陷→打回开发→复验」闭环，缺陷只登记不回流。落地：`parseDefects` 改按管道单元格解析（容忍 `**P1**`/反引号/行首 `|`，只认 id+P0-P3+模块三要素非 OBS 行）；`pipeline.ts` QA 改 `do…while` 有界闭环——P0-P2 阻断缺陷 → `advanceTask('rework')` + 开发修复子代理（新 `qaFixPrompt`，指令「先确认属实→修复→交还复验」）→ 复验，干净才 `pending-acceptance` 进产品验收；`QA_REWORK_LIMIT=2` 超限 → task/req needs-human + humanIntervention，跳过验收；验收块 `if (!qaBlocked)` 门控。`backlog.ts` 新增 `syncQaDefects`（按 reqId+defectId 幂等建卡/刷新）+ `verifyReqBugs`（复验/验收通过关 P0-P2 open 单，P3 观察项保留）。smoke 增补 3g 断言，typecheck/bundle/全套测试通过。
- 2026-08-20：**档位阶段集差异执行落地（ADR-0004 待办清理）**——`host/constants.ts` 新增 `STAGE_POLICY` 五档策略表（单一事实来源）+ `resolveStages()` 纯函数；`pipeline.ts` 的 design/scaffold/qa 门控改由 `enabled()`（档位阶段集 × 团队阶段交集）驱动，**取代散落 if/else**。语义关键：**design/scaffold 全档位按显式 flag 条件化（不吞显式请求，v0.8.1 原则泛化）**；patch 始终无独立 QA。初版曾把「medium 去 scaffold」做成结构化排除，审计发现会吞显式 needScaffold/needDesign → 已回正。新增 `test/stages.test.js`（五档展开行为测试，含「显式 flag 不被吞」回归断言），smoke 增补 `3f`，package.json test 链挂上。
- 2026-08-20：**【认知前置 + 架构落地】重构（ADR-0006，破坏性）**——从「持久化需求 A/B 实测」提炼：流水线质量低于原生的根因是跳过「建全局认知 → 架构决策」。落地：M0 状态核对（`core/sanity.ts`，start 跑 git 现状，注入所有阶段）；M1 架构阶段全模式启用（lite 轻量蓝图 / full-medium 蓝图+文档，`architectPrompt` 允许整读关键文件，产出 `<!-- blueprint -->` JSON）；M2 dev 继承蓝图 + 蓝图自动拆任务（文件冲突检测合并）；M3 QA/验收架构核验（`parseAcceptanceVerdict` 识别架构打回 → rework）；triage 架构护栏（持久化/存储/独立模块 → 强升 medium）。质量优先于 token：不砍「建全局认知」。
- 2026-08-20：**延迟注入修复**——新会话选团队时 agent 可能尚未加载（懒加载），导致 `agent.inject` 静默失败、模型无团队上下文→不走 teamflow。修复：选团队时若 agent 不可用，存入 `pendingInjections` 队列；在 `start`（流水线启动前）和 `getActiveTeam`（UI 加载时）补发注入。
- 2026-08-20：**TOKEN_HYGIENE v2 + PRD head+tail 切片**——通用 token 治理：文件作用域（只 read 任务目标文件）、禁止重复读（同文件 read ≤1 次）、批量修复（一次修完所有失败再跑，最多 3 轮）、AGENTS.md/SUMMARY.md 已注入无需重读；QA/验收 PRD 内联改 head+tail 组合切片（覆盖头部基线+尾部新增 AC，预算不变）。基于 BGM run 实际数据对比正常会话，只砍"正常会话不会做的操作"，零质量损失。
- 2026-08-19：**lite × needDesign 语义修复**：lite 模式不再吞掉显式要求的「UI/UX 设计」——去掉 pipeline 设计阶段闸门里的 `!options.lite`(design 以 `needDesign` 为准启用);lite 仍跳过独立技术方案文档(PRD/变更单即契约)。工具描述/types/triage/README 同步注明「lite + needDesign:true 保留设计阶段」。
- 2026-08-19：`journal.ownerSession` 溯源（发起会话 id，下发于 stageDetail）；跳转子代理按钮增加跨会话判定——ownerSession≠当前会话时禁用并 title/文案提示（DSH 目录按父会话加载、跨父导航待官方 PR）。
- 2026-08-19：PRD/活文档升级改 **mv 归档 + 增量干净文件**（结构上杜绝 edit-in-place）：`VERSION_SLICE_BLOCK` 真正接入 PRD prompt；旧版整文件 git mv 到 history/<旧版本>/，新文件只写增量 US/AC + 压缩回归基线（AC 仅编号+一行语义+指针，严禁照抄全文，防 35KB 巨无霸）；design/tech/QA 归档话术同步。
- 2026-08-19：token 计量收敛为官方口径（去除计费当量/上下文压力自定义概念）：`usage` = 输入未命中/命中/写缓存/输出/调用数 + 缓存命中率；熔断预算用官方总消耗；工作台卡片/任务卡/汇报均按官方口径展示。
- 2026-08-19：流水线视图重设计 —— 横向蛇形流程（相位从左至右、上下波浪错位 + SVG 弧线连接 + 沿路径流动高亮虚线 + 箭头终点），整块画布默认可拖拽平移 + 滚轮缩放 + 适应/± 控制簇，点阵网格背景与步骤序号徽标提升质感；历史 run 选择器门控到流水线 tab。验收结论解析修复为只认「验收结论」行（parseAcceptanceVerdict，误报实锤 tf-msytlok5）。
- 2026-08-19：阶段卡点击查看详情 —— 悬浮于画布右上的浮层（不挤占画布宽度；画布加高至 560；浮层内原生 wheel stopPropagation，滚轮只滚正文不触发画布缩放），官方口径 usage 全字段 + 阶段性产物全文 +「🎬 跳转子代理会话」（sessions.openSubagent，mode one-shot；按钮带引导文案「跳转后请切「对话」tab 查看轨迹」——DSH 未向第三方暴露切视图接口，待官方 conversation.setView PR 后一键直达）；host 新增 stageDetail RPC（读 stage.output 全文）；终态 checkpoint 不再删 stage.output（磁盘+内存保留全文，供详情/断点续跑，smoke 断言同步）。
- 2026-08-19：dev 子卡模型（一个需求一张轮转主卡 + 并行 dev 子卡）、assign 与 status 分离（teamflow_assign）、workspace 级隔离（DSH workspace UUID）、UI"+团队"触发 + teamflow_pause/resume、子代理路由跟随主线程、state.json 预编译索引 + 版本切片/一次成型纪律、backlogUpdate 参数对齐修复。
