# 锚点详情：输出单轨制

> 本文件是 `AGENTS.md` §5 中「输出单轨制」锚点的**论证、实锤与门禁细节**——从 AGENTS.md 原文迁出，
> 内容逐字保留（仅把表格分隔还原为正常 Markdown 段落）。**行为不变量以 `AGENTS.md` §5 为准**；
> 本文件属可检索层，**不注入会话**（同 `docs/devlog.md` 的定位）。

QA/验收文件即产物、回复仅摘要；文件缺失硬失败；parseAcceptanceVerdict 只认显式结论行（结论行字面量模板：最后一行「验收结论：✅/⚠️/❌/📝」），**无结论行 → needs-human（不猜结论）**，📝 需求不适用**只认行首结论写法**（2026-09-17 实测修 bug `tf-mu4i779p-kze5kl`，勿回退为全文命中：报告会在标题/正文里**引用并论证**该词不成立——「为什么不判「📝 需求不适用」」+「并非无需改动」——旧全文匹配据此把 `⚠️ 有条件通过` 判成 `reject`、run 落 failed 且 `humanIntervention` 仍为 false。现规则：剥掉 markdown/列表/表格前缀与结论标签后**该行须以 📝 开头**；句中命中落 needs-human）。**结论行取值的硬约束（2026-09-17 实测 bug `tf-mu4bve7t-duux2k` 修复，勿回退）**：只认**字面量模板行**（行首 `验收结论：`/`整体结论：`/`Acceptance verdict:`/`Overall verdict:`，允许 `## ` 与列表符前缀），多个命中取**最后一个**；**禁止回退为「第一个含『验收结论』的行」**——真实产物里 `## 1. 验收结论摘要` 这类**章节标题**会排在结论行之前，旧实现抓到标题 → 四档词落空 → **明明「验收结论：✅ 通过」却判 failed + needs-human**（把已通过的交付扣在人工上）；也不认 `**验收结论：**` 这类加粗破坏冒号连写的写法（宁严勿松）。门禁：`verdict.test.js` 8 条（含真实结构复刻样本）+ smoke 4 条
