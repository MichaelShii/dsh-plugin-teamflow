# 锚点详情：QA 打回闭环

> 本文件是 `AGENTS.md` §5 中「QA 打回闭环」锚点的**论证、实锤与门禁细节**——从 AGENTS.md 原文迁出，
> 内容逐字保留（仅把表格分隔还原为正常 Markdown 段落）。**行为不变量以 `AGENTS.md` §5 为准**；
> 本文件属可检索层，**不注入会话**（同 `docs/devlog.md` 的定位）。

QA P0-P2 → 打回开发确认+修复（qaFixPrompt）→ 复验 ≤2 轮；缺陷按 reqId+defectId 幂等登记、复验通过关单（P3 观察项保留）；parseDefects 容忍 `**P1**` 加粗（ADR-0007）。**缺陷解析按表头认表**（2026-09-15 修正）：只有**表头显式声明严重级列**（`严重级(P0/P1/P2/P3)` / `Severity (P0/P1/P2/P3)`，兼容 严重度/等级/级别）的表格才解析，列位置由表头决定；无严重级表头的表格**整表跳过**——禁止回退为「第 2 格 ∈ P0-P3 即缺陷」的位置判定（实锤 tf-mu2ioilr-95l4th：QA 的「复验对照表」被登记为 P2 → 每轮复验重生一个阻断缺陷 → 必然超限停线并跳过验收，**自我实现的停线**）；回归门禁 = L2 语料 `qa-report-en-reverify-table.md` + `test/verdict.test.js` 5 条断言。**返工治理三件套（2026-09-15 A/C/B：实测真正的返工来自「修复不完整 / 扫描面不完整」，不是缺陷多）**：**A** 每个 P0–P2 修复必须落**永久可执行门禁**（verify 套件断言，或随检查一起提交的 grep/脚本断言：修复前失败、修复后通过）+ 证据块给 `gate:` 与 `class sweep:`（**类别命中数 before→after**）——policy 级，缺失由 `FIX_GATE_PATTERN` 记 warn 留痕（host 无法证明门禁真存在，不做硬失败）；**B** 缺陷行新增 `检测命令` + `通过判据` 两列（P0–P2 必填）= 缺陷的**可执行定义**（修复方据此验收、复验方据此回归、误报用它当场证伪；表格行改按**未转义**管道符切分并还原 `|`——命令列几乎必然含 `
