# 锚点详情：token 官方口径

> 本文件是 `AGENTS.md` §5 中「token 官方口径」锚点的**论证、实锤与门禁细节**——从 AGENTS.md 原文迁出，
> 内容逐字保留（仅把表格分隔还原为正常 Markdown 段落）。**行为不变量以 `AGENTS.md` §5 为准**；
> 本文件属可检索层，**不注入会话**（同 `docs/devlog.md` 的定位）。

usage=输入未命中/命中/写缓存/输出/调用数+命中率；展示同口径（ADR-0003）。**来源=官方 Session 投影**（`tokenUsage` 四桶 + `sessionStats.steps` 调用数，与宿主 token-meter 同一份 fold）；事件扫描仅为无投影宿主的回退（宿主已弃用 `snapshotEvents/ownEvents` 同步读取，护栏复读检测仍读事件——见 docs/TODO.md）。**熔断走独立的新增口径**（`freshTokensOf` 排除 cacheRead + `FRESH_TOKEN_BUDGET`），两套口径不得合并（§4）。**熔断预算随缓存能力自适应（2026-09-18 probe-v2 实锤，勿回退）**：`FRESH_TOKEN_BUDGET = 200k` 的立论「cacheRead 廉价、新增只是零头」**只对有 prompt 缓存的 provider 成立**——有缓存实测 **1.2–2.4k fresh/次**（115 次调用 280k），不缓存时**每轮调用都要重付 system prompt + 工具定义（实测 ~15.5k/次）**，200k 于是退化成「一个阶段最多约 13 次调用」。故 `metering.effectiveFreshBudget(usage, base)`：**命中率 <50% 且调用数 ≥5** → 预算 ×3（保持原设计「≈两轮尝试」的相对余量），熔断日志走独立措辞 `diag.breakerUncached`（带命中率/调用数/放宽后预算）；有缓存 provider **分毫不动**；汇报口径仍是 `totalTokensOf`。实锤：`inception/mercury-2.5`（命中 10.4%、cacheWrite=0）17 次调用 259k → 熔断；同阶段在命中 90%+ 的 provider 上 17 次调用只需 25–40k。**引擎留痕（2026-09-18，勿删）**：`journal.engine = { provider, model }`（run 起跑由 `resolveChildRoute(parent)` 解析，落盘 + `log.engine` 一行）+ **逐阶段 `stage.provider/model`**（子代理路由可被改道，只记 run 级会答错"哪个模型跑了这个失败阶段"）+ 完成汇报「引擎（模型路由）」一行——**回答"是不是模型的锅"不该再靠解压会话文件翻 `request/header`**（这正是加它的原因）。**持久化面必须立「序列化完整性」门禁**（`test/journal.test.js` [7]）：静态解析 `JournalRecord`/`JournalStage` 接口字段，逐个断言 `serializeJournal` 写出；豁免表只两项（`result` 进程内运行期字段、阶段 `tokens` 历史遗留）且**豁免表本身也被断言**。首跑即抓出四个只写不落盘字段（`branch`/`mergeStatus`/`guardReason`/`guardOutcome`，同型第六次）——**同型教训**：`journal.options`/`execOptions`/start 输出 schema/`loadState`/`triageRecordOf`/`serializeJournal`，凡是"手工枚举的清单"都必须配结构化门禁
