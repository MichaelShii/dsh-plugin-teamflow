# TeamFlow v0.1.2

[中文](#zh) | [English](#en)

Patch release — one host log fix + open-source release polish. No feature changes vs v0.1.0/v0.1.1.

---

<a id="zh"></a>

## 中文

### 本次变更

- **修复（host）**：就绪日志工具数改为动态计数（消除「工具 8 个」写死文案，与实际注册数一致）
- **开源发布收尾**：
  - README 中英双语修正：架构计数 7/6 → 17/11（与实际 17 Remote + 11 模型工具一致）、防假交付「阶段产物全文保留（内存+磁盘）」真实语义、补充快速上手与环境要求、开发段压缩指向仓库 `AGENTS.md` / `docs/adr`
  - npm 元数据：`keywords` / `homepage` / `bugs`；`react` 移入 peerDependencies（宿主注入）；`peerDependenciesMeta` 将宿主私有包标 optional（防普通 `npm install` ERESOLVE）
  - CI 触发分支扩展为 `main` + `release-*`；构建关闭 sourcemap（npm 包体积 -58%，219.9kB → 93.1kB）
  - 新增 `SECURITY.md`（漏洞报告渠道 + 安全边界）与 `CONTRIBUTING.md`（环境 / 结构 / 约定 / PR 流程）
  - `deploy.mjs` 同步清单移除 `.map` 文件

### 安装

```bash
dsh plugin --profile web add dsh-plugin-teamflow
```

### 资源

- 使用与架构：[README.md](./README.md)
- 变更明细：[CHANGELOG.md](./CHANGELOG.md)

---

<a id="en"></a>

## English

### Changes

- **Fix (host)**: ready-log tool count is now dynamic (removes the hard-coded "8 tools" text, matches actual registrations)
- **Open-source release polish**:
  - Bilingual README fixes: architecture counts 7/6 → 17/11 (matching actual 17 Remote + 11 model tools), "full stage outputs retained (memory + disk)" true semantics, added quick-start and requirements sections, dev section condensed to point at repo `AGENTS.md` / `docs/adr`
  - npm metadata: `keywords` / `homepage` / `bugs`; `react` moved to peerDependencies (host-injected); `peerDependenciesMeta` marks host-private packages optional (avoids ERESOLVE on plain `npm install`)
  - CI triggers extended to `main` + `release-*`; sourcemaps off (npm package -58%, 219.9kB → 93.1kB)
  - Added `SECURITY.md` (reporting channel + security boundary) and `CONTRIBUTING.md` (environment / structure / conventions / PR workflow)
  - `deploy.mjs` sync list no longer references `.map` files

### Install

```bash
dsh plugin --profile web add dsh-plugin-teamflow
```

### Resources

- Usage & architecture: [README.md](./README.md)
- Changelog: [CHANGELOG.md](./CHANGELOG.md)